# AI-VIDEO Contract Routing Matrix

## Vidu Cloud Adapter

`src/ai_video/production/vidu.py` 独占 Vidu payload/status/download mapping，
`vidu_profile.py` 独占 dated profile、能力、下载信任模式与 per-call ceiling；
`vidu_download.py` 独占 credential-free public-IP pinned HTTPS（原域名 TLS、无 redirect）；`vidu_source.py`
只验证延长输入的 canonical submit/fetch/probe receipts，不拥有 lifecycle。显式 registry 注入，
沿既有 `VideoGenerationService` / `ProductionStateCommitter` lifecycle；不得自动选路、
fallback、重试 POST 或 activation。Profile hash、task/model、creation ID、exact egress、
one-use permit 与 sealed download trust 均 fail closed。显式 `authenticated_task` 无需
预知 CDN；默认 `fixed_origins` 保持旧 profile hash，额外约束 exact origin。
Focused verification：`python -m pytest -p no:cacheprovider tests/test_production_vidu.py tests/test_production_vidu_download.py -q`；
Harness 路由为 `production_video_provider`，包含 `production_vidu_tests` 与既有
Provider / neutral requirement / Architecture checks。配置边界见 [Vidu Provider](vidu-provider.md)。

本文件是修改代码前使用的契约路由索引。它回答四个问题：谁拥有该行为、哪些不变量必须保持、哪些替代路径禁止出现，以及最小相关验证是什么。

本文件不是 runtime 状态总账、phase tracker、implementation authorization 或历史验收记录。当前实现真相、阶段方向与 Agent 权限分别由下表中的 canonical source 管理；代码、测试和已验证运行时行为始终优先于文档摘要。

## Document Roles

| Concern | Canonical Source |
| --- | --- |
| Agent authority、工作边界、decision gates、Git 与 completion rules | `AGENTS.md` |
| Changed-path category、mandatory checks 与 local run receipt | `.agent/harness/policy.yaml`、`scripts/agent_harness.py`、`.agent/harness/runs/` |
| Canonical spec metadata、finite status vocabulary 与 static contract anchors | `.agent/harness/docs-contracts.yaml`、`scripts/docs_contract_gate.py` |
| 当前已实现行为、local/origin/release truth、已验证与未验证边界 | `docs/v0.2-runtime-baseline.md` |
| Phase dependency、status、future gates 与 slice direction | `docs/v0.2-agentic-production-roadmap.md` |
| 单个 slice 的 accepted scope、tradeoff 与 implementation detail | active spec / plan |
| Executable behavior | source code、tests 与本轮实际 runtime evidence |

使用方法：先用 `make harness-inspect` 根据 task-owned changed paths 查看 mandatory checks，再定位受影响的 surface，确认唯一 owner，保持 `Invariants`，不得引入 `Forbidden Alternate Path`。完成前运行 `make harness-verify` 并保留 passing receipt。下表的 `Focused Verification` 是 human-readable contract；`.agent/harness/policy.yaml` 是对应 executable routing，二者变更时必须同步。

## Cross-Cutting Contracts

| Contract | Invariant |
| --- | --- |
| Product Isolation | Legacy `0.1.x` CLI/Manifest/flat run layout 与 v2 Production Python APIs 保持隔离；除非独立获批，不得借 v2 change 修改 Legacy public surface。 |
| Mutation Ownership | `ProductionStateCommitter` façade及其 private implementation modules 是 v2 project、registry、render、graph、review、repair、image evidence、paid-provider state、generated-video lifecycle、Manifest activation 与 explicit recovery 的唯一 writer owner。 |
| Timeline and Dependency Ownership | `ResolvedTimeline` 独占 order/frame/sample/timing；P5 resolver 独占 desired fingerprint、precise invalidation 与 rebuild frontier。其它模块只能消费，不得复制推导。 |
| Replay and Recovery | Exact replay 不重复 provider、renderer、analyzer 或 Manifest write；unknown outcome fail closed；recovery 必须显式且不得 blind retry、remint permit、猜测 mixed state 或删除 complete orphan evidence。 |
| Local-First and Provider Safety | Legacy 默认只连接 local ComfyUI；v2 local adapter 必须 loopback-only。严格loopback、完全local/unmetered且无cloud egress的ComfyUI lifecycle、generation、retry、variant与benchmark按`AGENTS.md`无需user authorization；该豁免不放宽exact identity、canonical seam、permit、provenance、unknown-outcome、recovery或media gates。任何remote/paid Provider、secret或cloud egress继续受decision gates约束，且不得成为fallback。 |
| Evidence Truth | Immutable evidence 必须 content-addressed 并绑定 exact selected inputs。Plan、console text、Agent memory 或 heuristic 不得升级成 runtime、semantic acceptance 或 quality acceptance。 |
| Error Model | 跨模块失败使用 `AiVideoError` 与 `ErrorCode`；retryability 由 typed metadata 决定，常规 CLI 输出不得泄露 raw traceback。 |
| Development Governance Isolation | Codex、`AGENTS.md`、`.agent/`、`.agents/`、`.codex/`、`.workflow/`、Development Verification Harness、Documentation Contract Gate、Architecture Gate 与 developer-only Agent Memory 只拥有开发治理。`src/ai_video/**` Product Runtime 不得 import、执行或依赖这些 artifacts，也不得把 `.workflow/` session 当作 production session。 |
| Agent Session Fact Domain | 当前没有通用 Product Agent Loop、Tool Registry 或 event-sourced Product Session。未来若引入 Product Agent Session，其 append-only events 只能拥有 model-visible conversation、decision context 与 non-authoritative telemetry；Project、Registry、Manifest、Provider effect、activation、review、repair 与 delivery truth 继续由既有 domain owners 独占。 |

## Provider Console Browsing Surface

| Surface | Primary Owner | Invariants | Forbidden Alternate Path | Focused Verification |
| --- | --- | --- | --- | --- |
| Unified Video Library | `provider_console.py` 的 strict detail、`provider_console_render.py` 的已验证 active render 投影与 `provider_console_media_index.py` 的 evidence association；`runs-api.mjs` / `external-media.mjs` 的本机 transport；`library-contract.js` / `library-data.js` / `library-browser.jsx` 的临时浏览状态 | 内容按 SHA-256 + bytes 去重并保留来源和角色；成片只消费 strict reader 的 active render，恢复旁证不得提供 render token；版本只来自 verified workspace/project/Shot identity；所有来源使用同一筛选；手动 pin 不随刷新变化；播放与双视频比较只读，实测值来自 exact 视频解码；请求、预览 decoder 与分页有界。新增 library contract/browser/media Node tests 与现有 bridge checks 共用 `provider_console_node_tests`。 | 裸扫 fetch、第二 catalog/state owner、相似标题推导版本、任取歧义 Prompt/verdict、把播放或旁证提升为 P6/Final Acceptance、刷新期间提前删除有效媒体 cache、远端 fallback、Provider/Manifest mutation。 | `python -m pytest -p no:cacheprovider tests/test_provider_console.py tests/test_provider_console_media_index.py -q`；`node --test provider-console/tests/library-contract.test.mjs provider-console/tests/library-browser.test.mjs provider-console/tests/library-media.test.mjs provider-console/tests/runs-api.test.mjs provider-console/tests/external-media.test.mjs`；offline Vite compile 与 exact snapshot Harness；真实本机 A1–A10/browser QA 另验。 |

## Development Control Surface

| Surface | Primary Owner | Invariants | Forbidden Alternate Path | Focused Verification |
| --- | --- | --- | --- | --- |
| Local ComfyUI Supervisor | `scripts/comfyui_supervisor.py`、`.agents/skills/h3-video/SKILL.md` | 本地ComfyUI必须由每次`start`新建且不可复用的user-systemd transient unit `ai-video-comfyui-<32-lowercase-hex>.service`持有，固定loopback listen与health endpoint；systemd strict-namespace unit enumeration是唯一owner discovery，不保存第二套pointer/job state。User runtime advisory mutex串行化完整start preflight、creation与health/final-state确认，使并发caller只能在前一调用结束后重新检查owner/port。`start`仅显式启动且`Restart=no`，存在任一strict匹配unit或端口占用时fail closed，成功前同时确认health、active state、non-zero MainPID、同一systemd `InvocationID`，并从`/proc`确认loopback listener socket由该MainPID持有；创建后的每个failure path只清理本次unique unit。`status`/`logs`只读，多个strict匹配unit时拒绝ambiguous control；`stop`只作用于发现的单一strict unit。Supervisor不自动启动、重启、提交workflow或恢复generation。 | Agent统一exec/nohup拥有长进程、后台shell子进程、固定/可复用unit name、用非exact prefix吞并unrelated service、外置可漂移pointer state、未串行化的check-then-start、仅凭共享port health冒充本unit readiness、端口占用时接管、broad pattern stop、多个owner时猜测、post-create failure泄漏unit、自动restart/start、把service active冒充Provider/media/quality PASS。 | `python -m pytest -p no:cacheprovider tests/test_comfyui_supervisor.py tests/test_agent_harness.py -q && python scripts/comfyui_supervisor.py status`；符合`AGENTS.md` local ComfyUI exemption的真实`start`/`stop`无需用户授权，但用户明确的read-only或禁止media effects仍须停止。 |
| Development Verification Harness | `scripts/agent_harness.py`、`scripts/agent_harness_policy.py`、`scripts/agent_harness_proof.py`、`scripts/agent_harness_audit.py`、`scripts/agent_harness_workspace.py`、`scripts/agent_harness_runtime.py`、`scripts/agent_harness_receipt.py`、`scripts/harness_pytest_guard.py`、`.agent/harness/policy.yaml`、`Makefile`、`.github/workflows/mandatory-gate.yml` | Completion scope只接受non-empty staged delta或commit range，并在detached worktree验证exact snapshot；commands使用argv + `shell=False`、timeout与sanitized env；pytest Python process限制为loopback socket/DNS API，pytest child process及Node/npm command不在OS级network isolation内。Checks按policy priority先执行scope/docs/policy-audit/task Architecture Gate，再执行较昂贵suite；always-selected policy audit在exact detached snapshot发现unmapped、unverified、missing或unreferenced owned/test path时fail closed并停止后续checks。receipt绑定scope/policy/env/check artifact bytes/self-hash，freshness verifier还从exact commit/index policy重算scope、routing、ordered selection、argv/cwd与每条passed/skipped record。Policy-declared coverage只在全部covering checks于同一exact-snapshot run实际通过后抑制covered command；full pytest只能显式覆盖其真正包含的Python suites，不能吞并Node/build checks。Covered check仍保留selected truth并记录same-run skip依据；audit要求tracked Python及声明的JS/MJS tests被check argv直接引用，只有明确记录的broad-selector exemption例外；unknown path fail safe到full tests + task-delta Architecture Gate。Provider Console按bridge、web、Sites/Worker职责分路由：web source另做offline client compile，只有bundle/package/Worker/Sites路径承担完整Sites build。Node dependencies必须先在source checkout按exact lock安装；Harness只在installed lock与snapshot lock一致时将该tree临时链接到detached checkout，本身不安装。显式使用PR时，PR gate先inspect exact event base/head，只有selected checks声明Node workspace dependency时才用pinned Node和`npm ci --ignore-scripts`预置dependencies，并由GitHub runner独立生成receipt/JUnit/log artifacts；当前server enforcement允许直接push，但阻止force push/deletion。 | `--path` completion proof、空scope pass、在shared dirty tree运行checks、把sanitized env误当OS级network sandbox、默认check启动可联网child process、未校验lock就链接host dependencies、让Harness自行安装dependency、用historical baseline debt污染task gate、把policy audit留到expensive suite尾部、只凭`status=passed`或self-hash接受不完整receipt、把stale/tampered或开发者提交的receipt当CI proof、静默忽略policy/routing drift、未通过covering check就跳过covered command、用partial suite反向覆盖full tests、让full pytest静默覆盖Node/build checks、把workflow存在或workflow-only push冒充server enforcement、CI读取Provider secret或运行live/paid smoke。 | `make harness-test && make harness-audit && python scripts/agent_harness.py inspect --path .github/workflows/mandatory-gate.yml && python scripts/agent_harness.py inspect --path provider-console/tests/sites-worker.test.mjs`；staged completion用`make harness-verify`，commit completion用`make harness-verify-range BASE_REF=<ref>`并以`make harness-receipt RECEIPT=<path>`检查exact policy/scope/routing/check records与artifact hashes；远端另验证workflow run/check URL、active ruleset ID/API payload与`main` protection truth。 |
| Documentation Contract Gate | `.agent/harness/docs-contracts.yaml`、`scripts/docs_contract_gate.py`、canonical spec YAML front matter | Registry只属于Development Governance；`surface_id`与active canonical spec唯一，spec metadata exact匹配finite statuses，owner/evidence paths与Harness check IDs存在，Python assertions只做AST/static parsing。`docs_contract_check`是Harness always check且由documentation category显式拥有，因此code-only delta也会运行。Runtime baseline继续拥有动态implementation/evidence状态。 | Product Runtime import gate/registry、执行module读取constant、网络/Provider/credential/write、第二runtime registry、以regex/LLM宣称理解全部自然语言、用full tests或Architecture Gate替代docs contract check。 | `python -m pytest -p no:cacheprovider tests/test_docs_contract_gate.py tests/test_agent_harness.py -q && python -m scripts.docs_contract_gate check && make harness-audit`；arbitrary narrative与architecture semantics仍需human/Codex review。 |
| Raw Creative Input Director Strategy Preflight | `.agent/context/control-plane-playbook.md`是routing owner；`.agents/skills/open-video/SKILL.md`与`.agents/skills/open-video/scripts/validate_director_coverage.py`拥有advisory input optimization、strategy/coverage procedure和deterministic evidence validation | 尚无approved Shot/ordered coverage的raw input，无论missing、vague direction、concept/script、reference-led brief或draft prompt，一律先进入`open-video`；prompt presence/detail不等于approval。Agent保留explicit user constraints，补齐有界creative gaps，并根据narrative beats、action/space progression、camera/blocking trajectory、pacing、continuity风险与明确user preference选择`single_take`或`multi_shot`。Validator验证input kind/evidence、declared constraint inventory结构、verbatim source anchoring、global/beat-specific coverage binding、strategy/source/rationale、duration sum、finite categories与coverage结构一致性；它不证明constraint extraction完整或natural-language semantic entailment。Agent必须依次review `raw evidence -> inventory`是否漏提explicit constraints，以及`inventory -> coverage`是否遗漏、反转或擅自改写。Provider capability仍由current profile owner校验。只有已批准且不修改creative intent/coverage的Shot可跳过。 | raw prompt直接进入Provider-specific authoring、用“已有prompt”冒充approved Shot、优化时覆盖user hard constraints、漏提、漏绑或伪造constraint IDs、把declared inventory completeness冒充exhaustive extraction或semantic consistency、从时长或Provider capability推导creative strategy、伪造user evidence、以连续extend冒充`multi_shot`、validator PASS冒充Shot approval/Provider authorization/media quality/P6/Final Acceptance，或让Product Runtime依赖Agent Skill/script。 | `python -m pytest -p no:cacheprovider tests/test_open_video_skill.py tests/test_seedance_authoring_skill.py tests/test_runtime_skill_boundary.py tests/test_agent_harness.py -q` |
| Seedance Authoring Skill | `.agents/skills/seedance-authoring/**`；`AGENTS.md`只存durable routing summary，`.agent/context/control-plane-playbook.md`是detailed routing owner | 只消费approved Shot/Generation Requirement、preselected exact Seedance model/profile identity、version/mode/runtime surface和semantic reference roles；shared authoring knowledge加exactly one version overlay。Mode是profile，不拆Skill；output仅为advisory prompt/reference expression，必须回到existing AI-VIDEO contracts，`runtime_skill_calls = 0`。 | 第二router/capability registry、asset manifest/provider tag compiler、runtime/API/ComfyUI执行或validation、Provider selection/lifecycle、continuity/P6/Final Acceptance/Harness ownership，或让`src/ai_video/**`依赖Skill。 | `python -m pytest -p no:cacheprovider tests/test_seedance_authoring_skill.py tests/test_runtime_skill_boundary.py tests/test_agent_harness.py -q` |
| AI-VIDEO Session Record Hook | `.codex/hooks.json`、`.agents/skills/record-ai-video-session/SKILL.md`、`.agents/skills/record-ai-video-session/scripts/session_record_hook.py`、`docs/record_for_agent/2026-08-21-session-evidence-automatic-recording-and-rag-boundary.md` | `SessionStart`初始化session-local `ACKED`空scope；`PostToolUse`只从本session的`apply_patch`或simple exact `git add <specific-files>`登记task-owned paths，并排除`docs/record_for_agent/`。checkpoint fingerprint只绑定这些path的当前bytes/type，不绑定全仓`HEAD`、index或unrelated dirty/untracked state；task-owned content变化进入`PENDING`。每个session state的load/update/write与ACK由同目录mode-`600` file lock串行化；显式path list达到bounded cap后，以event-scoped overflow digest继续保证后续归因事件重开`PENDING`，不得静默丢失。`PreCompact`对同一pending fingerprint最多注入一次strong reminder，`Stop`只对未ACK的session-owned checkpoint返回同一`capture_request_id`的`decision: block`；recursive Stop不ACK。`recorded` ACK必须携带`learning_outcome=no_candidate|pending_candidate`，`no_record`只接受`not_applicable`，随后才回到`ACKED`。是否达到stable boundary、durable record写入与distillation outcome本身继续由对应Skill独占；hook state只写`.git/ai-video-session-record-hook/`且Product Runtime不得依赖。 | hook使用全仓fingerprint猜session ownership、解析chained/broad/glob Git command扩大ownership、无锁覆盖并发path attribution、达到path cap后静默丢ownership、因unrelated HEAD/index/untracked变化重开checkpoint、recursive Stop自动ACK、record artifact/commit触发自身、伪造或猜测`capture_request_id`、`recorded`但无learning outcome就ACK、直接写`docs/record_for_agent/`、注册global hook、运行Provider/tests/network/media、读取transcript/raw secret、把hook request当作runtime truth或制造无ACK出口的Stop loop。 | `python -m pytest -p no:cacheprovider tests/test_record_ai_video_session_hook.py tests/test_agent_harness.py -q` |
| Experience Learning And Confirmation | `.agents/skills/distill-ai-video-learning/**`、`docs/record_for_agent/learning/**`；`record-ai-video-session`只拥有classification/Evidence Index capture与automatic handoff/ACK enforcement，Agent Memory只拥有authority-preserving retrieval和allowlisted metadata projection | stable record后automatic evaluation；`recorded` ACK必须携带`no_candidate|pending_candidate`。Opt-in empirical record以`evidence_index_version: "1"`声明identity spine；distillation唯一按`independence_key`去重support/counter，保留same-attempt technical/human等proof layers，并以typed relation区分new attempt、new proof、conclusion supersession与input reuse。Admission只接受`TWO_INDEPENDENT_ATTEMPTS`、`CONTROLLED_MULTI_ARM`或`MATERIAL_EXISTING_CLAIM_UPDATE`，由Skill-local read-only validator对显式paths fail closed；legacy/`needs_identity`仍可检索但不自动admit。Agent Memory只投影`record_kind/topic_id/learning_eligibility/evidence_index_version`，不改变ranking、path-owned authority/document kind或count。Active adopted lane与pending candidate lane分离；candidate checkpoint只含claim path，确认绑定exact commit与其中file bytes SHA-256及sanitized actor/time/evidence pointer；`CONFIRMED`不等于`ADOPTED`，只有target owner完成mandatory verification后pending才替换active。 | 用document、record link、RAG hit/chunk、artifact version、proof layer或same evidence多次引用冒充independent units；从path/chunk合成key；`INPUT_REUSE_ONLY`进入support/counter；模糊identity时猜测/fallback；frontmatter伪造learning authority或metadata改变ranking/admission；Product Runtime/Provider自动学习caller、RAG score充当confidence、单case泛化、pending覆盖active、无immutable confirmation preimage、无user confirmation修改Skill/Policy/Preflight/Contract/Gate、generic patch engine、自动Provider selection/retry/media/activation/P6/Final Acceptance/target commit/push/release、Learning Claim成为第二Production truth。 | `python -m pytest -p no:cacheprovider tests/test_distill_ai_video_learning_skill.py tests/test_experience_evidence_identity.py tests/test_record_ai_video_session_hook.py tests/test_agent_memory.py tests/test_agent_harness.py tests/test_runtime_skill_boundary.py -q && python -m scripts.architecture_gate check` |
| Generated-Video Analysis Hook | `.codex/hooks.json`、`.claude/settings.json`、`src/ai_video_mcp/server.py`、`src/ai_video_mcp/serialization.py`、`src/ai_video_mcp/analysis_hook.py`、`tests/test_mcp_analyze.py` | Codex与Claude Code project-local `PostToolUse`复用同一queue，只从structured successful tool result或Claude MCP `content[].text` JSON接受fresh、existing、non-symlink、repo-contained local MP4；relative path按event cwd解析。Queue先跳过existing exact identity再应用单event最多64个new output的cap，多个outputs只启动一个detached batch；overflow必须显式报告且repeated event仍能继续排队。plain-text path、failed result、旧文件、repo外文件与`video-analysis`自身结果均忽略。Hook只以mode-`600` job写入`.git/ai-video-analysis-hook/`，不在hook timeout内运行media work；spawn失败必须删除本batch exclusive jobs使后续event可重试。同repo全部显式MCP tool calls（包括不同Agent的独立stdio processes）与background analysis共用mode-`600` worker lock全局串行；explicit waiter在取得lock前保持async cancellation-aware，已取消的waiter不得随后启动operation；operation启动后的cancellation必须继续持锁直到worker真正结束。等待lock不改变tool schema/output，也不等于共享process-local Whisper/model cache。Worker从held source FD复制mode-`400` private MP4 snapshot，只向现有`video_analyze` core传入该snapshot；写result前必须重验snapshot SHA-256、source SHA-256与queued metadata，随后删除snapshot。result/lock identity绑定exact source SHA-256与automatic analysis-profile fingerprint；automatic profile固定为最多4帧、scene detection开启、Whisper关闭。Claude-compatible client的model endpoint/credential由client config独占；MCP/hook不保存、不选择也不调用语义模型。result只是不进入Product Runtime的advisory raw evidence，`production_verdict=null`。显式MCP调用仍保留，P6 Production review仍只能从durable intent与committer-issued one-use permit进入。 | 扫描工作区猜新视频、从free-form prose提取路径、同步运行ffmpeg/Whisper、无界fan-out或并发media analysis、为显式调用与hook建立不同lock owner、已取消waiter之后ghost执行、operation仍在运行时提前释放lock、把execution串行冒充single process或single GPU-resident model、跳过existing identity前截断导致valid output永久遗漏、直接分析mutable source path、复用不同analysis profile的旧result、让hook/MCP保存或选择Claude-compatible endpoint/model/credential、分析submit/poll/failed/旧artifact、repo外或symlink path、递归消费自身analysis结果、运行Provider/network/paid effect、自动retry generation、写Manifest/Registry/activation/review/repair/final acceptance、把queue/result当作P6 PASS或delivery truth。 | `python -m pytest -p no:cacheprovider tests/test_mcp_analyze.py tests/test_record_ai_video_session_hook.py tests/test_agent_harness.py -q` |

## Quality Intelligence Advisory Surface

| Surface | Primary Owner | Invariants | Forbidden Alternate Path | Focused Verification |
| --- | --- | --- | --- | --- |
| QualityExperienceRecord v1 Q0 Passive Capture | `src/ai_video/quality_intelligence/**`、explicit non-Production `pilot_dataset_root/quality-experience/v1/` | `QualityExperienceStore`只写显式dataset root；schema `1.1`以独立`continuity_evidence` binding承载evaluator pointer/fingerprint/hashes并与record artifact/Shot/constraints cross-check，保持`1.0`兼容；P6 observations只承载strict-reopened Review/Repair/Final Acceptance receipts，累积repair outcomes固定`stale`。`capture_post_qa_quality_experience()`只接受exact project/attempt selector与strict Q0 advisory/analyzer/human context；runtime planning/routing/provider/prompt/parameters/inputs只从Manifest 2.10 checkpoint、artifact/probe/provenance/evaluator/Registry evidence strict派生，未持久化lineage为tagged `not_applicable`，caller不得覆盖。Analyzer `analysis/**`与human `reviews/**` documents均no-follow exact reopen；human metadata还绑定fallback hash/evaluation fingerprint。Pure adjudicator派生PASS/FAIL的`continuity_score=1.0/0.0`，`NOT_EVALUATED`及任何stale/incomplete evidence fail closed。prospective record一attempt一deterministic `AttemptIdentityKey`，same key/same bytes为zero-write、same key/different bytes为typed conflict；cohort/roster/index closure、historical import、exact lookup与advisory projection既有contract不变。 | Q0写Manifest/Registry/P6/P8、引入第二Production writer/QA/acceptance truth、caller提交runtime projection、continuity evidence与record artifact/Shot/constraints内部矛盾、历史P6 outcome冒充fresh、把capture插入Provider/committer/evaluator/reviewer/recovery/activation callback、运行Provider/probe/analyzer/evaluator或自动创建Pilot index、mutable `latest`、让`lab_only`进入Pilot index、raw prompt/Provider response/signed URL/credential/private path/reviewer PII进入record/error/repr/fixture/projection、projection写docs或build/search Agent Memory。 | `python -m pytest -p no:cacheprovider tests/test_quality_experience_capture.py tests/test_quality_experience_models.py tests/test_quality_experience_store.py tests/test_quality_experience_dataset.py tests/test_quality_experience_rag_projection.py tests/test_production_continuity_evaluator.py tests/test_production_generated_video_e2e.py tests/test_agent_harness.py -q`；另运行`python -m scripts.architecture_gate check`。 |

## Legacy Runtime Surfaces

| Surface | Primary Owner | Invariants | Forbidden Alternate Path | Focused Verification |
| --- | --- | --- | --- | --- |
| CLI | `src/ai_video/cli.py` | Public commands 保持 `validate`、`run`、`resume`；`validate` 无副作用；typed failure 转成用户可理解的输出。 | 在其他模块解析 CLI、增加隐式 v2 command、绕过 typed error mapping。 | `python -m pytest tests/test_cli.py -q` |
| Config and Paths | `src/ai_video/config.py` | YAML + Pydantic loading；持久化路径为干净绝对路径；non-local ComfyUI 必须 explicit opt-in。 | 在 pipeline/render/client 中重复路径策略或默认放行 remote host。 | `python -m pytest tests/test_config.py -q` |
| Workflow Loading | `src/ai_video/workflow_loader.py` | API JSON 与 UI workflow JSON 都走标准 production loader。 | 裸 JSON/YAML parsing 形成第二条 UI-to-API conversion path。 | `python -m pytest tests/test_workflow_loader.py -q` |
| Workflow Rendering | `src/ai_video/workflow_renderer.py` | Template + binding + shot context 的转换保持纯逻辑、no-network，并提供可定位的 binding path error。 | 在 CLI/pipeline 写死 node ID 或在 renderer 内执行 transport。 | `python -m pytest tests/test_workflow_renderer.py -q` |
| ComfyUI Transport | `src/ai_video/comfy_client.py` | 只负责 HTTP transport、polling、artifact collection 与 typed transport failure；不知道 Shot orchestration。 | 在 client 中实现 Shot order、resume、state activation 或 remote fallback。 | `python -m pytest tests/test_comfy_client.py -q` |
| Pipeline and Resume | `src/ai_video/pipeline.py` | 顺序 Shot execution、bounded typed retry、optional last-frame chaining；resume 从既有 manifest 恢复并跳过仍有效工作。 | 对同一 `run_id` 再调用 `run()` 模拟 resume，或使用 blanket downstream rerun。 | `python -m pytest tests/test_pipeline.py tests/test_resume_e2e.py -q` |
| Legacy Manifest and Output | `src/ai_video/manifest.py`、`src/ai_video/pipeline.py` | Manifest 原子写入；artifact hash 代表已持久化 bytes；flat `runs/<run_id>/` layout、terminal state 与 `final_output` 保持可恢复。 | 非原子终态写入、带 `..` 的持久化路径、无 schema authorization 的 layout mutation。 | `python -m pytest tests/test_manifest.py tests/test_pipeline.py -q` |
| ffmpeg | `src/ai_video/ffmpeg_tools.py` | 负责 probe、clip validation、frame extraction、normalize 与 stitch；stream-copy 不可用时使用既有 re-encode fallback。 | 在 pipeline 复制 ffmpeg policy，或把 v2 renderer ownership隐式并入 Legacy helper。 | `python -m pytest tests/test_ffmpeg_tools.py -q` |
| Architecture Gate | `scripts/architecture_gate/**`、`architecture_gate.toml` | Gate只做deterministic local Python source analysis。Schema v2的`module_sets`、`dependency_rules`与exact dated exceptions是human contracts的唯一machine projection；repository rule values只能来自TOML。LOC/fan-out/cycle继续使用historical baseline ratchet，direct runtime import rules始终对current snapshot绝对求值，`update-baseline`不得豁免；unknown顶层field、错误schema type、active但零匹配的module set与syntax evidence缺失均fail closed。Function-local direct imports进入dependency rules，标准`TYPE_CHECKING` guard不进入runtime rules；Schema v1保持metrics-only compatibility。 | 独立architecture manifest或Skill rule copy、把Harness categories解释为module ownership、在Python source hard-coderepository rule list、通过刷新baseline或wildcard exception隐藏dependency violation、复用Production dependency graph、把static PASS解释为public API、canonical writer、runtime call graph、SDD compliance或media/Production acceptance、机械拆散cohesive transaction lifecycle。 | `PYTHONDONTWRITEBYTECODE=1 python -m pytest -p no:cacheprovider tests/test_architecture_gate.py tests/test_architecture_gate_cli.py -q && PYTHONDONTWRITEBYTECODE=1 python -m scripts.architecture_gate check --base-ref HEAD --format json`；dynamic imports、任意`TYPE_CHECKING` alias、re-export、Node/workflow与runtime dataflow仍需其他evidence。 |

## Production Runtime Surfaces

| Surface | Primary Owner | Invariants | Forbidden Alternate Path | Focused Verification |
| --- | --- | --- | --- | --- |
| P2 Production Reader | `src/ai_video/production/models.py`、`hashing.py`、`paths.py`、`validation.py`、`registry.py`、`project.py` | Strict/read-only/no-network loading of Manifest-selected exact project/registry revision；semantic hash、reference 和 root containment 必须验证。 | 在 reader/registry/validation 中写入、恢复、激活 state，或绕过 exact selected revision。 | `python -m pytest tests/test_production_models.py tests/test_production_validation.py tests/test_production_registry.py tests/test_production_project.py tests/test_config.py tests/test_cli.py -q` |
| P2A State Commit and Recovery | `src/ai_video/production/state_commit.py`、`_state_commit_*` | Single committer owner；initial bootstrap 先在 scratch strict reopen exact Project/Registry referenced-artifact closure，再写 immutable snapshots 并以 Manifest 2.0 revision 1 原子激活；bundle-identity exact replay 为 zero-write；immutable snapshot write/fsync/promote/reopen；final Manifest replace 是 logical commit point；recovery 只接受 exact old/new state。 | 第二 writer、未引用/缺失的 bootstrap artifact、reader auto-recovery、overwrite promotion、schema downgrade、猜测或自动激活 orphan。 | `python -m pytest tests/test_production_bootstrap.py tests/test_production_state_commit.py tests/test_production_state_recovery.py tests/test_production_models.py tests/test_production_project.py tests/test_production_state_commit_structure.py -q` |
| P3 Composition and HyperFrames | `src/ai_video/production/composition.py`、`_hyperframes_source.py`、`hyperframes.py`、`visual_media.py` | `ResolvedTimeline` 是唯一时序真相；local raster 与带 exact `VideoAssetMetadata` 的 H.264 MP4 visual spans 都进入同一个 pinned local HyperFrames path；video trim必须精确覆盖resolved Shot；caption `font_family` 只能使用 pinned renderer 同名 bundled canonical family 或 supported CSS generic，unsupported/alias-substituted family 必须在 source staging 前 typed fail closed；selection 先 durable，verified source/output/state 后才能原子 activation。 | 第二 timeline、alternate renderer fallback、native video audio绕过P4 mixer、未验证 output activation、任意 custom font、silent font alias/fallback、伪造 `local()` font-face、禁用 renderer lint、导出低层 runner 形成旁路。 | `python -m pytest tests/test_production_composition.py tests/test_production_hyperframes.py tests/test_production_state_commit.py tests/test_production_state_recovery.py -q` |
| P4 Voice and Captions | `src/ai_video/production/audio.py`、`captions.py`、`elevenlabs.py` | Provider-neutral sealed request；budget/egress preview 和 durable intent precede submit；`CaptionTrack` 是 canonical structured source；audio/caption timing 仍归同一 `ResolvedTimeline`。 | 第二 audio timeline、burned-in pixels 充当 caption truth、blind provider retry、ElevenLabs implicit/default path。 | `python -m pytest tests/test_production_audio.py tests/test_production_captions.py tests/test_production_elevenlabs.py tests/test_production_voice_captions_e2e.py tests/test_production_state_commit.py tests/test_production_state_recovery.py -q` |
| P5 Dependency and Selective Rebuild | `src/ai_video/production/dependency.py` | Graph snapshot immutable 且不存 mutable lifecycle；Manifest 独占 desired/applied states；canonical desired recursion 与 precise transitive invalidation保持不变；render unit 只能 final activation 后一起 fresh。 | Blanket stale、第二 resolver、graph 推导 timeline、单独推进 composition/timeline/source/render node。 | `python -m pytest tests/test_production_dependency.py tests/test_production_selective_rebuild.py tests/test_production_models.py tests/test_production_project.py tests/test_production_state_commit.py tests/test_production_state_recovery.py -q` |
| P6 Review and Repair | `src/ai_video/production/review.py`、`continuity_evaluator.py`、`continuity_onnx_backend.py`、`local_continuity_reviewer.py`、`src/ai_video_mcp/tools/review.py`、`optimize_plan.py`、`apply_optimization.py` | `video-analysis`只收集raw evidence；Manifest/committer拥有review/repair/final-acceptance lifecycle；semantic PASS需要policy-selected durable evidence。Continuity evaluator只提交exact request/MP4/constraints/profile-bound raw measurements，verdict继续由P6 deterministic adjudicator派生。Hybrid v1必须以sealed RGB sampler + detector/ReID track证明motion、signed entrance/exit与same-track re-entry；coverage/confidence/identity stability不足一律`not_evaluated`。CUDA profile必须绑定exact model/conversion/runtime/preprocess/decode/NMS/provider bytes且禁CPU fallback；local assembly必须显式提供absolute model/ffmpeg paths、tool identities与bound human fallback，不得注册default或构造无fallback reviewer。未校准profile允许automatic mismatch fail closed，但automatic match必须降为`not_evaluated`并由bound human evidence确认。identity、camera axis、framing无可信backend时同样必须转human fallback；repair绑定exact approved P5 closure。 | Analyzer/model自报Production verdict、grayscale/frame-difference heuristic自判人物出入画、把不支持、低置信度或未校准automatic match猜成Production PASS、缺失或不完整continuity evidence仍activation、CUDA profile静默CPU fallback、runtime自动下载model、未经授权repair、第二QA/control path。 | `python -m pytest -p no:cacheprovider tests/test_mcp_review.py tests/test_mcp_optimize_plan.py tests/test_mcp_apply_optimization.py tests/test_production_review.py tests/test_production_continuity_evaluator.py tests/test_production_continuity_onnx_backend.py tests/test_production_local_continuity_reviewer.py tests/test_production_repair.py tests/test_production_state_commit.py tests/test_production_state_recovery.py -q` |
| P7 Image Contract and Activation | `src/ai_video/production/image.py`、P7 state-commit/reader extensions | Request 绑定 exact target Shot/role、prompt/parameters、Character/Scene/reference 与 base tuple；measured PNG/provenance durable；committer原子切换 target-only project/registry/graph。 | P7 自建 resolver/writer、automatic reference regeneration、remote Provider、renderer/video generation 或 Legacy CLI path。 | `python -m pytest -p no:cacheprovider tests/test_production_image.py tests/test_production_image_e2e.py tests/test_production_models.py tests/test_production_registry.py tests/test_production_validation.py tests/test_production_project.py tests/test_production_dependency.py tests/test_production_selective_rebuild.py tests/test_production_state_commit.py tests/test_production_state_recovery.py -q` |
| P7.1 Local Image Adapter and Human Import | `src/ai_video/production/comfy_image.py`、`image_import.py`、sealed workflows/profiles | Concrete adapter 只接受 profile-listed loopback origin并禁用 proxy/redirect；exact components/workflow/nodes 在 R+1 前 preflight；human import 只记录 truthful actor/source/PNG evidence。 | Remote/fallback endpoint、preflight 后补验证、虚构 web backend/request/automation、第二 import writer。 | `python -m pytest -p no:cacheprovider tests/test_production_comfy_image.py tests/test_production_image_import.py tests/test_production_p7_1_local_image_e2e.py tests/test_comfy_client.py tests/test_workflow_loader.py -q`；涉及 host inventory 时另运行 `scripts/check_p7_1_comfy_compatibility.py` |
| Ecommerce Product-Interaction Source Preparation | `commercial_execution.py`、`commercial_reference.py`、`commercial_source_preparation.py`、`commercial_visual_review.py`、`commercial_image_import.py`、`commercial_dependency.py`、`_commercial_source_state.py`、`_commercial_project_reader.py`、`_state_commit_commercial_source*.py`、existing Planner/Router/compiler/P6/P5 owners | `AdCreativePlan /2`的`PRODUCT_INTERACTION` intent纯投影为exact `ProductReferenceSet`和registered-import request；`P7_GENERATION` fixed BLOCKED。Manifest 2.12只由`ProductionStateCommitter`保存candidate、authorizer-selected review intent、evidence/receipt与approval；intent durable consume后才签发process-local one-use permit，caller/analyzer不能自批durable PASS。P6 semantic PASS、current Registry/import PNG/QA policy/dependency graph/Shot/Character/Scene lineage与approved keyframe exact一致后，Planner才生成provider-neutral `/3` product-fidelity requirement；direct recovery与每个replay都strict reopen full current chain并绑定caller request/candidate。Router只允许同一first-frame I2V，不fallback到T2V/generic reference/overlay。Product/reference/keyframe/Shot edges仍由existing Dependency Graph计算closure，classifier只提出root/boundary。Historical Ad `/1`、requirement `/1`/`/2`和Manifest 2.0–2.11 strict reopen保持兼容。 | Caller-supplied legacy source pointer满足new attempt、candidate/importer/analyzer自批PASS、第二Registry/Manifest/P6/repair owner、错误request/candidate复用旧receipt、未注册或stale product bytes、approval前进入Planner、P7 callback、T2V/generic-reference/flat-overlay降级、自动repair、把offline structural SELECTED解释为Provider/media/P6/Final Acceptance。 | `python -m pytest -p no:cacheprovider tests/test_production_commercial_execution.py tests/test_production_commercial_reference.py tests/test_production_commercial_source_preparation.py tests/test_production_commercial_visual_review.py tests/test_production_ecommerce_product_interaction_e2e.py tests/test_production_ad_creative.py tests/test_planning_video_planner.py tests/test_shot_readiness_gate.py tests/test_production_video_requirement.py tests/test_production_shot_router.py tests/test_production_provider_neutral_adapters.py tests/test_production_image_import.py tests/test_production_review.py tests/test_production_repair.py tests/test_production_state_commit.py tests/test_production_state_recovery.py tests/test_production_state_commit_structure.py -q` |
| Paid Provider Safety Gate | `src/ai_video/production/paid_provider.py`、`_state_commit_paid_provider.py`、`_paid_provider_project_reader.py` | Provider-neutral preview、exact egress、secret reference、explicit actor authorization 与 budget reservation 必须绑定同一 sealed request；committer先持久化 one-use submit intent，Gate-owned `PaidProviderSubmitReceipt` 独占 billable external-effect identity；known-no-effect、accepted与outcome-unknown使用不同 durable outcome，unknown不得自动重试。 | Adapter自授权、自行预留/结算budget、在durable intent前submit、持久化raw secret、复制external task ID、unknown后remint permit或blind resubmit。 | `python -m pytest -p no:cacheprovider tests/test_production_paid_provider.py tests/test_production_paid_provider_state.py tests/test_production_paid_provider_e2e.py tests/test_production_state_commit.py tests/test_production_state_recovery.py -q` |
| P8 Video Schema and Read-Only Evidence | `src/ai_video/production/models.py`、`validation.py`、`_lifecycle_schema.py`、`registry.py`、`_video_project_reader.py`、`project.py` | Manifest 2.7/2.8独占historical video lifecycle；Manifest 2.9 additive保存continuity evaluator intent/evidence checkpoint；Manifest 2.10的`EVIDENCED` state进一步强制exact `VideoProbeReceiptPointer`与`VideoProvenanceReceiptPointer`成对存在，并在pure adjudication前持久化。Pointer必须canonical、content-addressed并共同绑定request/fetch/artifact/evaluation/profile identity，reader/recovery只strict reopen验证；2.9历史evidence-only state保持兼容但不得携带2.10 pointer。Registry 2.2 generated-video contract不变，remote/local证据链不得混合。Pre-generation `generated_video` Shot可携带唯一empty target role，但该role必须只允许`VIDEO`；它不注册asset，candidate activation仍须由existing committer替换为exact generated-video binding。 | Schema/reader写state、graph保存mutable video/evaluator lifecycle、empty target role用于非`generated_video` strategy或允许非VIDEO/multiple roles、用placeholder/fake MP4冒充pending target、2.8偷带2.9 pointer、2.9偷带2.10 pointer、2.10 evidenced state缺probe/provenance、接受symlink/non-content-addressed receipt bytes、local lane伪造Paid Gate/cost/remote egress、把schema phase当作第二writer。 | `python -m pytest -p no:cacheprovider tests/test_production_bootstrap.py tests/test_production_validation.py tests/test_production_models.py tests/test_production_registry.py tests/test_production_project.py tests/test_production_local_video_state.py tests/test_production_generated_video_e2e.py tests/test_quality_experience_capture.py -q` |
| P8 Video Provider Contract and Activation Lifecycle | `src/ai_video/production/video.py`、`video_contracts.py`、`_video_capability_fingerprint.py`、`video_compiler.py`、`seedance.py`、`seedance_capabilities.py`、`minimax_h3.py`、`minimax_hailuo.py`、`video_artifact.py`、`continuity_evaluator.py`、`video_candidate.py`、`video_generation.py`、`video_fake.py`、`_state_commit_video*.py` | Provider-neutral request/capability/profile resolution覆盖typed image/video/audio bindings、reference/edit/extend modes、exact/adaptive dimensions、exact/provider-selected/frame timing和MP4/MOV identity；legacy request/resolved hashes保持兼容。`output_recovery_strategy`与job/status `lookup_supported`保持独立并进入current capability/profile identity；historical缺field时以`None`严格重开且serializer/fingerprint省略`None`。New remote attempt只允许`DURABLE_FILE_ID`或`REQUERY_BY_EFFECT_ID`，缺失或one-shot strategy由pure compiler在`VideoGenerationService.start()`前typed拒绝且保持零attempt/Manifest/permit/Provider effect；Seedance/H3使用effect re-query，Hailuo使用durable file ID，Local T8保持local-specific `None`。Provider optional model/output echoes缺失不阻断fetch，冲突echo仍fail closed；artifact SHA/probe只由AI-VIDEO exact local bytes证明，`VideoProvenanceReceipt.model_id/profile_sha256`只表示requested/selected identity。Optional `execution_stack_hash`存在时必须进入request/resolved/activation-scope seals（schemas `/7`、`/8`、`/6`），缺失时历史payload与hash保持bit-for-bit。`request_input_hash`、`generation_id`、`resolved_generation_hash`、Gate external-effect identity与artifact SHA保持分离；committer独占request、submit linkage、status、held-FD fetch、measured validation、provenance、Project/Registry/P5 candidate与single-Manifest activation/recovery。Continuity-bound request在candidate validation时必须提供active QA policy-selected explicit evaluator/human evidence；缺失、stale、coverage不完整或P6判定非PASS时停在validation且不得activation。Fetch本身不等于activation。 | 用`lookup_supported=True`旁路one-shot output、process-local URL cache或raw signed URL persistence、Provider metadata覆盖local SHA/probe、把requested model/profile冒充Provider-observed actual execution、Provider-specific core fields、automatic Provider selection/fallback、第二writer/resolver/task ID、以output bytes充当desired identity、把仅fetch完成或continuity未通过的candidate当成durable registered/activated asset、recovery自动选择candidate、绕过P3 validation直接渲染。 | `python -m pytest -p no:cacheprovider tests/test_production_provider_neutral_adapters.py tests/test_production_video.py tests/test_production_seedance.py tests/test_production_minimax_h3.py tests/test_production_minimax_hailuo.py tests/test_production_generated_video_e2e.py tests/test_production_quality_gate_coordinator.py tests/test_video_candidate.py tests/test_video_generation.py tests/test_production_video_state_recovery.py tests/test_production_state_commit.py tests/test_production_state_recovery.py -q`；另运行`python -m scripts.architecture_gate check`。 |
| Provider-Neutral Generation Requirement | `src/ai_video/production/video_requirement.py`、`_video_intent_validation.py`、`_h3_prompt.py`、`src/ai_video/planning/video_planner.py`、`src/ai_video/production/shot_router.py`、`src/ai_video/production/video_compiler.py` | `VideoPlanner`从current canonical Shot、typed intent/evidence与continuity唯一派生plan内嵌、hash-bound requirement；Planning只向Router交付verified projection。`/4` rich intent显式绑定performance、treatment、lighting、audio/music与单一camera motion/relation；I2VA/FL2VA必须携带exact conditioning，T2VA必须不携带conditioning。Explicit `GenerationOperation.TEXT_TO_VIDEO`使用独立intent hash schema，仅允许无media reference且continuity为none/semantic的typed semantic request，Character/Scene context不变成fake asset evidence。`_h3_prompt.py`只把sealed facts机械编译为唯一H3 three-field grammar；完整historical `/1` T2VA可保留其缺省camera motion/relation并由versioned native compiler表达，字段不足则typed unsupported。Router继续独占exact Provider/profile/capability selection并输出prompt-free `ProviderBoundVideoRequest`；selected adapter compiler只做deterministic native expression，无法表达时返回typed unsupported且不改写requirement、不fallback。New lifecycle lineage使用request `/5`、resolved `/6`、activation scope `/4`，历史hash/reopen保持兼容。 | run-local prompt构造、T2V携带image/video/audio reference或conditioning、把semantic Character/Scene冒充media evidence、orchestration直接创建new provider-bound request、Router之外选择Provider/profile、adapter重创作Shot或降低requirement、adapter自行fallback、relation夹带第二camera motion、incompatible anchor仍进入effect、把neutral key-value或raw JSON字段直接作为H3 native prompt、把offline compile映射声称为live/quality acceptance。 | `python -m pytest -p no:cacheprovider tests/test_production_video_requirement.py tests/test_production_video_intent_validation.py tests/test_production_h3_prompt.py tests/test_planning_video_planner.py tests/test_shot_readiness_gate.py tests/test_production_shot_router.py tests/test_production_video_fake.py tests/test_production_provider_neutral_adapters.py tests/test_production_video.py -q` |
| Shot Readiness Gate | `src/ai_video/quality_gates/**`、`src/ai_video/planning/_current_plan_projection.py`、`src/ai_video/planning/_asset_readiness.py`、`src/ai_video/planning/video_planner.py` compatibility façade、`src/ai_video/production/video_transition.py` causal policy | Planning verifier独占current v3 request/plan/embedded requirement的freshness projection；`ShotReadinessGate`独占pre-submit structural `READY/BLOCKED`与typed STOP；exact三项diagnostics固定为binding、eligibility、required assets。Rich continuity `/4`还必须通过versioned pairwise policy绑定previous/target exact Shot与intent identities、typed causal dimensions及合法ellipsis/reset/commercial semantics；diagnostics仍进入binding check，不新增第二Gate owner。Historical `video-planner/2`保持reopen兼容但new attempt必定STOP；READY只返回verified requirement projection，BLOCKED后handoff及downstream calls为零。 | Gate重建Planner/requirement truth、`plan_hint` handoff、v2 new-attempt conversion、用Shot顺序或prose替代causal reachability、coarse/fine transition双真相、Router/Provider/Manifest/Registry/committer/timeline/P6 ownership迁移、post-fetch quality verdict或把READY解释为generation/activation/quality acceptance。 | `python -m pytest -p no:cacheprovider tests/test_shot_readiness_gate.py tests/test_planning_video_planner.py tests/test_production_shot_router.py tests/test_production_video_requirement.py tests/test_production_video_intent_validation.py tests/test_production_video_transition.py tests/test_production_video.py tests/test_errors.py -q` |
| Shot Continuity Technical Core | `src/ai_video/production/video.py`、`video_artifact.py`、`continuity_evaluator.py`、`continuity_onnx_backend.py`、`local_continuity_reviewer.py`、`dependency.py`、`_state_commit_video*.py`、`_video_project_reader.py`、`minimax_hailuo.py`、`seedance.py` | Manifest 2.8保持terminal extraction/evidence compatibility；continuity-bound new attempt使用Manifest 2.10 intent-before-effect与evidence-before-candidate checkpoint，Manifest 2.9仅保持historical evidenced replay compatibility，exact retry/recovery不得重复evaluator side effect。Continuity request必须是exact terminal-frame I2V并绑定scene/character/camera/framing/light/color/motion/entrance-exit constraints；target MP4 activation前必须有exact complete evidence并由P6判定全部通过。P5只沿显式terminal asset input edge传播真实closure；general YOLOX/OSNet CUDA profile仅提供显式local technical capability，local factory只装配existing reviewer callable且不自动注册到generation path，未校准或unsupported dimensions仍fail closed并转human fallback。 | 临时抽帧绕过Registry/provenance、intent前运行evaluator、retry重复sampler/model/human fallback、resolve时删降continuity、T2V/prompt-only/remote fallback、用prompt/first-frame SHA/灰度活动冒充output continuity acceptance、第二timeline/renderer/writer、把CUDA technical smoke宣传为live media/quality acceptance。 | `python -m pytest -p no:cacheprovider tests/test_production_video.py tests/test_production_continuity_evaluator.py tests/test_production_continuity_onnx_backend.py tests/test_production_local_continuity_reviewer.py tests/test_production_generated_video_e2e.py tests/test_production_dependency.py tests/test_production_selective_rebuild.py tests/test_production_minimax_hailuo.py tests/test_production_seedance.py -q` |
| Shot Continuity P0 Preparation | `src/ai_video/production/video_execution_stack.py`、`execution_stack_materialization.py`、`shot_continuity_source_runtime.py`、`shot_continuity_source_qualification.py`、`shot_continuity_source_operator.py`、`video_compiler.py`、`shot_continuity_m0_caller.py`、`shot_continuity_m0_qualification.py`、`shot_continuity_m0_seed_reseal.py`、`shot_continuity_m0_fast_validation.py`、`shot_continuity_m0_policy.py`、`video_transition.py`、`image_import.py`、`_state_commit_p0_qualification.py`、`scripts/prepare_shot_continuity_endpoint_repair.py`、`workflows/qualification/**`、Manifest `2.11` pointer | P0只持久化 truthful automated-browser imports，或 exact `codex_imagegen_tool` PNG + human approval 的 non-browser import；endpoint repair必须从active creative bundle建立fresh root，只revision approved A3并保留A1/A2/A4 identities，不复制旧video attempt；receipt必须绑定exact source A2/A3 Shot revision/content hash/artifact path与endpoint asset ID/SHA，bootstrap与reopen保留并重验source Shot bytes，target不得位于source root内；source qualification profile必须exact绑定active repaired Shot的正整数revision，不得硬编码初始revision `1`。其余exact Project/Registry-bound candidate stacks、transition policies、real-Shot validation set、inventory/calibration/rubric/effect-budget/human-freeze inputs与limitations。Upstream source runtime以真实`Shot.shot_id`和既有唯一`approved_endpoint` role构造sealed四Shot CompositionSpec，通过canonical applied-evidence resolver与`ProductionStateCommitter.bootstrap_dependency_graph()`建立active graph后再升级Manifest；candidate必须同时reseal target Shot role与唯一Composition layer，使generated asset进入composition/renderer-source graph edges，并能仅从activated Project/Shot/Registry重建同一inputs/graph。candidate preparer必须绑定同一`ProductionDependencyInputs`，不得注入空graph或新增第二role/writer。Source transport必须同时验证self-reported deployment identity和实际`base_url`为profile固定literal loopback origin；source workflow全部required node schemas未exact seal时必须在runtime inspection、intent、permit、upload与submit前fail closed。qualification-only operator必须从exact profile document、active Project/Registry/graph pointers、source stack/compiler/workflow与A2/A3 bytes canonical hash派生sealed compilation projection，最终`VideoGenerationRequest`仍只由`video_compiler.py`唯一构造；generation actions只暴露`inspect/preflight/status/submit/poll/fetch`并在fetch后停于`validate`，Manifest 2.14 post-fetch closure另由显式`upgrade-manifest`与source-boundary `validate` action触发，不得自动串联。fresh guard必须拒绝dedicated bundle内任何既有video attempt，preflight必须以no-follow regular-file reads验证完整Production bundle hash前后相同并只输出source filename/hash/size summary，不得注册active Provider family或自动进入candidate/P6/activation。candidate可以由唯一committer逐个materialize，但qualification inputs必须只绑定当前materialized stack hashes，所有依赖identity的policy/validation/receipt必须同步reseal。materialized stack的exact profile（包含binding bytes）/compiler/workflow bytes必须content-addressed持久化，并在普通reopen与recovery中no-follow rehash；selected stack存在absent component时fail closed，targeted reopen只授权明确已materialized candidate。M0 preparation、materialization与caller必须显式选择`fast-v1`或`quality-v1`，selection绑定profile/stack/rubric/capability且不得默认、自动fallback或跨policy资格化；两条policy共享同一frozen M0 rubric，均只由requirement-level evidence聚合`PASS`/`FAIL`/`NOT_EVALUATED`，P6与human evidence owner不变。Fast固定独立literal `Hybrid`四锚点、sealed Turbo LoRA与4 steps；quality保留既有Stock20/no-LoRA 20-step compiler bytes。默认seed继续由exact closure content-addressed派生；显式`historical-materialized-m0-fixed-seed-v1`只允许跨Registry/P0 closure保留seed；显式`historical-materialized-m0-runtime-repair-fixed-seed-v1`只允许在同一historical materialized M0 lineage上替换exact一个`comfyui` runtime seal。两种fixed-seed mode都必须在materialization与pre-effect reopen证明exact historical prepared receipt、materialized M0 stack与profile source hash；prompt、components、workflow、geometry、sampling、T8/VHS runtime或额外runtime drift均fail closed。Runtime replacement必须由typed `ExecutionStackRuntimeReseal`绑定current M0 stack/source ComfyUI seal、CAS检查full inventory revision，且target seal必须从本次exact hashed profile source重开匹配；唯一materialization owner再在同一Manifest lock内原子reseal stack、runtime inventory、其余inputs、policies、validation set与prepared receipt，replay必须zero-write。显式`inventory-only-repair`只允许在stack已达target seal但inventory revision仍为旧值时补齐同一target revision，不得更换runtime seal。V1 pre-effect snapshot必须通过`required_materialized_candidates=("m0",)`重开、重验exact source hashes与frozen target/M1 absent/dependent evidence后才能交给后续caller；selected policy guard还必须把fresh snapshot的stack hash与durable resolved request直接比对。Generic service在preview前执行一次read-only guard，唯一committer在同一intent write lock内重新执行guard并紧接着持久化intent/permit；real-committer regression必须覆盖request→intent→unknown-outcome→zero-resubmit。exact replay zero-write，recovery重验完整selected closure，Project/Registry漂移清除pointer。Model-facing calibration必须固定 exact source geometry、`1344x768` canvas、32 multiple与`BHWC [1,768,1344,3]` normalized tensor。 | 把browser automation伪装为human import、保存signed URL或虚构backend/request ID、以artifact_id冒充`Shot.shot_id`、绑定不存在的`source_video` role、以自报identity掩盖non-loopback transport、source node schema未seal仍提交、operator或其他module成为第二`VideoGenerationRequest.create()` owner、从tests复制假lineage hash或绕过operator直连ComfyUI、输出preflight原始PNG bytes、只hash`state/`或follow symlink证明零写入、通过更换IDs重复submit、暴露endpoint/model/prompt/seed/retry/fallback override、fetch后自动candidate/P6/activation、generated Shot role已切换但Composition仍引用旧A3、注入空graph、以全`stale/blocked`丢弃已有Project/Registry applied truth、隐式crop/stretch、materialize含absent component的candidate、用全体materialized gate替代targeted candidate guard、M0 caller省略guard或绕过selected-policy guarded reopen直接进入intent/submit、隐式fast→quality fallback、fast PASS资格化quality capability、P0 receipt或preflight snapshot声明winner/PASS/P6/activation、未经typed runtime reseal任意改runtime、target runtime未绑定hashed profile source、把prompt/model/T8/VHS drift伪装为ComfyUI repair、runtime seal变化后保留stale inventory commit、第二writer/lifecycle、Project/Registry漂移后继续使用旧bundle。 | `python -m pytest -p no:cacheprovider tests/test_prepare_shot_continuity_endpoint_repair.py tests/test_shot_continuity_source_runtime.py tests/test_shot_continuity_source_qualification.py tests/test_shot_continuity_source_operator.py tests/test_video_candidate.py tests/test_production_video_transition.py tests/test_production_p0_qualification.py tests/test_shot_continuity_m0_materialization.py tests/test_shot_continuity_m0_validation.py tests/test_shot_continuity_m0_seed_reseal.py tests/test_shot_continuity_m0_caller.py tests/test_shot_continuity_m0_policy.py tests/test_shot_continuity_m0_fast_validation.py tests/test_shot_continuity_m0_policy_preparation.py tests/test_production_image_import.py tests/test_production_generated_video_e2e.py tests/test_production_state_recovery.py tests/test_production_video_state_recovery.py -q` |
| Qualification Source Boundary P6 | `src/ai_video/production/shot_continuity_source_review.py`、`shot_continuity_source_operator.py`、`_state_commit_video_source_boundary.py`、`_state_commit_video_candidate.py`、`_video_project_reader.py`、Manifest `2.14` state | Exact `approved_endpoint` source request因`continuity_binding=null`必须使用独立source-boundary checkpoint，不得合成binding或复用ordinary continuity evaluator。Operator generation flow仍在fetch后停于`validate`；另行显式`upgrade-manifest`与`validate`才可消费already-fetched exact MP4。Committer先持久化intent，再将decoded frame 0/terminal、RGB PSNR、BT.709-luma SSIM、P0 rubric `30 dB / 0.9`、exact A2/A3/stack/request/fetch、human continuity dimensions与pacing waiver封入evidence/receipt/probe/provenance。Canonical runtime profile authority来自request的sealed requirement/provider binding与historical P0 materialized stack/profile bytes；`intent.source_profile_content_hash`只保留qualification-profile audit identity，不单独授予acceptance authority。只有deterministic P6 `PASS`准备inactive candidate；`FAIL`/`NOT_EVALUATED`留在validate并可strict reopen/recovery，replay/recovery不重复evaluator/human effect，PASS不自动activation或M0。 | Fake `continuity_binding`、generic `validate_once()`绕过P6、把audit-only profile label当作canonical runtime authority、只凭SSIM/Harness/console text或裸“通过”签发PASS、rubric drift仍沿用旧阈值、intent crash后blind retry、FAIL后candidate、PASS后自动activation/M0、reader/adapter成为第二writer。 | `python -m pytest -p no:cacheprovider tests/test_shot_continuity_source_review.py tests/test_shot_continuity_source_operator.py tests/test_production_models.py tests/test_production_project.py tests/test_production_video_state_recovery.py -q` |
| Hard-Cut Continuity Technical Core | `src/ai_video/production/image.py`、`comfy_image.py`、`video.py`、`_video_continuity.py`、`video_dependency.py`、`_state_commit_image*.py`、`_state_commit_video.py`、`_image_project_reader.py`、additive hard-cut profile | C2 image request必须严格为`character + continuity_terminal`，terminal binding exact active source video/Shot/derived PNG/constraints；generated keyframe provenance使用真实graph IDs。P8 hard-cut binding必须精确绑定P7 keyframe request/output并保持历史hash omission semantics。P5只沿显式`terminal -> keyframe -> video` closure传播。 | 原地改写accepted profile、terminal伪装scene creative、audit-only terminal、terminal原样first-frame冒充hard cut、dangling graph hash、blanket stale、把C2 technical lineage冒充reference continuity quality acceptance。 | `python -m pytest -p no:cacheprovider tests/test_production_image.py tests/test_production_comfy_image.py tests/test_production_video.py tests/test_production_dependency.py tests/test_production_generated_video_e2e.py tests/test_production_state_commit.py tests/test_production_state_recovery.py -q` |
| Shot Router Runtime | `src/ai_video/production/shot_router.py`、`_shot_router_contracts.py`、`_shot_visual_resolver.py` | Pure resolver消费activated Shot的`exact_terminal`、`reference`、`semantic`、`none` continuity contract与verified neutral requirement：terminal分别只能作为first frame、reference或被排除；reference/semantic typed state必须先以sealed token进入activated Shot的既有continuity constraints，target Shot hash继续进入现有request/P5 truth。每个input asset绑定exact selected Registry revision；canonical角色/场景reference还必须绑定对应Character/Scene ID与content hash，最终投影的request binding保持唯一。Router独占Provider/profile/capability selection；routing与continuity mode正交，selected exact capability必须满足requirement，denial不fallback，semantic/audit hashes分离，provider-bound projection不含prompt。 | 以Provider分类替代continuity contract、Provider ranking/fallback、Router state writer、P5/timeline复制、secret/budget/network调用、无Registry/Character/Scene provenance的canonical reference、把C2 derived-keyframe technical lineage冒充reference quality acceptance、重要角色降级裸T2V、接收plan或自行重建requirement。 | `python -m pytest -p no:cacheprovider tests/test_production_shot_router.py tests/test_production_video_requirement.py tests/test_planning_video_planner.py tests/test_production_video.py tests/test_production_dependency.py tests/test_production_selective_rebuild.py -q` |
| Local H3 Continuity Adapter | `src/ai_video/production/local_video.py`、`comfy_video.py`、`workflows/{templates,bindings,profiles}/minimax_h3_fl2va*` | Comfy-Org pinned official I2V workflow只作为reviewed upstream；derived API workflow/profile seal upstream、ComfyUI commit、四组件、node inventory、binding与output bounds。H3 capability必须公开profile的exact width/height range与dimension multiple，使Router在selection前拒绝Provider无法resolve的geometry；workflow/profile/binding changes同时路由workflow与video-provider gates。Adapter只接受profile列出的literal loopback origin，exact terminal PNG进入`first_frame`，`last_frame`只在request显式存在时进入节点图；local submit使用独立one-use durable permit并复用唯一committer activation/recovery。 | raw smoke JSON直接升级production、UI/demo/LoRA/turbo metadata、remote refiner/cloud fallback、能力声明宽于sealed profile、静默增删`last_frame`、混用`fl2va`/`ref2va`、伪造Paid Gate、profile hash变更后不reseal。 | `python -m pytest -p no:cacheprovider tests/test_production_comfy_video.py tests/test_production_local_video_state.py tests/test_production_video.py tests/test_production_generated_video_e2e.py -q` |
| Local T8 H3 Provider Family | `src/ai_video/production/local_video.py`、`video_generation.py`、`comfy_t8_video.py`、`comfy_t8_turbo_video.py`、`comfy_t8_native_turbo_profile.py`、`comfy_t8_native_turbo_video.py`、`local_h3_provider_family.py`、`workflows/{templates,bindings,profiles}/minimax_h3_t8_*` | 单一identity `comfy-local-h3-t8`的production snapshot以exact identity同时公开Quality v1、hybrid Turbo v1和四个独立T8-native Turbo V2 T2VA/I2VA/FL2VA/Ref2VA child；旧variant、provider与two-child family fingerprint保持bit-for-bit，six-child snapshot固定为`d3b8e5cc31570763aae6f7454ca794737634c345ec3ea6bbbcaadc36196381dd`。Quality v1与hybrid Turbo v1的T2VA request必须使用compiler version `3`和`h3-three-field-v1` native prompt；完整 `/1` 与 `/4` requirement均只由`_h3_prompt.py`机械表达。`/1`只投影current-Shot action、performance、motion、camera、lighting与audio，禁止把global Story/Scene bookkeeping、future beats、identity IDs、abstract objectives、neutral key-value或JSON字段传入workflow；无对白mode只发射authored ambience/physical foley，任何待发射字段中的`speech`、`dialogue`、`narration`、`voice(s)`或H3 dialogue tag均按exact field path typed fail closed。dialogue的canonical BCP-47 core `language`必须sealed，extension/private-use不进入该contract，H3当前只把`zh-*`与`en-*`机械映射为native language tag，历史缺失或其他未支持language均typed fail closed且不得字符猜测。旧compiler version `1`/`2`与字段不足的intent同样typed fail closed。V2统一adapter固定non-pruned mode-compatible model、official-source/pinned-converter/exact-header-audited LoRA、exactly one `MiniMaxH3DualClockSamplerT8`、4 steps、`dual_clock_euler/native_flow`、`1344x768`、124 frames、24fps、native audio与CRF 17；通用fingerprinted role/group cardinality由`video_contracts.py`定义、Router selection与resolved request共同验证。四条V2 lane均有独立loopback technical proof并为profile-level `live-ready`；Ref2VA dynamic references必须使用T8/ComfyUI autogrow keys（例如`ref_images.ref_image_0`），且`ref_video_audios`仍不连接。任何profile未达`live-ready`时preflight必须在permit、upload与prompt effect前fail closed。Family仍只做exact dispatch且不保存lifecycle state；selected child复用唯一committer lifecycle，成功输出必须唯一选择`*-audio.mp4`。 | 覆盖或重封既有v1、把blocked/offline child加入production snapshot、Router或family ranking/fallback、T8私有neutral fields或raw JSON prompt、将future Story/Scene beat或opaque identity ID泄漏进current-Shot prompt、从dialogue字符集猜language、pruned V2 base、Larry sampler与native sampler双owner、裸`ref_image_1`/错误ordinal key、`ref_video_audios`隐式关联、remote endpoint/credential、第二lifecycle、接受video-only output、把technical acceptance声称为主观media quality或P6 Final Acceptance。 | `python -m pytest -p no:cacheprovider tests/test_production_comfy_t8_video.py tests/test_production_comfy_t8_turbo_video.py tests/test_production_comfy_t8_native_turbo_video.py tests/test_production_local_h3_provider_family.py tests/test_production_provider_neutral_adapters.py tests/test_production_shot_router.py tests/test_production_video.py tests/test_production_local_video_state.py -q` |
| P8 MiniMax Adapters | `src/ai_video/production/minimax_h3.py`、`minimax_hailuo.py` | Concrete adapters保持显式、可删除且不反向污染P8 core；固定capability/profile只接受exact compatible request；raw credential不进入model/receipt/repr，transport禁redirect；Gate permit紧邻唯一submit消费，poll/fetch绑定同一task，signed result URL不持久化。三条Hailuo任务已有真实succeeded/fetched MP4 evidence；H3 live到达真实API但因余额不足返回`402 / code 1008`，所以H3仍只有offline acceptance。 | 注册为default/fallback、绕过Paid Provider Gate或committer、自动降级model/profile、泄露secret/signed URL、redirect following、unknown submit后blind retry、把H3余额失败宣传为live success，或用既有live evidence授权新调用。 | `python -m pytest -p no:cacheprovider tests/test_production_minimax_h3.py tests/test_production_minimax_hailuo.py tests/test_production_video.py tests/test_production_video_state_recovery.py tests/test_production_paid_provider.py tests/test_production_paid_provider_state.py tests/test_production_paid_provider_e2e.py tests/test_production_state_commit.py tests/test_production_state_recovery.py -q` |
| P8 Seedance Cloud Adapter | `src/ai_video/production/seedance.py`、`seedance_asset.py`、`seedance_capabilities.py`、`seedance_reference_duration.py`、`seedance_profile.py`、`remote_media.py` | 2026-08-19 official matrix的七个current/legacy-callable Model IDs必须完整且不可由injected profile扩张或漏mode；Endpoint binding、expected response Model ID、output container和same-task identity进入durable evidence。credential只接受injected `ARK_API_KEY` reference；permit紧邻唯一POST消费，无POST retry；poll/fetch精确绑定task/model，redirect/cross-origin/oversize/MIME/ISO-BMFF brand均fail closed。Payload始终显式绑定`generate_audio`，但省略false/default/MP4/zero-valued optional defaults。Input materialization有两条显式lane：already-materialized Ark identity继续要求sealed、human-observed `Active` receipt并导出`asset://`；同一Seedance Provider刚生成并exact-fetch的普通video可由durable `RemoteMediaMaterializationReceipt`绑定task/model、opaque file identity、URL hash/origin与exact MP4 hash/bytes/MIME，再由`VideoGenerationService.refresh_remote_reference_lease_once()`验证attempt仍是Manifest exact active source、重开exact evidence并签发process-local one-use refresh permit；当exact active source request使用`nominal_seconds`时，permit与lease还绑定该nominal milliseconds，remote R2V binding必须使用该sealed nominal值并继续绑定同一exact bytes/materialization，物理duration仍由active artifact/probe独占。Seedance helper只有消费该permit后才能重新query同一locator、完整GET验证同一bytes并签发最长5分钟的in-memory `SeedanceRemoteReferenceLease`导出HTTPS `reference_video`。完整signed URL不得进入Manifest、receipt、repr或log；lease过期、locator轮换、bytes变化、identity不匹配或nominal proof不匹配都必须在新POST和permit消费前停止，且不得静默fallback到I2V。local Registry ID仍不能伪装成`asset://`或URL。`synthetic_photorealistic_person` receipt保持历史读取兼容，但authorizer与resolver必须在POST和permit消费前拒绝其inline egress。2026-08-29/30 Mini remote-output live chain已用exact-active Shot 1 refresh同一locator并复核同一bytes，随后唯一一次`VIDEO_EXTEND` POST直接消费HTTPS `reference_video`，Shot 2 fetched、Per-Shot Gate PASS并activation；该证据证明live chaining与本次技术/视觉Gate，不外推其他模型、billing价目正确性、P6或Final Acceptance。 | 接受retired/marketing alias、profile扩张官方能力、fallback到MiniMax keys、把local asset ID拼成`asset://`/URL、无exact bytes evidence导入remote reference、持久化raw signed URL、用nominal值覆盖exact measured artifact/probe duration、绕过Service从未activated或stale source签lease、公开或可重复mint refresh/lease authority、过期lease继续submit、locator轮换后只信host/HEAD或不重验bytes、把provider-output lease泛化成未经egress gate的local H3/T8 uploader、把photorealistic person-like synthetic bytes提交给Ark、unknown submit后blind retry、把diagnostic或单次live R2V证据外推为其他模型、P6、Final Acceptance或Provider-wide quality。 | `python -m pytest -p no:cacheprovider tests/test_production_seedance.py tests/test_production_video.py tests/test_video_generation.py tests/test_production_paid_provider.py tests/test_production_paid_provider_state.py tests/test_production_video_state_recovery.py tests/test_production_state_recovery.py -q` |
| Base AI Comic E2E | `tests/test_production_base_ai_comic_e2e.py`、P3-P7 owners | No-Video-Provider path 必须沿唯一 committer/resolver/timeline/review control path完成 durable reopen、exact repair 与 zero-side-effect replay；repair不得 regenerate unaffected P7/voice/caption assets。 | 新 production coordinator、第二 writer/resolver、隐式 Video Provider、用 E2E acceptance 推导 paid/live authorization。 | `python -m pytest -p no:cacheprovider tests/test_production_base_ai_comic_e2e.py -q && python -m scripts.architecture_gate check` |

## Quality Gate Architecture Boundary

Quality architecture 采用 **Universal Production QA + Domain-Specific Acceptance Gate**。当前 generic `QaLayer` 是既有 P6 lifecycle envelope，不是跨 Ecommerce、Drama 与 market outcome 的完整 taxonomy；不得通过改名或复用 `STRATEGY` / `SEMANTIC` 建立第二套 business acceptance。

| Truth Plane | Primary Owner | Invariants | Forbidden Alternate Path | Focused Verification |
| --- | --- | --- | --- | --- |
| Universal hard validation | Existing Registry、media/candidate validators、`ResolvedTimeline`、composition/render、audio/caption owners | artifact bytes/provenance、decode/container/codec/geometry/fps/duration、frame/sample timing、source trim、render/composition identity与structural audio/caption correctness始终由现有Runtime owner验证。 | Domain Skill重复实现media/timeline/renderer truth，或用Domain PASS绕过hard fail。 | 对应Production surface tests；本boundary slice另运行`tests/test_production_review.py`与Base E2E。 |
| Caption final-media quality review | `caption_quality_contracts.py` owns the typed policy/context/finding payload；`caption_quality.py` owns exact context derivation and deterministic six-group adjudication；`_caption_quality_p6.py` is the only private adapter into the existing committer-owned P6 lifecycle | Caption-aware artifacts use additive `2.1`; Manifest `2.15` is the first container allowed to carry `CAPTION` state/pointers. A current outcome must strict-reopen exact track/cue/audio/style/timeline/render/policy/request/evidence identities and recompute the stored verdict. New Universal profile v2 with captions requires `AUDIO_CAPTION_BINDING`, `LAYOUT`, `CAPTION`, and a matching caption policy before effects. | Analyzer self-reported aggregate PASS、legacy `LAYOUT.caption_overflow_tolerance_milli` as a second caption owner、external Gate 1 callback returning CAPTION PASS、reader migration、blind retry/remint、or trusting the receipt envelope without reopening request/evidence. | `python -m pytest -p no:cacheprovider tests/test_production_caption_quality.py tests/test_production_quality_gate_coordinator.py tests/test_production_project.py tests/test_production_state_commit.py tests/test_production_ecommerce_post_media_e2e.py tests/test_production_base_ai_comic_e2e.py -q` |
| Explicit Gate 1 orchestration | `src/ai_video/production/quality_gate_coordinator.py`只拥有preselected Universal profile/context/policy coverage与one-shot调用顺序；hard validator与P6 lifecycle owner保持不变 | caller必须显式调用；任何effect前strict reopen exact profile/context/policy；hard checks先于required review layers，任一`FAIL`、`NOT_EVALUATED`、stale或unknown outcome立即STOP且不retry。Captioned v2 profile的`CAPTION` branch只消费canonical caller注入的private strict-reopen outcome。PASS只产生in-memory `eligible_for_domain_gate=true`，不持久化P6、Manifest或Final Acceptance。 | background/watch trigger、profile fallback、external review callback伪造CAPTION PASS、第二validator/P6 writer、自动repair/activation/Final Acceptance，或在typed Domain seam不存在时执行Gate 2。 | `python -m pytest -p no:cacheprovider tests/test_production_quality_gate_coordinator.py tests/test_production_review.py -q` |
| Ecommerce whole-ad Gate 2 closure | `src/ai_video/production/ecommerce_quality_gate.py`拥有exact-target Gate 2 validation/evidence construction；`_ecommerce_quality_gate_p6.py`只提供private existing-P6 adapter；`src/ai_video/production/ecommerce_ad_coordinator.py::run_ecommerce_ad_production()`拥有唯一public canonical Shot → render activation → private final-media closure ordering；durable write与Final Acceptance仍只属于`ProductionStateCommitter` | Production result必须先完成sequential Shot barrier，再调用preselected canonical render activator，然后从committer唯一root重新打开active render及其exact timeline、从real `VideoGenerationService`重新打开ordered activated Shot checkpoints，并在内部执行Gate 1；caller不得注入project root、timeline或Gate 1 result。Gate 2随后绑定sealed handoff、Shot request/evidence/asset hashes、exact `CompositionSpec` / `ResolvedTimeline`、active output SHA-256、QA policy与durable semantic `ReviewRequest`。只有existing P6 transaction记录Gate 2 PASS且全部current required receipts为PASS才可返回production `complete=true`；任一post-gate media mutation/rerender产生new acceptance target并使旧receipt失效。 | 任意file path/manual MP4直接进入P6、把Shot-only `run_ecommerce_ad_generation()` return当deliverable、caller构造Shot/Gate 1 PASS、只有domain findings而无exact target envelope、Gate 2自写Manifest/receipt、background自动accept、禁止FFmpeg而不检查evidence freshness。 | `python -m pytest -p no:cacheprovider tests/test_production_ecommerce_quality_gate.py tests/test_production_ecommerce_post_media_e2e.py tests/test_production_review.py tests/test_production_ecommerce_ad_coordinator.py -q` |
| Universal and shared policy-driven review | `src/ai_video/production/review.py`、caption/continuity evidence contracts、`ProductionStateCommitter` P6 lifecycle | `TECHNICAL`检查black/silence/clipping/required-motion freeze/frame diversity；historical policy `LAYOUT` retains caption overflow, while caption-aware `2.1` LAYOUT only owns safe-area/collision/transition and `CAPTION` owns six-group final-media caption evidence；`STRATEGY`只表示reported visual-strategy result；`SEMANTIC`是policy-selected evaluator/human evidence envelope；`FINAL_ACCEPTANCE`只由committer汇总exact fresh required receipts。 | Analyzer/model自报Production verdict、Domain owner写Manifest/receipt、aggregate score抵消required `FAIL` / `NOT_EVALUATED`、dual caption thresholds、Final Acceptance隐式激活candidate。 | `python -m pytest -p no:cacheprovider tests/test_production_review.py tests/test_production_caption_quality.py tests/test_production_base_ai_comic_e2e.py -q` |
| Domain authoring and media acceptance | Ecommerce authoring由`.agents/skills/ecommerce-ad-workflow/`拥有；Ecommerce post-media Runtime由上方Gate 2 seam拥有；Drama Development authoring/rubric与exact HUMAN evidence contract由accepted `docs/superpowers/specs/2026-08-28-ai-video-drama-authoring-and-acceptance.md`拥有 | `AdQCReport.ready`只表示Ecommerce authoring readiness；`AdCreativeReviewReport.is_ready`只表示sealed handoff integrity且`production_verdict=None`。Ecommerce media evidence必须进入existing P6 exact-target binding；Production physical/perceptual continuity与Drama motivation/causality/emotional/payoff continuity保持不同owner；Drama typed Runtime Gate 2/P6 seam仍未实现。 | Skill或Drama Development artifact写Manifest、创建`ReviewReceipt`/`FinalAcceptanceReceipt`、把Base AI Comic或Ecommerce closure称为Drama acceptance、用physical continuity替代narrative continuity。 | `python -m pytest -p no:cacheprovider tests/test_ecommerce_ad_workflow_skill.py tests/test_production_ad_creative.py tests/test_production_ecommerce_quality_gate.py tests/test_shot_readiness_gate.py -q` |
| Market performance | Future separately approved analytics/empirical feedback owner；当前不存在Production acceptance owner | CTR、CVR、CPA、ROAS、hold rate、retention、completion等只属于outcome evidence；不得从authoring、P6或Final Acceptance预测。 | 跨domain `Quality Score`、`Ad QC PASS -> predicts ROAS`、`Drama QC PASS -> predicts audience success`。 | 当前无Runtime implementation；任何future seam须独立spec/plan、真实校准数据与authorization。 |

Drama M6-D Shot 01 的versioned source-binding读取由run-local
`runs/drama-m6-d-key-at-the-waiting-room-20260829-v1/shot01_authoring_source_binding_v3.py`独占。
它是Development authoring loader，不是新的Product subsystem：显式选择v3 metadata acceptance，严格重开Git/working
bytes，保留v1 semantic parent与v2 timing repair；仅将已接受的graph transition组合到source-binding view，复用原完整
source-binding校验、Planner/Router/compiler与current lineage validator。禁止latest/fallback、历史driver globals
patch、修改旧accepted bytes、手写VideoGenerationRequest或任何Production/Provider effect。
Focused verification：`python -m pytest -p no:cacheprovider tests/test_drama_shot01_authoring_source_binding.py -q`，
Harness check owner为`drama_authoring_source_binding_tests`；实际当前run的pure reopen另以exact sidecar记录，不能代替media Gate。

`ShotReadinessGate.READY`只证明pre-submit structural binding/eligibility/assets；它不证明post-media quality、P6、activation或Final Acceptance。Base AI Comic E2E当前证明exact selected policy（`LAYOUT + CAPTION`）下的durable closure，不证明完整Drama semantic acceptance或human perceptual caption quality。Independent Format Gate暂缓：现有`DeliveryProfile`、`CompositionSpec`、`ResolvedTimeline`和policy继续拥有format/timing/layout约束，除非未来出现重复且不可由这些owner表达的stable requirements。

### Shot Continuity M0 Live Qualification

- Primary owner：`shot_continuity_m0_operator.py`只编排显式`quality-v1` lifecycle；`shot_continuity_m0_caller.py`独占full pre-effect/permit-inner guard；`shot_continuity_m0_runtime.py`独占local checkout与model-byte closure；`shot_continuity_m0_qualification.py`在已seal的compiler bytes内独占seed mode/shape，`shot_continuity_m0_seed_reseal.py`独占historical fixed-seed与single-ComfyUI-runtime-repair proof；`shot_continuity_motion_tail.py`只保留historical full-source receipt compatibility，`shot_continuity_terminal_motion_tail.py`独占frame-exact terminal-window materialization与receipt，`shot_continuity_m0_motion_tail.py`独占current M0 shortest-window acceptance；`shot_continuity_m0_feasibility.py`独占exact-bound human endpoint decision/approval；durable write仍只属于`ProductionStateCommitter`。
- Invariants：M0只接受exact one-submit `attempt/generation/output/policy/stack/profile` approval，且在initial preflight与intent lock内重新打开并逐字段对比current request、active Project/Registry、四锚点和accepted upstream P6。Runtime必须验证sealed inventory对应的clean ComfyUI/T8/VHS checkouts与exact model bytes。Current `official_2_to_15s` M0 reference必须是24fps、48 frames、2000ms且终止于source terminal frame；selected-window motion analyzer、完整decoded frame sequence与terminal frame必须通过held no-follow descriptors绑定exact source/tool/derived bytes。Operator `preflight`必须走caller full guard，不得只运行Provider input check。
- Forbidden alternate path：将historical full-source receipt用于current M0、由caller自报reference duration bounds、pathname hash后重新打开source或ffmpeg、允许dirty或untracked runtime code、用两个孤立闪变冒充continuous motion、仅比较approval hash、provider-only preflight、retry/fallback/M1/activation、把technical/Harness PASS当P6或human quality acceptance。
- Focused verification：`python -m pytest -p no:cacheprovider tests/test_shot_continuity_motion_tail.py tests/test_shot_continuity_terminal_motion_tail.py tests/test_shot_continuity_m0_materialization.py tests/test_shot_continuity_m0_seed_reseal.py tests/test_shot_continuity_m0_feasibility.py tests/test_shot_continuity_m0_operator.py tests/test_shot_continuity_m0_reprepare.py tests/test_shot_continuity_m0_caller.py tests/test_video_compiler_c4_qualification.py -q`。

## Canonical API Entry Points

本节只约束容易混淆 normal business entry 与 internal seam 的跨 owner、effectful、durable、replay/recovery-sensitive API。`Allowed Caller` 描述谁可以进入该 seam；它不同于上表描述谁拥有 implementation 或 durable truth。Package export、public method、类型可构造或 test 中直接调用均不自动构成 normal Production caller authorization。

`Effect Class` 只使用 `PURE`、`READ_ONLY`、`LOCAL_EFFECT`、`EXTERNAL_EFFECT`、`PAID_EXTERNAL_EFFECT`、`DURABLE_WRITE`、`RECOVERY` 作为认知标签，不是 authorization、完整 effect accounting 或新 gate。多阶段 entry 可以列出多个 class；durable mutation 仍只归 `ProductionStateCommitter`。

| Operation | Canonical Entry | Allowed Caller | Effect Class | Durable Owner | Required Gates | Forbidden Bypass |
| --- | --- | --- | --- | --- | --- | --- |
| Load Production Project | `load_production_project()` | Planning、read-only inspection 与 approved Production orchestration | `READ_ONLY` | — | Manifest-selected exact Project/Registry revision、semantic/content identity、path containment | 直接 YAML/JSON/model loading、`load_production_project_candidate()` 充当 normal selected-state reader |
| Compile Ecommerce Ad Creative Plan | `create_ad_creative_plan()`、`compile_ad_creative_handoff()`、`compile_ad_creative_plan()` | Approved ecommerce authoring/runtime handoff | `PURE` | — | Typed accepted proposal、sealed `AdCreativePlan`、sealed base `CompositionSpec`、exact plan/source-evidence provenance、Product Truth/claim lineage、typed Shot/product/graphic/sound bindings、all capability gaps resolved | Repo外finalization script、任意字符串冒充source evidence、把commercial copy塞进`CaptionTrack`、flat overlay冒充physical interaction、创建第二timeline/renderer/writer |
| Prepare Provider-Neutral Video Requirement | `VideoPlanner.plan()` → `require_current_video_plan()` | Approved Planning flow | `PURE` | — | Current canonical Shot、typed intent/evidence、fresh embedded requirement、Shot Readiness `READY` | Caller直接构造 requirement/projection、把 `BLOCKED` 或 historical v2 plan交给 Router |
| Route Exact Video Capability | `VideoGenerationResolver.resolve_requirement()` | Production orchestration持有 verified requirement projection | `PURE` | Router | Exact Provider/profile/capability policy与selected Registry evidence | 使用legacy/general `resolve()`处理new neutral requirement、Router外选择/排名/fallback |
| Compile Provider-Native Video Request | selected adapter `compile_request()` → selected Provider `resolve()` | Router-selected video orchestration | `PURE` | — | Exact `ProviderBoundVideoRequest`、capabilities、profile与compiler identity | 直接创建new provider-bound request、adapter重创作/降级requirement或自行fallback |
| Start Generated-Video Attempt | `VideoGenerationService.start()` | Approved generated-video orchestration | `DURABLE_WRITE` | `ProductionStateCommitter` | Exact resolved request、activation scope与current base tuple | Provider effect、submit intent或candidate write先于durable attempt |
| Submit Local Video | `VideoGenerationService.submit_local_once()` | Owning local-video orchestration；stack-bound lane还必须提供其approved caller guard；符合`AGENTS.md` local ComfyUI exemption时无需user authorization | `LOCAL_EFFECT` | `ProductionStateCommitter` | Fresh pre-submit guard、durable local intent、committer-issued one-use permit | 直接调用`LocalVideoProvider.submit_local()`、Comfy transport submit或在intent前preview/submit |
| Submit Paid Video | `VideoGenerationService.submit_once()` | Explicitly authorized generated-video orchestration | `PAID_EXTERNAL_EFFECT` | `ProductionStateCommitter` | Exact preview、budget reservation、cloud egress、actor authorization、durable intent、one-use Paid Provider permit | 直接调用paid adapter `submit()`、adapter自行授权/结算、unknown outcome后remint或blind retry |
| Poll or Fetch Generated Video | `VideoGenerationService.refresh_once()` / `refresh_local_once()` / `fetch_once()` / `fetch_local_once()` | Owning orchestration for the same durable attempt | `EXTERNAL_EFFECT` / `LOCAL_EFFECT` | `ProductionStateCommitter` | Exact submission、latest durable phase、terminal success before fetch、held-FD sink | 直接调用Provider `get_status()`/`fetch()`并保留未持久化observation或未注册bytes |
| Refresh Remote Video Reference Lease | `VideoGenerationService.refresh_remote_reference_lease_once()` | Owning orchestration for one currently exact-active remote source Shot | `EXTERNAL_EFFECT` | `ProductionStateCommitter` remains evidence owner | Manifest active-pointer replay、reopened submit/status/fetch receipt、process-local one-use refresh permit、same task/model/locator、fresh full-byte accessibility verification、最多5分钟lease且每次resolve重验source仍current-active | 直接调用Provider lease helper、从unactivated/failed/superseded source签lease、另行mint refresh/lease authority、只做HEAD/host check、持久化signed URL、rotation后继续或fallback |
| Validate Generated-Video Candidate | `VideoGenerationService.validate_once()` | P8 candidate review/activation orchestration | `LOCAL_EFFECT` / `DURABLE_WRITE` | `ProductionStateCommitter` | Measured artifact、probe/provenance、active QA policy与required P6 continuity evidence | 为new one-action control调用legacy `fetch_and_activate()`、analyzer/evaluator自行activation |
| Activate Generated-Video Candidate | `VideoGenerationService.activate_once()` | Approved P8 activation orchestration | `DURABLE_WRITE` | `ProductionStateCommitter` | Exact current candidate、fresh complete evidence、current Project/Registry/graph base | 直接调用committer pointer primitives、把fetch/validation success当activation |
| Exact Generated-Video Replay | `VideoGenerationService.fetch_and_activate()` 的exact-active replay branch | Owning orchestration reopening the identical completed attempt | `READ_ONLY` | `ProductionStateCommitter` | Identical sealed request/resolved/artifact/activation identity | 重新submit、poll、fetch、analyze、mint permit或写Manifest；不得将legacy combined path用于new staged control |
| Recover Production State | `recover_production_state()` | Explicit operator/recovery orchestration | `RECOVERY` | `ProductionStateCommitter` | Exact old/new state、bounded owned temp与strict reopened evidence | Reader/Registry auto-recovery、猜测mixed state、自动激活orphan、blind retry |
| Resolve Selective Rebuild Closure | `resolve_dependency_state()` → `select_rebuild_nodes()` | Rebuild planner consuming the canonical immutable graph | `PURE` | Dependency resolver owns decisions；committer owns later writes | Current desired/applied fingerprints、typed dependency edges与exact closure | Blanket stale、第二resolver、graph推导timeline、单独推进atomic render unit members |
| Run P6 Review Lifecycle | `ProductionStateCommitter.begin_review()` → `run_review_analysis()` → `record_review_receipt()` | Policy-selected P6 review orchestration | `DURABLE_WRITE` / `LOCAL_EFFECT` | `ProductionStateCommitter` | Exact review intent、one-use analyzer permit、raw evidence、deterministic adjudication与fresh policy | Analyzer/model自报Production verdict、intent前analysis、raw evidence直接充当PASS |
| Run Ecommerce Production Closure | `run_ecommerce_ad_production()` → Shot barrier → preselected canonical render activator → private final-media closure → private P6 adapter → existing P6/Final Acceptance APIs | Canonical Ecommerce application orchestration | `LOCAL_EFFECT` / `DURABLE_WRITE` | `ProductionStateCommitter` remains sole writer and root | Shot-stage result、reopened exact Shot checkpoints、sealed handoff、committer-reopened canonical timeline/render、internally executed Gate 1、selected Ecommerce policy、caller-provided authorized evaluator与all required fresh PASS receipts；只有Final Acceptance后production result才`complete` | 接受任意MP4 path、把Shot-only return当deliverable、caller-supplied timeline/Gate result、shell/FFmpeg direct-delivery、重算timing、选择Provider、第二P6 writer、把function return外推为repository外publication/market success |
| Record Approved Repair and Outcome | `ProductionStateCommitter.record_approved_repair_receipt()` → scoped mutation → `record_repair_outcome()` | Trusted P6 repair orchestration | `DURABLE_WRITE` | `ProductionStateCommitter` | Policy-selected authorizer、exact P5 closure、rerender与fresh reviews | 未授权repair、blanket invalidation、repair module成为第二writer、旧review关闭outcome |
| Record Final Acceptance | `ProductionStateCommitter.record_final_acceptance()` | P6 final-acceptance orchestration | `DURABLE_WRITE` | `ProductionStateCommitter` | Current graph/timeline/render/QA policy与全部fresh required PASS receipts | `evaluate_final_acceptance()`结果或analyzer/console text冒充durable Final Acceptance |
| Render Final Composition | `render_with_hyperframes()` | Production composition orchestration | `LOCAL_EFFECT` / `DURABLE_WRITE` | `ProductionStateCommitter` | Canonical `ResolvedTimeline`、pinned local HyperFrames、2.2 graphic/source audit、durable selection、verified output | 低层runner、arbitrary HTML/JS/CSS/network、alternate renderer/fallback、第二timeline、直接激活未验证output |
| Import External Image | `prepare_human_image_import_commit()` / `prepare_automated_browser_image_import_commit()` → `ProductionStateCommitter.commit()` | Trusted human/import orchestration using truthful observed evidence | `DURABLE_WRITE` | `ProductionStateCommitter` | Exact measured PNG bytes、actor/source receipt、candidate Project/Registry/P5 transition | 仅调用asset/validation helper就声明已import、虚构backend/request、第二import writer |

只登记满足至少一项的entry：跨canonical owner、产生local/external/paid effect、durable write、replay/recovery-sensitive、normal caller与internal seam易混淆，或bypass会破坏Product invariant。不得登记private helper、ordinary pure utility、data class、receipt/pointer model、trivial loader、Provider内部helper、test-only API、函数签名副本或volatile runtime/provider status。本节不要求新增普通API时登记，也不得升级为独立registry、parser、schema、Harness/Architecture gate或第二ownership matrix。

## Completion Use

本矩阵只定义 surface-level focused gate。最终交付仍必须遵守 `AGENTS.md`：检查实际 diff ownership，通过 Development Harness 运行全部受影响 surface 的 mandatory checks，报告 fresh receipt path，区分本轮执行证据与历史记录，并明确 remaining risk、未验证区域和 publication 状态。
