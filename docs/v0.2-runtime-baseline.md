# AI-VIDEO v0.2 Current Runtime Baseline

Status: P2-P7、Base AI Comic E2E、P7.1 runtime contract、P8 provider-neutral/Paid Provider lifecycle、provider-neutral generation requirement与Provider-minimal output-recovery offline slices、ShotReadinessGate offline slice、QualityExperienceRecord v1 minimum Q0 passive-capture runtime、MiniMax adapters、generated-MP4 composition compatibility与Shot Continuity provider-neutral technical core均已进入本地版本历史或current local implementation change。Local MiniMax H3 `fl2va` sealed adapter、two-Shot technical live-local proof、additive quality profile Alice A/B与新requirement/readiness的单次Local H3 technical live regression已完成；4–8 Shots Quality Pilot、cloud/paid live、subjective quality acceptance与Final Acceptance仍未完成。2026-08-19 Seedance full-model adapter已完成offline/fake acceptance；同日一次授权的Mini diagnostic live submit已succeeded/fetched，但tracked audio-opt-out payload尚未live reverify、budget尚未settle、candidate尚未activation。P7.1 M8 blind benchmark仍未验收。
Scope: 当前 `0.1.x` Legacy runtime、project-local `video-analysis` MCP、P2-P7 Production contracts、P7.1 sealed ComfyUI adapter/human import、P8 provider-neutral generated-video lifecycle、plan-internal provider-neutral generation requirement、sealed output-recovery capability、pure ShotReadinessGate、Router-owned provider binding、deterministic adapter compilers、Paid Provider Gate、Fake/MiniMax/Seedance adapters、Shot Continuity request/evidence/activation/P5 contracts、独立的registered H.264 MP4 composition consumer，以及只写explicit non-Production dataset root的Q0 Quality Experience advisory data plane。
Branch Truth: cached `origin/main` 当前为 `007e99cb1d79f9c2b91a909b15db1f9360f65236`；ShotReadinessGate及其他后续工作位于local `main`，尚未push/release/publish。Local runtime truth不是GitHub runtime truth。

## Current Product Contract

- 当前产品是 local-first Python CLI；ComfyUI 默认必须是 loopback host，非本地 ComfyUI 需要 `allow_non_local: true`。
- 公共命令只有 `ai-video validate`、`ai-video run` 和 `ai-video resume`。
- `validate` 不创建 run、不上传素材、不提交生成任务。
- Manifest v1 和 flat `runs/<run_id>/` layout 仍是 runtime truth。
- Legacy generation path 仍只有 Wan/ComfyUI；v2 P3/P4 另有显式 Python API 驱动的本地 HyperFrames composition/render path。P4 提供 provider-neutral voice contract 与 ElevenLabs thin opt-in adapter，但没有默认或已授权的 live Provider path，也没有 Remotion、Captions.ai 或其它 renderer。
- Legacy final composition 仍是 silent-video normalize + stitch；`ffmpeg_tools.normalize_clip()` 和 fallback stitch 都显式使用 `-an`。这不改变显式 v2 P4 HyperFrames audio/caption render path。
- P2 只通过 `ai_video.production.load_production_project()` 暴露；不增加 CLI 或改变 Legacy loader。
- P2A-P8 只通过 Python API 暴露：`ProductionStateCommitter`、project/registry/voice/graph/review/repair/image/video lifecycle、Paid Provider Gate、provider-neutral contracts、explicit adapters、`render_with_hyperframes()`、pure decisions和explicit recovery；不增加CLI或改变Legacy Manifest v1/layout。
- Q0 `ai_video.quality_intelligence`只通过Python API暴露strict/frozen records、historical imports、immutable store、Manifest-derived cohort/roster/Pilot index、exact lookup与pure advisory projection；不增加CLI、dependency、database/service或Production write path。

## Development Governance Boundary

- 当前 repository 的 Codex、`AGENTS.md`、Skills、`.agent/`、`.agents/`、`.codex/`、`.workflow/`、Development Verification Harness、Documentation Contract Gate、Architecture Gate 与 developer-only Agent Memory 都属于 Development Governance，不是 AI-VIDEO Product Runtime dependency，也不拥有 production state。
- Development Governance现包含validator-backed evidence identity：新建或实质更新且opt-in进入distillation的empirical record使用flat `record_kind/topic_id/learning_eligibility/evidence_index_version`与compact `Evidence Index`；`distill-ai-video-learning`唯一按`independence_key`去重support/counter，并以`TWO_INDEPENDENT_ATTEMPTS | CONTROLLED_MULTI_ARM | MATERIAL_EXISTING_CLAIM_UPDATE`区分admission basis。Same-attempt的technical/human proof layers保留但只计一个unit，legacy/`needs_identity` record仍可检索且fail closed不admit。Skill-local validator只读显式paths；Agent Memory只向Hit/JSON/text投影allowlisted classification metadata，不改变ranking、authority、document kind或evidence count。`record-ai-video-session`完成substantial stable record后仍自动执行bounded candidate evaluation，record hook只在`recorded` ACK携带`no_candidate|pending_candidate`时关闭checkpoint。合格claim写入`docs/record_for_agent/learning/**`并以`advisory_learning / learning_claim`纳入experience corpus，在下一次authorized refresh后可由RAG检索；active adopted lane与pending candidate lane分离，自动流程只创建pending与只含claim path的candidate checkpoint。用户确认绑定exact candidate commit及其中file bytes SHA-256，目标owner完成真实修改与mandatory verification后pending才替换active并标记`ADOPTED`。本slice为forward-only，不迁移历史records、不改变既有pending candidate bytes，也没有Product Runtime、Q0 automatic caller、Q1 automatic recommendation、Provider policy engine或Production acceptance owner。
- Agent Memory dense answerability对real query与language-matched irrelevant control使用相同Chroma candidate budget，再比较score、null excess与Top-1 separation；不同`n_results`产生的approximate-neighbor偏差不得充当relevance evidence。Canonical null token若在真实record中出现，exact BM25 lexical provenance仍可返回，但不能因此获得`dense`或`hybrid` admission；authority、scope quota、ranking merge、stale-index与advisory-only边界不变。
- 当前 local implementation 包含 repo-local `ecommerce-ad-workflow` offline authoring Skill：progressive G0-G7 router、Product Truth / claim lineage / Hook / ad beats / product presentation / commercial copy / audio coverage / CTA / one-variable variants / capability-aware Runtime handoff references、strict versioned input/package JSON Schemas、30-second 9:16 examples，以及只读取显式 JSON 文件的 deterministic validator。它仍不联网、不读取credential、不调用Provider或生成媒体，也不写Project、Registry、Manifest或其他production state。独立的additive Runtime bridge现提供sealed `AdCreativePlan`、`create_ad_creative_plan()` accepted-proposal builder、`compile_ad_creative_handoff()` typed Shot/composition handoff、compatibility `compile_ad_creative_plan()`与pure `review_ad_creative_plan()`；exact plan hash、source-evidence pointer/provenance、product/graphic/sound bindings会进入existing `CompositionSpec -> ResolvedTimeline -> HyperFrames` path，不新增timeline、renderer、writer、activation owner或CLI。Verified physical product interaction、tracking/mask/occlusion/lighting/camera matching仍是显式capability gap；pure review比较完整projected semantics且`production_verdict`固定为`None`，没有P6、Final Acceptance、live-ready、push、release或publication结论。
- 当前Quality architecture的durable boundary选择`Universal Production QA + Domain-Specific Acceptance Gate`，不新增第二QA lifecycle。Additive caption-aware P6 artifacts use `2.1` and the mutable container uses Manifest `2.15`; historical artifacts and Manifest `2.0`–`2.14` remain strict-reopen compatible and cannot carry `CAPTION` state. `UniversalQualityGateCoordinator.run_once()`仍只拥有显式、non-persistent Gate 1 ordering；new profile v2 with captions requires structural binding、`LAYOUT`、`CAPTION` and matching policy before effects。Ecommerce public closure从`ProductionStateCommitter`唯一root重新打开active render/timeline，先执行或zero-write strict-reopen private CAPTION P6 transaction，再执行Gate 1；external review callback cannot supply CAPTION PASS。`EcommerceQualityGateCoordinator`随后把Gate 1 PASS/profile/context/result、sealed handoff、Shot lineage、exact timeline/output/policy与durable `ReviewRequest`封装为whole-ad target。Both private adapters only use existing `begin_review()` → one-use `run_review_analysis()` → `record_review_receipt()`；`ProductionStateCommitter` remains the sole lifecycle writer。
- Ecommerce `AdQCReport.ready`只表示authoring package readiness，`AdCreativeReviewReport.is_ready`只表示deterministic handoff integrity且`production_verdict=None`。`run_ecommerce_ad_generation()`仍明确是Shot-only API，不产生deliverable；`run_ecommerce_ad_production()`固定执行sequential Shot barrier → canonical render activation → CAPTION P6 transaction/replay → Gate 1 → Ecommerce Gate 2 P6 semantic receipt → required-receipt rollup。Caption context binds exact track/cue/audio/style/timeline/render/policy identities；six requirement groups use group-specific coverage, and `UNINTENDED_TEXT` requires whole-render coverage. Missing/partial/unsupported/conflicting evidence remains `NOT_EVALUATED`; current direct FAIL blocks. Only all current required PASS receipts plus committer-owned Final Acceptance produce `complete=true`. This is executable engineering evidence, not a human perceptual-quality verdict, publication guard, or market-outcome claim; no live Provider/media generation was performed by this slice.
- `.agent/harness/docs-contracts.yaml`只登记canonical spec metadata、finite implementation/live/quality/release statuses、owner/evidence paths、Harness check IDs与有限static assertions；`scripts/docs_contract_gate.py`只做local deterministic parsing，并作为Harness always check运行。它不证明任意自然语言段落的语义正确，也不取代本文件对动态implementation/evidence状态的ownership或human/Codex architecture review。
- Architecture Gate当前接受schema v1 metrics-only config与schema v2 module dependency contract；repository `architecture_gate.toml`以四个named module sets绝对阻止`src/ai_video/** -> scripts.*`、Legacy core反向依赖Production、Production反向依赖Planning/Quality，以及`_state_commit_* -> state_commit`façade。规则覆盖top-level与function-local direct runtime imports、relative normalization与standard `TYPE_CHECKING` guard区分；unknown顶层field、错误schema type、active但零匹配的module set、syntax evidence缺失及invalid/stale/expired exact exception均fail closed。Dependency rules不进入也不能由historical LOC/cycle/fan-out baseline刷新消除。该Gate仍只属于Development Governance，不证明完整module ownership、public/internal API、canonical writer、dynamic import、Node/workflow、runtime dataflow、SDD adoption或Production/media acceptance；没有新增独立architecture manifest或`modular-sdd` Skill。
- 当前实现没有 DeepSeek Harness、Cordis、通用 Product Agent Loop、Product Tool Registry 或 event-sourced Product Session。`VideoPlanner`、Shot Router 与 quality gates 是 deterministic domain logic；它们的命名不代表已存在通用 Agent Runtime。
- Product runtime 的 durable facts 继续按 domain 分治：Project schema、Asset Registry、Production Manifest、`ProductionStateCommitter`、Dependency Graph、`ResolvedTimeline`、Provider adapters 与 review/repair receipts 各自维持现有 canonical ownership，不由 developer session、prompt、memory 或 Harness receipt 替代。
- 未来若增加 Product Agent Session，session replay 只能恢复 model-visible context 或 non-authoritative telemetry；不得重新触发 Provider、renderer、analyzer、materializer、Manifest write、activation 或其他 external effect，也不得成为 production lifecycle 的第二事实源。

## Verified Implemented Behavior

- Provider-minimal evidence boundary现已完成offline implementation。Current remote capability使用独立的
  `output_recovery_strategy`区分durable file、effect re-query与non-recoverable one-shot URL；historical
  capability payload缺field时仍按原bytes/hash重开，但new remote attempt缺少supported strategy会在pure
  compiler、`VideoGenerationService.start()`之前以`OUTPUT_LOCATOR_NOT_RECOVERABLE`停止，且不会创建
  attempt、Manifest intent、permit或Provider effect。Seedance与MiniMax H3通过effect identity重新查询当前
  output URL，MiniMax Hailuo保持durable file ID，Local T8不进入remote strategy contract。Offline fake
  Provider只返回`job_id + status + re-queryable output_url`时已沿neutral requirement、Router、compiler、
  canonical fetch与AI-VIDEO local SHA/probe到达未激活`CANDIDATE`；optional Provider model/output echoes不能
  覆盖local media proof，provenance model/profile只表示requested/selected identity。Exact implementation
  range为`0f73c53..9e999d1`，Harness receipt为
  `.agent/harness/runs/provider-minimal-evidence-boundary-implementation-20260826-v3/receipt.json`。该proof
  没有调用Provider/network/credential/ComfyUI或生成媒体，也不证明activation、Gate 2、P6、Final
  Acceptance、live API grammar、billing或media quality。

- 2026-08-26 current local change 将 M0 从单一隐式 Stock20 route扩展为显式
  `fast-v1 | quality-v1` policy contract。新的P0 preparation与M0 materialization CLI都要求用户选择，
  selection被写入content-addressed calibration input并与selected M0 stack identity fail-closed绑定；无默认
  fallback，也不会在fast失败后自动提交quality。`quality-v1`继续使用既有byte-for-byte Stock20/no-LoRA
  compiler与20 steps；`fast-v1`使用独立offline-candidate profile/compiler/workflow，固定literal `Hybrid`、
  exact four anchors、sealed Turbo LoRA与4 steps。两条policy必须共享同一frozen M0 rubric，均可在取得exact
  artifact、requirement-level evidence后交给既有P6/human adjudication形成`PASS` / `FAIL` / `NOT_EVALUATED`；
  policy层本身不构造verdict或第二evidence owner，但结论
  只限定各自的`conclusion_capability_id`，不能互相资格化，也不替代P6、Final Acceptance或activation。
  Fast profile目前只证明offline source/materialization/caller contract；尚未完成fresh live node-schema preflight、
  Provider submit、媒体分析或human quality verdict，因此不得描述为live-ready或已PASS。

- 2026-08-23 Shot Continuity P0 contracts 已在 current local implementation 中形成 offline
  `qualification_prepared` seam：Manifest `2.11` 只选择一个 immutable P0 receipt pointer，
  `ProductionStateCommitter` 独占 stack/policy/validation/input/receipt writes、exact replay、reopen与recovery；
  Project/Registry identity变化会清除 selected pointer。独立 `AutomatedBrowserImageImportReceipt` 诚实记录
  GPT Image 2 MCP/browser automation + human approval、exact PNG bytes/dimensions/timestamps/prompt fingerprint，
  未知 backend/request identity保持空值且不持久化 signed URL。`HumanImageImportReceipt` 也可将
  `codex_imagegen_tool` 的 exact PNG 与后续 human approval 记录为独立 imported asset，并使用
  `codex-imagegen-import/1` tool identity；它仍不得虚构 backend/request、automated-browser 或 Provider
  execution evidence。`scripts/prepare_shot_continuity_endpoint_repair.py` 从既有 active creative bundle
  克隆 A1/A2/A4 exact identities，只 revision user-approved A3、Project/Registry/dependency/P0 closure，且不复制
  旧 video attempt；import receipt 同时绑定生成时选择的 exact A2/A3 Shot revision、content hash、artifact path
  与 endpoint asset ID/SHA，bootstrap保留这些content-addressed source Shot bytes，普通reopen重新核对且拒绝
  source内嵌target root；source qualification profile必须绑定active repaired Shot的正整数revision，不能硬编码
  初始revision `1`。Rainy-station bundle位于 ignored run root
  `runs/shot-continuity-rainy-station-p0-20260823-v5/production`。2026-08-23 candidate-neutral I1已通过
  qualification-only literal `Hybrid`四锚点compiler、Stock20/no-LoRA profile与applicable workflow关闭M0
  materialization gate。2026-08-24获准的loopback read-only `/object_info` preflight进一步将M0 workflow
  所需12个exact node-input schema seals写入M0-owned profile；canonical materialization owner以旧M0 stack
  hash为expected current identity执行显式reseal。2026-08-24后续qualification-only checkpoint又将
  exact M0 seed固定为由pre-generation content-addressed closure确定性派生的
  `186510892520232452`；任何其他caller-supplied seed即使非负也会在preview/intent/upload前拒绝。
  该profile/compiler变更由同一owner重新seal；Manifest revision `7`选择receipt
  `d1af93a23feceab1bb88c6988bf14cd768a5ddf12c2514c743ff4eb80645d981`，M0 stack更新为
  `a3711dbfee67be30549ec3d787bd76cd750eb36b5580955ed69368dcdd302230`，profile/compiler/workflow
  hashes分别为`87871386bc0481569586eee5d7822d5ba0d204ac1a69e73d714d6ac6926b9956`、
  `04403cfcedfc2032d0a0eb8f7c9aea17492e948a39559851bab9ff84799d4016`与
  `963bd91ad81ca102053deb08b29a7aa8fb6849a6256a78ccbfbc6138d7a855d2`；所有依赖M0 identity的
  qualification inputs、transition policies、validation set与receipt均已content-addressed reseal/reopen，
  actual exact replay保持revision `7`且88个project files的size/mtime/SHA-256不变。M1保持原stack
  `4d08741636647fbb29f9cf69a69a2c81d62156cef128f619d26a17b72e94c01b`与
  `materialization_status=unmaterialized`，缺失Hybrid artifact仍以`presence=absent`、
  `content_hash=none`表示；selected stack含absent component时materialization fail closed。
  Calibration 固定 source `1659x948 RGB` 与 exact `7:4` geometry，确定性等比 `64/79` 投影到 sealed Local T8
  `1344x768` canvas和 normalized float `BHWC [1,768,1344,3]` tensor；A2 metadata prompt误写
  `dark raincoat` 的事实保留为 limitation，后续 video prompt使用 H3 official three-field grammar并固定
  canonical mustard-yellow wardrobe。I1只关闭M0 submit的execution-stack materialization前置条件，尚未执行
  Validation V1、M0/M1 video generation、winner selection、active capability registration、creative PASS、
  P6 verdict、Final Acceptance、push或release。随后新增的candidate-neutral V1 pre-effect snapshot owner只执行
  `required_materialized_candidates=("m0",)` guarded reopen、exact source/frozen target/M1 absent/dependent evidence
  重验并返回immutable hashes；real-committer regression进一步证明exact replay保持project tree bytes/mtime zero-write，
  persisted source或dependent-evidence tamper经同一owner拒绝且不产生额外写入。Provider-neutral request、resolved
  request与activation scope现在可以optional seal同一`execution_stack_hash`，字段缺失时历史hash/reopen保持
  bit-for-bit；任何stack-bound local request省略pre-submit guard都会fail closed。`M0ValidationPreSubmitGuard`
  通过`VideoGenerationService.submit_local_once()`的显式callback先在preview前重开bundle并比对request stack，
  唯一committer再在同一个intent write lock内重复guard并紧接着持久化intent/permit，从而关闭P0 pointer或
  dependent evidence在preview窗口漂移的TOCTOU。Generic service不内建M0 policy，guard本身不写
  intent/permit/Manifest、不授权或执行M0 submit，因此Validation V1与所有媒体/视觉evidence仍未启动。
  2026-08-24后续source-bootstrap checkpoint已将selected offline bundle推进为
  `runs/shot-continuity-rainy-station-p0-20260824-v6/production`：P0 persistence不再要求
  `source_execution_stack_hash`别名到M0/M1 destination candidate，而是额外持久化、重开、验证并恢复一个
  independent local FL2VA quality source stack；receipt的ordered candidate selection仍严格只包含M0/M1。
  Manifest revision `4`选择receipt
  `286991af1b8c08ef213af794f353d4ad84becad69b117b15a9836a6a53487937`；source stack
  `1635abc300b33e94c513b2c92b6b99b770944b51d88a2689c69d619d861db2b4`与M0 stack
  `8f9661c358162544a3cefd0f7ff4483063658fcb37de09f301ef7fe2500039d2`均为`materialized`，M0
  seed更新为绑定该initial P0 closure的`6369959770063611369`；M1仍为原
  `unmaterialized`/Hybrid `absent/none`。所有qualification inputs现在同时绑定exact source与M0 stack
  hashes；policies分别绑定source stack与destination M0，materialization owner对二者共同reseal全部dependent
  evidence。source loader对同一次no-follow reopen得到的profile/workflow/binding bytes执行standard topology、
  binding与native-setting validation后才seal；M0 caller还要求canonical accepted upstream generation request的
  `execution_stack_hash`与该source stack exact一致，否则在intent、upload或queue effect前zero-write拒绝。一次
  actual exact replay保持revision `4`且project tree metadata hash
  `b71ef90b420da8e5ae63c7a51558fdaf3a9a4019d7ab1524aef7a737abc04707`、bytes hash
  `d3a7a632243c569448ba8f534e0bd8756729adbf645773cf5bde9a482227bde3`均不变；provider effects为零，
  没有生成video。该checkpoint只关闭source execution-stack bootstrap circularity，不等于已有accepted source
  output、motion-tail、P6 PASS或M0 submit许可。下一项真实gate是仅用该sealed FL2VA source stack生成并登记
  一个A2→A3 upstream Shot 3 candidate，经existing generated-video lifecycle和P6/human acceptance后，从同一
  accepted bytes派生terminal与motion-tail；随后M0只验证edge 3→4，使terminal=A3、identity=A2、endpoint=A4
  保持独立。

- 2026-08-24 current local implementation change进一步关闭source execution的offline ownership缺口：新P0
  preparation以real `Shot.shot_id`、唯一`approved_endpoint`与verified Project/Registry applied evidence建立active
  graph；generated Shot candidate会同步reseal可由activated Project/Shot/Registry deterministic重建的
  CompositionSpec，使output video进入composition与renderer-source edges，并由real
  `ProductionStateCommitter` regression证明unknown submit只留下一个durable intent且zero resubmit。source transport
  同时绑定profile deployment identity与实际literal loopback `base_url`。2026-08-24一次明确授权的local-only
  `/object_info` read将source workflow所需14个exact node-input schemas写入repository qualification profile，
  `source_node_schema_status=sealed`且profile content hash更新为
  `0c7bfb22fb5ec660a649366715617bf92b90e4fab2418ac29774192f2ff7fb58`；derived seed与既有P0/execution-stack
  closure均未改变。该read没有upload、workflow submit、poll、fetch、Provider generation或媒体产出；schema seal
  只关闭runtime inspection前置条件，不构成submit authorization、candidate、P6或Final Acceptance。

- 2026-08-25 current local implementation新增qualification-only non-test source operator：它只从exact
  qualification profile document、active Project/Registry/dependency-graph pointers、sealed source
  stack/profile/compiler/workflow与A2/A3 Registry bytes派生canonical-hashed compilation projection；最终
  `VideoGenerationRequest`仍只由`video_compiler.py`唯一构造，并通过internal
  `scripts/execute_shot_continuity_source.py`逐步暴露`inspect/preflight/status/submit/poll/fetch`。fresh guard
  拒绝dedicated bundle中的任意既有video attempt；preflight使用no-follow regular-file reads比对完整Production
  bundle hash并只输出source filename/hash/size summary，不返回PNG bytes。该operator
  复用唯一`ProductionStateCommitter`、既有source Provider与`VideoGenerationService`；不注册active Provider
  family，不提供endpoint/model/prompt/seed/retry/fallback override，且fetch后固定停在`validate`。Focused fake
  regression已证明preflight state tree zero-write、同attempt zero-resubmit与无automatic candidate/activation；
  本条记录尚不声称fresh real bundle、live submit、MP4、P6、human acceptance或activation已完成。

- 2026-08-25后续current runtime evidence已将上一条的live边界推进到一次且仅一次Local source attempt。
  Commit `69dbb56`将M0/source qualification profiles显式reseal到current canonical `124`-frame Project
  identity；fresh root `runs/shot-continuity-rainy-station-p0-20260825-v7/production`在Manifest revision `6`
  materialize source stack `482da299c4e3c434ae3f92fbac88982cf989d2ff333799e3ddba3e4073b17093`与M0 stack
  `b58597090ba9c630b21ef3691905a9728da1e2bcdf9a9e89531386c31a904573`，M1仍为unmaterialized
  `absent/none`。Zero-effect preflight重验ComfyUI commit、14个source/12个M0 node schemas、exact component
  bytes、A2/A3与完整bundle，durable tree hash before/after同为`c25505792988779c2ddd81d048540f08dfade15ee1e36764782b55cd66d03dd5`。
  Exact attempt `rainy-station-source-a2-a3-20260825-v1`使用seed `3625644815483226956`完成
  `submit/poll/fetch = 1/1/1`、retry/fallback/remote/paid=`0`；fetched MP4位于
  `state/video-generation/fetch/files/5ac292e7ad42dc3a1692b699a3a54378d7b9feabbd23d82cc8209adc506c8f02.mp4`，
  SHA-256 `5ac292e7ad42dc3a1692b699a3a54378d7b9feabbd23d82cc8209adc506c8f02`，2,965,696 bytes，
  H.264 High `1344x768@24fps`、124 frames、5.167s，并含32kHz stereo AAC；完整video/audio decode通过。
  该artifact仅technical fetch PASS：frames 107→108（4.458s→4.500s）decoded RGB MAD为`42.6812`并有
  明显构图/时空跳变，frames 120→123 pair MAD仅`0.0642/0.0433/0.0559`，违反frozen no-cut、
  no-teleport与continuing-gait hard gate。因此source quality结论为FAIL，automatic P6保持`NOT_EVALUATED`、
  human/P6 verdict未记录；Manifest revision `11`停在`next_action=validate`，没有candidate asset、activation、
  terminal/motion-tail derivation或M0 effect。用户随后授权existing exact MP4 real-media review；Codex直接查看
  全片contact sheet、frames 104–111与116–123逐帧条带，确认同一hard-cut式构图瞬移及near-static tail，
  不只是metric推断。Exact source request的`continuity_binding=null`，因此current
  `HybridContinuityEvaluatorV1`不能canonical运行且不得合成binding；direct visual evidence保持source
  `FAIL`，但不冒充human/P6 verdict或durable candidate preparation。ComfyUI queue清空后已由supervisor停止。

- 2026-08-26 current repair checkpoint保留上述v7为historical failed evidence，并以用户批准的独立
  motion-compatible A3 endpoint建立fresh root
  `runs/shot-continuity-rainy-station-p0-20260826-v9/production`。新A3 asset
  `image-import-ff896c4d23cfd1b0d426e64f6243c4c5cca3f9ed2e95a3dfb5b08821e98e2322`绑定exact PNG
  SHA-256 `347e1208215f2d2a4a0b37ec60dbed62daed19dd62ac2a776ab1b308f9fb9c1c`；repaired target Shot
  revision/hash为`2`/`691dd13ec05b9ef19910649e89c38b7be50235fb5a4c5215ea21c3ff37f3ffe8`。P0 receipt为
  `80149ed5e866572b9bd10e4ea37c7ebe452e8cd4497e4ab5ebc9387b75e11fc2`；materialized M0 stack/profile为
  `073f0f429f49d828538b12fce6d7616a7834ebb53dbe31efe43c22fda41d0a24` /
  `808f688e62131653d0d3e2b0ffa877645a684b4011bc680dfa42a653c98f975a`，source stack为
  `482da299c4e3c434ae3f92fbac88982cf989d2ff333799e3ddba3e4073b17093`，M1仍unmaterialized。
  Source qualification profile派生sealed seed `3742455256126004804`；唯一attempt
  `rainy-station-source-a2-a3-motion-endpoint-20260826-v1`以request hash
  `3dd67dc23cc7e7cd981d0f49e75cfcfb4b23ab3967f3ef9365f4fd9029b055ed`完成
  `submit/poll/fetch = 1/1/1`，retry/fallback/remote/paid均为0。Fetched MP4 SHA-256为
  `5324a2b0c65fea658b0ab5267020dbb00ecc080988a50987c46c1f9d21e4bbdf`，2,484,571 bytes，H.264 High
  `1344x768@24fps`、124 frames、5.167s并含32kHz stereo AAC；完整video/audio decode通过。

  Manifest已显式从`2.11`升级到additive `2.14`，再对上述already-fetched exact bytes执行独立source-boundary
  P6 checkpoint；没有再次调用Provider或ComfyUI。Measurement contract
  `2b5aaddb094453798059a808722c31e7de38f390ede7a433035009e05216c248`冻结decoded frame `0/123`、
  ffmpeg `rgb24`、Pillow Lanczos no-crop、RGB PSNR `>=30 dB`与BT.709-luma uniform-7x7 sample-covariance
  SSIM `>=0.9`。实测first为`33.510 dB / 0.922665`，last为`32.539 dB / 0.905545`；exact-bound human
  decision为`PASS_WITH_PACING_WAIVER`，decision hash
  `51d3523e4125d20fd3c0a046f57f202c1ca1a7c0f911be310b45d03b28646a05`。Intent/evidence/receipt hashes为
  `cb3e1fae39fca585a42dc7731300fa2e8ce8400fc170e3aa22b416e757403e94` /
  `085bd34f5830d35fe2a09b155e4c0870d81072ec57eb21e5e9bff394e1303b98` /
  `47bd637b7abdc0c6e9a3227f6b4f770b6c61f007d796ea158f4b090bba892048`，P6 verdict为`pass`。

  Current Manifest revision `20`、phase `candidate`，strict reopen为`2.14 / 20 / candidate / pass`；inactive
  candidate的Project revision/hash为`3`/`f05a3f681bfedcf804e9fb59fb6fa85537aa45ec37e987b8449723f5b05de8f5`，
  Registry为`a74c81510fb2ebae271525e95480c3ac934a569e5860c7b0e8bd364b794f655d`，Dependency Graph为
  `520b59b0b43bcbd811ebfa56d862015f5a7a7f3c0d52e52a5f1ed2b8d65658cf`；active pointers仍保持Project `2`、
  Registry `8902beaf...`与Graph `74180e63...`。该closure没有合成
  `continuity_binding`；candidate及terminal evidence已准备但未activation，M0/M1、Final Acceptance、push、release
  均未发生。v7/v9 numeric seed不同，故observed repair的因果归因仍只由historical isolated endpoint A/B支持。

- 2026-08-26 current M0 `quality-v1` checkpoint在同一v9 root执行了exactly one local submit。提交前新增并
  strict reopen full-source zero-copy motion-tail：独立derived asset
  `video-shot-rainy-station-3-motion-tail-m0-quality-v1`复用accepted source SHA-256
  `5324a2b0c65fea658b0ab5267020dbb00ecc080988a50987c46c1f9d21e4bbdf`，receipt为
  `83d2b14a0b1f23bfdf96edcc211d685cdef9b2b71b143b29f879a81698b3b77e`。Fixed ffmpeg analyzer v2对
  frames `0..61`与`62..123`分别确认61个non-zero consecutive transitions；exact A4 feasibility approval
  `1c9da8c69fb16b705eaf8e39c7e6747a15531303d0de774aead98f9578886c37`绑定attempt/generation/output、
  `quality-v1`、M0 stack
  `c37f6a594e473a5d73ed6a19a4d3e9892420880a99ed71149e8e525b4c519b4d`与profile document
  `367de2fa959634781849d152932b4fc0ef5cc984218144e00670ad04d5b0b8c1`。Real `open + preflight`
  重新哈希exact model/runtime/input bytes并通过；没有Provider effect。

  唯一attempt `rainy-station-m0-quality-v1-20260826-v1`以resolved request hash
  `392e098918b6dfb4aacd05db0e546735a7288e90ad5bfe4d3d5fa8ff96992163`取得Provider request
  `1873dc95-b44c-4218-8407-5b2610b31d07`。它在`SamplerCustomAdvanced`执行约`48.80` seconds后因
  `torch.OutOfMemoryError`终止；Comfy history为`status_str=error`、`outputs={}`。Manifest `2.14`
  revision `31`持久化`video_provider_failed`/`failed`，observation
  `3efe1d80eb02dd29b51a6fe5467ba0a1bf532c00455d52386094eed1c0372db4`，local fetch receipt为空、
  candidate asset列表为空，next action为`stop`。Action count为submit/poll/fetch=`1/1/0`，retry/fallback/M1/
  activation=`0/0/0/0`；没有MP4，故media analysis、P6、human visual verdict与Final Acceptance均未执行。
  Queue清空后ComfyUI已停止，T8 checkout恢复到原
  `28cb160827c245b2d6a37539df30c1d7c5e7aecd`。该结果是runtime-capacity M0 FAIL，不是model-quality
  verdict；M1 exact validated pruned pair仍`absent/none`，所以conditional M1 inventory gate保持blocked。

- 2026-08-26 isolated `--novram` v2 attempt在新的exact approval
  `84912344b7a7e1d23ac808d3a996947d89e26e199df46551ec4bebd11455fade`下执行；approval以
  `7f9198e1c803bdc464fd16fa175131806cafc4535e09224b55ac83604bd73e1b`绑定用户选择`A`、同一v2 scope、
  exactly one submit与no retry/fallback/activation/M1。Commit `efac4018a5d218b6ce4bfa70cb955e70cdeac921`为
  supervisor新增显式`--novram`，保持默认`--lowvram`与`--use-sage-attention`；focused `17 passed`、
  native reviewer `accept`，exact-range receipt
  `.agent/harness/runs/comfyui-supervisor-novram-range/receipt.json` fresh PASS。

  Fresh unit的exact `ExecStart`仅包含`--novram --use-sage-attention`，submit前queue empty、GPU free约
  `21.1 GiB`。Real preflight通过后，request
  `af5975ea6ce075b7af7d3b8caeb2851d9188fc3cd929afbf1fc3af5f0f0b286d`取得Provider request
  `8a944ae0-ac95-4ea9-9d38-60262ba0d568`。唯一poll在约`10:56`后观测到node `10`
  `SamplerCustomAdvanced`的`torch.OutOfMemoryError`，trace为
  `comfy_kitchen.int8_linear -> torch.cat(scaled_parts)`；peak allocated/reserved为`16007/19424 MiB`。
  Observation `904669db6d980121b9eb6e34962e9e945bc3bbbac0f0f651c70892d5e6dfaa5a`将Manifest推进到
  revision `39`、terminal `video_provider_failed` / `failed` / `next_action=stop`。Submit/poll/fetch=`1/1/0`，
  retry/fallback/activation/M1=`0/0/0/0`；无MP4，所以probe/decode/`video-analysis`/P6/human verdict均为
  `NOT_EVALUATED`。Queue清空后supervisor已停止、8188关闭，T8恢复`28cb1608…`且clean。

- 2026-08-26 MCP-released `--lowvram` v3 attempt在用户选择`A`后执行。Exact project-local
  `video-analysis` MCP PID `3329563`以`SIGTERM` clean exit，释放其`6534 MiB` GPU占用；不属于本task的两个
  Godot进程未触碰。外部owner的ComfyUI unit退出且8188关闭后，GPU free由约`21.2 GiB`升至`27.3 GiB`。
  Human decision/approval
  `4917189d62cc65b295a26f4534e0bde6322b6d50fc514797fca8258014e5aaf1` /
  `a9130be5880bea854e53d65672c438788ace605c9f116e5f3aa14d53a28f568b`绑定scope fingerprint
  `737534a93efb817cb2b160b870da84f5a2e2bfc3c5809665aa3fd096fb5c992c`、default `--lowvram`、exact one
  submit与no retry/fallback/activation/M1。

  Fresh unit的exact `ExecStart`为loopback `--lowvram --use-sage-attention`，submit前queue empty、GPU free
  `27492 MiB`。Real preflight通过后，request
  `168a91ec21c84f1dfc7b539e4d9a7f10e7cf0170ec3f70b429feb645e32cec73`取得Provider request
  `22f4e9d9-5d24-43c2-b16c-799d5b05503f`。唯一poll在约`52.77 seconds`后观测到node `10`
  `SamplerCustomAdvanced`再次OOM；trace为`comfy_kitchen.int8_linear -> torch.cat(scaled_parts)`的MLP
  `self.fc1(x)`路径，peak allocated/reserved为`23706/25952 MiB`。Observation
  `20e3d685966a4d3b180dca143dbdeab8b64aaee20555b7d738bc390014b31b1a`将Manifest推进到revision `45`、
  terminal `video_provider_failed` / `failed` / `next_action=stop`。Submit/poll/fetch=`1/1/0`，
  retry/fallback/activation/M1=`0/0/0/0`；无MP4，故probe/decode/`video-analysis`/P6/human verdict仍为
  `NOT_EVALUATED`。Cleanup后supervisor停止、8188关闭、T8恢复clean原commit；被释放的PID `3329563`保持退出，
  `nvidia-smi`不存在`ai_video_mcp` GPU compute app，其他CPU-only MCP client process不属于本次termination scope。
  该attempt证明释放MCP提高了exact workflow可用headroom，但当前Stock20 Ref2VA/Hybrid route仍为capacity FAIL，不是
  model-quality verdict。

- 2026-08-27 isolated `--novram`随后证明同一M0 `quality-v1` Stock20/no-LoRA、20-step T8 route可完成runtime。
  V4 full-source-reference与v5 exact 2秒terminal-motion-tail/fixed-historical-seed分别执行一次独立授权的local
  one-submit，均完成poll/fetch并产生`1344x768`、24fps、124-frame、5.167s MP4；因此三次早期OOM只保留为各自
  attempt history，不再代表current execution capacity。V4被human reviewer以人物移动明显偏慢拒绝并canonical关闭。

  V5 attempt `rainy-station-m0-quality-v1-terminal-tail-20260827-v5`的request/fetch/artifact分别为
  `6f4af2798a8acf145903a7ab3981a32cc8e1763c1e088cdaafb2825fcd727acd` /
  `909ebbeaf84b6cc5b904d693fd92a65bad535ae9a5af3907070d5bb81a23e7a9` /
  `22bdcad0ee2da2614b6781bd8d46ffd478174259557ca96be66614dd61ec49e6`。Full decode与literal unique-frame
  evidence通过，但human verdict为“速度不通过、后段变成近静图、seam明显”。Advisory Farneback诊断定位frame
  `83 -> 84` / `3.500s`的唯一大突变，并测得`3.500-5.167s` mean flow `0.012688`，相对前2秒下降约`91.6%`。
  Manifest `2.14` revision `65`已将attempt关闭为`video_provider_failed` / `failed/validate` / `stop`，完整local
  fetch evidence保留、candidate asset IDs为空；没有activation、retry、fallback、M1、P6或Final Acceptance。
  这是真实model-quality FAIL，不是capacity或播放器速度结论，也不撤销既有30秒成片的独立human acceptance scope。

- 2026-08-27 v6 isolated layout-repair experiment进一步关闭了v5 root-cause uncertainty。ComfyUI
  `e01fb4c56b7a88149d469b99cbbfe3223d715054`已将last-frame keyframe `cond_t`绑定到累加reference span后的
  target origin；M0 stack `c1e2c7bff9bf312eaf76a9996561a1c3334412aa4ffe2faa5360c38933afb89a`、profile
  materialization hash `244db35ca7123455ec31c1a3e7615d2b715af5ebeb45e136264aba9eaa5daf9e`与fixed seed
  `5696170359736854830`已通过canonical inventory reseal与zero-write replay。唯一v6 request
  `f7db17c2196195e68e70712b0471c8c7ee4b238031422ca6d823f2a3093183c8`完成submit/poll/fetch=`1/1/1`，
  exact MP4 SHA-256为`dc84a342e11e799e34b56e5257f3b6e7b7ca95205cf54ba2c288b3db8ab03580`，H.264
  `1344x768`、24fps、124 frames、5.167s，AAC 32kHz stereo，full decode通过。Frame-level measurement显示
  hard snap从v5的`3.500s`推迟到frame `101` / `4.208s`，endpoint SSIM单帧增量仍为`+0.742706`，随后
  frame `104 -> 123` flow约`0.006456..0.010950`。用户原速human verdict为motion speed、latter-segment
  dynamic continuity与seam continuity三项`FAIL`。Manifest `2.14` revision `75`已canonical关闭v6为
  `video_provider_failed` / `failed/validate` / `stop`，fetch evidence保留且candidate为空；没有retry、fallback、
  activation、M1、P6或Final Acceptance。RTX 5090已证明可在`--novram`完成current route，因此该结果是current
  exact H3/A4 conditioning profile的model-quality rejection，不是hardware或全部local H3 capability的general
  verdict。Current path停止继续prompt/seed/attention/retry迭代；任何新的endpoint contract或模型路线仍需独立授权。

- YAML + Pydantic config loading，以及相对路径到干净绝对路径的解析。
- API-format 和 UI-graph workflow 都通过 production loader 加载。
- Template + binding 渲染 prompt、seed、init image、resolution、frame count、generation FPS、output prefix 和 character reference，而不是在 pipeline 中写死 node ID。
- 顺序 Shot execution、bounded typed-error retry 和 optional last-frame chaining。
- `CharacterProfile` 与 reference image config；当前 pipeline 只上传并哈希每个 Character 的第一张 reference image。
- terminal failed Attempt、Shot failed state 和 Run failed state 在异常逃逸前通过 `atomic_write_manifest()` 持久化。
- Resume 验证 Shot clip/last-frame hashes，并对真实消费 previous frame 的 direct consumer 比较 `chain_input_hash`；same-hash regeneration、explicit `init_image` 或不存在 binding 时停止传播。
- Attempt-qualified staging、canonical artifact promotion 和进程内 rollback，避免 failed resume 覆盖上一次成功 clip/frame。
- `shot.fps` 只覆盖 workflow generation；`defaults.fps` 同时作为 generation fallback 和固定 delivery normalization FPS。
- ffmpeg/ffprobe clip validation、last-frame extraction、normalize、stream-copy stitch 和 re-encode fallback。
- Project-local `video-analysis` MCP 提供 probe、frame extraction、Whisper transcription、scene detection、heuristic review、optimization planning 和受限的 config/shot edit 建议。加载本repo MCP config的native Codex agents可见同一组8个tools；全部显式tool calls（包括独立stdio server processes）与background hook worker共用`.git/ai-video-analysis-hook/worker.lock`并按repo串行执行。显式waiter在取得lock前被取消时不会随后启动operation，operation启动后的cancellation仍继续持锁至worker结束。该lock只阻止execution overlap，不共享或释放各process的Whisper/model cache。
- Codex `.codex/hooks.json`与Claude Code `.claude/settings.json`复用同一个project-local generated-video analysis queue；Claude MCP `content[].text` JSON与既有structured tool response都必须明确返回fresh repo-local MP4才异步排队现有`video_analyze` core。relative output按event cwd解析，先跳过existing queue identity后单event最多排队64个new exact outputs，并只启动一个detached batch；同repo media analysis继续由worker lock串行。Hook本身不运行media work；后台worker从held source FD复制mode-`400` private snapshot，只分析该snapshot，并在写result前重验snapshot/source SHA-256与queued metadata。result identity同时绑定source SHA-256和automatic analysis-profile fingerprint；automatic profile最多抽4帧、执行scene detection且不启用Whisper，结果只写`.git/ai-video-analysis-hook/`作为advisory raw evidence。Claude-compatible client的model endpoint与credential继续由client config拥有，hook/MCP不保存、不选择也不调用语义模型。它不替代显式MCP调用，不进入Product Runtime，也不拥有P6 intent、permit、verdict、activation或Final Acceptance。
- Strict/frozen v2 `ProductionProject`、`ProductionBrief`、`Story`、`Character`、`Scene`、`Storyboard` 和 `Shot` schemas，未知字段被拒绝。
- Deterministic artifact envelope hash 与 exact `ArtifactReference`；Production Manifest 同时固定 active project revision/content hash 和 registry revision。
- 六种 Shot `visual_strategy` 的 static validation，包括 concrete role-to-asset ID/type/source binding、deterministic motion directive 和 explicit hybrid layers。
- Read-only content-addressed Asset Registry verification：duplicate ID、revision filename/content hash、local file size/SHA-256 和 typed provenance fields。
- `project.yaml`、`state/`、`creative/`、`assets/` 和 `asset_root` 的 traversal/symlink containment；internal symlink 只在 resolved target 仍留在对应 boundary 时允许。
- P2 loader 无网络、不创建目录、不改变 input mtime，且保留 `ASSET_REGISTRY_INVALID` 与 `PRODUCTION_PROJECT_INVALID` 错误分层。
- P2A 的 `ProductionManifest` 是 v2 project/registry state lifecycle 的单一 owner；其 nested active pointers 固定 exact relative path、semantic revision/content hash 和 file SHA-256。`project.yaml` 仍是稳定且会被验证的 entrypoint，但不是 active snapshot bytes truth。
- `ProductionStateCommitter.bootstrap_initial_state()` 是空 project root 的唯一 canonical bootstrap API：caller artifacts 必须精确等于 sealed Project 与 Registry 的 referenced-artifact closure；完整 bundle 先在 scratch 经 `load_production_project()` strict reopen，随后才写 immutable Project/Registry snapshots、stable `project.yaml` 并以 Manifest 2.0 revision 1 单点激活。相同 bundle 即使使用不同 `attempt_id` 也只做 zero-write exact replay；不同、缺失、额外或 tampered inputs 均 fail closed。
- `ProductionStateCommitter` / `state_commit.py` 是唯一 v2 writer/recovery owner：POSIX 同一文件系统下用 `state/commit.lock`，immutable snapshot temporary write + file fsync + promote-without-overwrite + parent-directory fsync + reopen verification。Manifest 还会 durable record running/failed attempt lifecycle；仅 final active-pointer atomic replace 是 single logical commit point。
- P2A recovery 只能显式调用，不会在 reader load 中自动执行。它只接受完整 old pair 或完整 new pair；mixed/tampered active state 失败而非猜测。它只清理 bounded、non-succeeded attempt 的 owned temp files，并 preserve/report complete orphan snapshots，不删除、不激活。
- P3 的 strict `CompositionSpec` 解析为 sealed `ResolvedTimeline`；后者是唯一 frame/sample order、Shot boundary与source trim timing truth。除 local PNG/JPEG/WebP `STATIC_IMAGE` 外，独立 generated-MP4 compatibility slice 现接受带 exact `VideoAssetMetadata` 的本地 H.264 MP4 `GENERATED_VIDEO` / `EXISTING_VIDEO` spans；MP4 delivery尺寸/fps必须匹配、trim必须精确覆盖resolved Shot。Additive 2.2 contract只允许显式列入`graphic_layer_ids`的registered image作为generated/existing-video Shot上的graphic layer，并把bounded commercial text、claim references与entry/exit treatment解析到同一timeline；未声明overlay继续按Shot primary media type fail closed。`IMAGE_MOTION`、`MOTION_GRAPHICS`、`HYBRID`、arbitrary motion directive、crossfade、tracking/mask/occlusion/lighting integration和Remotion selection仍明确失败。
- P3 固定 `hyperframes@0.7.103` 与已验证 Chrome Headless Shell `152.0.7928.2`。每个 version/doctor/lint/check/render tool process 都进入 fresh user/network/PID namespace；非 version 命令必须带 root `--json`，controlled env 不继承 proxy/credential，且没有 host 或 alternate-renderer fallback。
- HyperFrames source 使用 root boolean `data-no-timeline`、root-only duration、clip frame/sample metadata 与 capture-safe CSS keyframes。MP4 span以stable-ID muted local `<video>`和resolved `data-start` / `data-duration` / `data-media-start`消费；2.2 commercial graphics只使用deterministic local HTML/CSS、escaped text、boundedplacement与constrained fade/slide/scale treatment，不开放script、event handler、external CSS/font/import或network。Native source audio不会绕过P4 mixer。结构化 URL/media/source audit、lint/check JSON、bundle hash 和 phase rehash 在 render-state activation 前完成。
- `render_with_hyperframes()` 在任何 executable 前 durable begin；普通 pre-activation failure 由 outer lifecycle 终结，进入 `activate_render_state()` 后的 candidate/final 四个窗口由 P2A writer 独占。成功路径是 R+1 selection、R+2 candidate-prepared、R+3 single `active_render_state` switch；不确定 replace outcome 必须 explicit recovery。
- Canonical immutable layout 位于 `state/render/{timelines,sources,source-receipts,render-receipts,outputs,states}/`；scratch 位于 safe attempt root 且不进入 semantic receipt。输出 hash、size、ffprobe metadata、framemd5 fingerprint 和 durable bytes 全部绑定同一 held verification FD。Verify failures统一为 P3 `RENDER_FAILED`，不泄漏 `FFMPEG_FAILED`。
- Production Manifest 2.0/2.1/2.2 继续只读兼容；2.0 的首次 render lifecycle write 原子迁移到 2.1，已有 2.2 不会被降级。2.0-2.2 保留历史 project/registry pair-change pointer behavior；Manifest 2.3 不再用 blanket stale，而由 P5 dependency states 精确比较 desired/applied。Immutable source/output/state evidence始终保留；recovery 使用 exact selected identities 并 preserve/report complete render orphans。
- P4 将 `dialogue | narration | ambience | sfx | bgm` 建模为 first-class audio kinds。Local import 与 generated speech 都记录 source、speaker/voice、language、script hash、duration、sample rate、channels、loudness、timing、gain/fade/ducking 和 typed provenance；物化后必须 probe、hash、reopen verify 再进入 registry。
- Provider-neutral `VoiceAssetProvider` 接收 immutable/self-sealing `VoiceGenerationRequest`。提交前必须完成 deterministic budget/egress preview 与 authorization；P2A 先写 R+1 request，再写 R+2 submit intent 并签发 committer-issued one-use permit，随后才允许 provider call。Provider outcome 绑定 materialized audio、alignment、cost、policy/retention 与 provenance receipts；R+3 candidate graph 验证完成后才由 R+4 atomic Manifest activation 切换 exact Registry 2.1 revision。
- Canonical `CaptionTrack` 记录 script/transcript identity、segments、optional words、half-open start/end、speaker、segmentation policy、confidence/provider、style reference 与 timing fingerprint。它是 structured source 而不是 burned-in pixels；selected HyperFrames renderer 负责 layout/drawing。
- P4 通过 `CompositionSpec.audio_tracks` / `caption_tracks` 扩展既有 P3 contract，并由同一个 `ResolvedTimeline` 解析 canonical `audio_spans` / `caption_cues`。该 timeline 继续独占 order、frame、sample 和 timing；不存在第二条 canonical audio timeline。
- HyperFrames 只消费已解析的 timeline，按 canonical order deterministic mix gain/fade/ducking，并在同一次 selected-renderer invocation 中 mux audio、按 half-open cue window 绘制 captions。Source/output/receipt 验证继续在 P2A activation 前完成；caption pixels 不是 `CaptionTrack` 的持久化真相。
- `ProductionStateCommitter` 仍是唯一 v2 writer/recovery owner。Voice lifecycle 的 R+1/R+2/R+3/R+4、audio import 和 render activation 都写同一个 `ProductionManifest`；成功 P4 lifecycle 使用 Manifest 2.2，Registry继续使用2.1，Composition/ResolvedTimeline现在接受forward-compatible 2.1 audio/caption与additive 2.2 advertising graphics extensions。2.0/2.1 payload与source-byte compatibility保留，禁止schema downgrade、第二Manifest、第二registry writer或automatic recovery。
- Provider call 后任何不能确认 authoritative outcome 的 crash window 都持久化为 `outcome_unknown`，explicit recovery 不会 remint permit 或 blind resubmit。Exact replay 不重复调用 provider/renderer；tampered/stale/mismatched evidence fail closed，pre-activation failure 保持旧 active triple，complete orphan 只 preserve/report。
- 默认 acceptance 使用 committed deterministic WAV/caption/style fixtures、fake provider/fake transport 和 no-network tests。ElevenLabs 只是显式 opt-in concrete candidate，adapter 不从 `ai_video.production` package root 导出；本 slice 未安装 SDK、未读取 secret、未使用免费或付费 quota、未发起 live call。真实调用即使账户有免费额度也必须另获授权，并重新通过 budget、egress、secret redaction 与 crash-safe request/outcome persistence gates。
- P5 `DependencyGraphSnapshot` 2.0 是 immutable、content-addressed/versioned resolver input，只保存 typed nodes、typed edges、`DependencyReason` 与 fingerprint contributions。它没有 desired/applied、fresh/stale/failed/blocked/superseded 或其它 mutable status，也不计算 Shot order、frame、sample 或 timing。
- `ProductionManifest` 2.3 是 `active_dependency_graph`、per-node desired/applied fingerprints 与 lifecycle 的唯一 owner。Desired fingerprint 由 canonical local contributions、typed edge contribution 与 upstream desired 递归组成；无变化输入停止传播，变更只让精确 transitive dependents stale/blocked。Failed same-desired 保留且不auto-retry；changed desired重新进入stale ready frontier；exact replay无execution unit与state advance。
- P5 显式消费 P2 creative/registry semantics、P3 CompositionSpec/opaque ResolvedTimeline/render contracts，以及 P4 voice、audio、alignment timing、caption style与mix inputs。`ResolvedTimeline` 继续独占 order/frame/sample/timing；Graph 不重算或另造 canonical timeline。
- CompositionSpec、ResolvedTimeline、renderer source与render是一个durable `render_with_hyperframes` execution unit；四个node只有在final `RenderStateSnapshot` activation后才能在同一个Manifest replace中一起fresh。旧render pointer不能证明新composition/timeline，`record_dependency_node_applied()`不得分别推进这四个node。
- `ProductionStateCommitter` 继续是唯一 Manifest、registry、render与graph writer/recovery owner。Canonical graph snapshot 位于 `state/dependency_graph.<revision>.json`；project/registry/voice/render/graph activation共用同一个 final Manifest replace commit point。Graph temp write/fsync/promotion/directory-fsync/verification/final replace crash windows、unknown outcome、partial/temp/orphan snapshot、forward-compatible reader/recovery与idempotent replay均fail closed；不存在第二 graph writer或automatic recovery。
- Required mutation matrix 使用 two-Shot、two-visual、two-voice、two-caption/style、BGM/SFX、real deterministic ResolvedTimeline 与 fake source/render receipts，证明 script、voice settings、alignment policy、caption timing/style、audio mix、visual asset、CompositionSpec、renderer source/render contract只重建精确节点；alignment receipt在timing fingerprint不变时不会制造blanket stale。P7 通过既有 generated `AssetRecord` seam 接入，未修改 P5 resolver 或 `dependency.py`。
- P6 将 QA policy、ReviewRequest、ReviewEvidence、Review Receipt、RepairRequest、Approved Repair Receipt、Repair Outcome 与 Final Acceptance 建模为 immutable、versioned、content-addressed evidence；Manifest 2.4 introduces the lifecycle owner and Manifest 2.15 additively permits caption-aware `2.1` policy/context/evidence/receipt/pointer state. The reader never migrates; selecting a caption-aware policy performs the sole explicit atomic upgrade. Strict project reopen and Final Acceptance rehash the CAPTION request/evidence chain and recompute its verdict. P5 graph仍不保存 mutable review status。
- `ProductionStateCommitter` 在 lock 内从 verified `LoadedProductionProject + ResolvedTimeline + Shot.visual_strategy` 生成并重验 non-empty、无遗漏/重叠的 technical windows，durable consume review intent后才签发 process-local one-use analysis permit。`video-analysis` 只消费 permit并收集 raw luminance/audio/frame evidence，不写 Manifest、不判 Production verdict；公开 MCP surface不能绕过 committer。
- Technical、layout、strategy、semantic 与 final acceptance 分层 fail closed。合法 `static_image` 不因低 frame diversity失败；video-like strategy 的 genuine frozen、black、silent、clipped evidence由 selected policy裁决；semantic PASS必须来自 policy-selected evaluator/human identity，heuristic、console text或Agent memory不能自我升级。
- Repair approval默认关闭，必须由 injected trusted authorizer返回 policy-selected actor；authorization、exact targets与P5 transitive invalidation closure被 sealed receipt绑定。只有 approved repair能进入现有P5 transition；exact replay不重复analysis/repair/render，unknown outcome不blind retry，rerender后必须有current fresh review才能记录outcome或final acceptance。
- QA policy-only变化只 stale affected review state，不重建asset/render。render state、output hash、timeline、graph或policy identity变化会使旧review/final acceptance失效；Composition、ResolvedTimeline、renderer source与render继续只由同一次final render activation原子fresh。
- P7 `ImageGenerationRequest` 是 immutable/self-sealing contract，显式绑定 target Shot/role、prompt/seed/dimensions、exact Character/Scene/reference identities 与 base project/registry/graph。`ImageAssetProvider` 保持 provider-neutral；P7.1 增加一个 concrete loopback-only `ComfyLocalImageProvider`，但没有 remote adapter 或 fallback。
- P7 request、preview、authorization、submit intent、provider result、measured PNG 和 provenance evidence 都在 provider boundary 前后 durable seal/reopen。成功路径只创建一个 target Shot revision、append 一个 GENERATED/PNG Registry record，并由 `ProductionStateCommitter` 在同一个 final Manifest 2.5 replace 中切换 exact project/registry/graph tuple；P6 state 继续组合保留。
- 两个 `static_image` Shot 可复用同一 Character/Scene registered references，同时各自得到不同 PNG 和 exact provenance。Output activation 复用既有 P5 resolver，只 stale target Shot visual projection 与精确 downstream closure；无关 Shot、audio、voice/caption nodes保持fresh。P7不承诺reference bytes变化自动regenerate，必须由Codex提交新的exact request。
- Exact replay 的 provider/materializer/Manifest write 为零。Explicit recovery 只接受 exact old/new project/registry/graph tuple，不 remint permit、不 blind resubmit、不猜测 mixed activation；bounded incomplete temp可清理，完整未选中image/receipt/snapshot只preserve/report。
- P7.1 local profile sealed exact model components、official-workflow derivative、binding、license、ComfyUI commit、sampler/scheduler/steps/guidance 与 loopback endpoints。Non-replay generation 在 R+1 前强制验证 component size/SHA-256、checkout commit、registered `/object_info` nodes 与 workflow semantics；任何 mismatch 不写 Manifest、不上传 reference、不提交 prompt。
- 2026-08-18 首次 M7 授权 session 因 smoke reference fixture 不是可解码 PNG 而在唯一一次 Qwen submit 后失败，committer 标记 `outcome_unknown` 并 recovery 到 Manifest revision 6，没有 FLUX submit；第二次 session 已证明 Qwen，但 FLUX 被 `ImageScaleToTotalPixels` 因缺 `resolution_steps` 拒绝；最终 fix 在 live required-input preflight 与 `resolution_steps=1` 之后，同一日 fresh authorized rerun 在 loopback `http://127.0.0.1:8188` 上同时通过 Qwen 与 FLUX technical live acceptance：Qwen one first call / replay 加零，elapsed 185501 ms，1456x720 RGB PNG 1209630 bytes SHA-256 `62218606af56ac7d0651c437245d9db386f48f44194f28cdc9b4bd8c94ad40f7`，profile `local-image-profile:sha256:3871ae162aabe70ff3217ea6a9dbc4e83194b70a1d00e2ca249da202d4d8c6df`，workflow SHA-256 `34f1d2a67d049b646eef1c8f0c51aa8c1ad6b9eb5ef176c9ec422c3ff384a85f`；FLUX one first call / replay 加零，elapsed 19201 ms，1456x720 RGB PNG 1190375 bytes SHA-256 `303ea92b23a3820047aa5412d8d7b376ffb532d4e028aa7627b9fe19279e8180`，profile `local-image-profile:sha256:5066c080d58d3d999446ca5db195e8e81fd982052d94144452737f1e89a0f9c7`，workflow SHA-256 `e44a10fc8d6e6e491055de361d50dacaffb42adeb915f7c3596250a38b66dd22`；endpoint 文字为 loopback、`remote_used=false`、`browser_used=false`，无 secret、API key 或 quota。整个 session 没有 blind retry；M7 现在是 technical live-accepted milestone。
- P7.1 human import 使用 `chatgpt_images_2_web`、human actor、ordered offset timestamps 与 exact PNG evidence，不声称 backend model、provider request、durable submit 或 browser automation。Character、Scene、key Shot 和 repair replacement 都复用既有 project/Registry/P5 graph atomic commit；不存在第二 writer。
- Ecommerce product-interaction source preparation已在current local change实现offline structural slice：sealed `AdCreativePlan /2`纯投影商业Shot责任，`ProductReferenceSet`绑定active Registry exact image bytes；V1只支持registered local/human-observed interaction-keyframe import，`P7_GENERATION`没有caller callback或fallback并固定typed BLOCKED。Commercial source review复用active P6 `QaPolicy`：injected authorizer选择exact actor，唯一`ProductionStateCommitter`先持久化content-addressed intent，再durable consume并签发process-local one-use permit；analyzer不能自行选择durable PASS或写Manifest。Manifest 2.12保存request/candidate/review/approval lifecycle，普通project reopen、explicit recovery与每个exact replay都会经standard loader重验caller request/candidate、intent/evidence/receipt/approval、Registry/import PNG/QA policy/dependency graph、Shot、Character与Scene lineage；exact replay不推进Manifest revision。Approved source经current Planner生成provider-neutral `/3` product-fidelity requirement，Router在capability selection前强制同一first-frame I2V lineage，T2V、generic reference、wrong/stale approval均typed BLOCKED；neutral compiler只绑定approved keyframe，额外product references不伪装成Provider input。Commercial dependency helper建立`ProductReferenceSet/product assets -> keyframe -> generated Shot`精确closure，P5仍是唯一resolver且repair classifier不自动执行。Qingyan Shot 03/04 fake/import path只有offline tests；本slice未调用Provider、network、credential、media generation、renderer或paid effect，不证明真实商品/人物/动作质量、generated-video P6 PASS或Final Acceptance。Exact immutable commit-range receipt为`.agent/harness/runs/ecommerce-product-interaction-source-preparation-final-20260825/receipt.json`。
- P8 provider-neutral core以sealed request/capability/profile、Paid Provider Gate和stepwise submit/poll/fetch表示generated-video candidate；`VideoFlexibleOutputRequirement`覆盖exact/nominal/provider-selected/frame timing、adaptive dimensions和MP4/MOV，legacy request/resolved hashes由回归测试固定。`exact_seconds`继续要求exact frame/duration pair；`nominal_seconds`只接受exact pair或按declared FPS多一个inclusive endpoint frame及其对应不足一帧的duration，不产生exact timeline duration。Manifest 2.7 / Registry 2.2、Fake、candidate activation与explicit recovery均已offline accepted；fetched candidate本身仍不是Registry/Project/Graph activation。
- QualityExperienceRecord v1 minimum Q0 runtime现由top-level `ai_video.quality_intelligence`拥有：prospective success/known-failure/outcome-unknown records与typed historical imports使用strict/frozen complete exact-binding schemas和two-tier content/file hashes；独立`store.lock`覆盖record scan、attempt conflict、immutable promotion与reopen。Pilot cohort从strict-read base Manifest bytes建立，terminal roster先验证ordered attempt-ID prefix再strict-reopen exact video request；store write/reopen均要求4–8 distinct-Shot index的record `AttemptIdentityKey`集合与roster exact相等并重验owner pointers、terminal observation与copied projection。Exact lookup不依赖Agent Memory；sanitized RAG projection验证dataset content seal，只返回`authority = advisory_experience` bytes，不写docs也不build/search index。该slice没有运行Provider、renderer、analyzer、recovery、activation或Pilot，不证明自动学习/选参/Provider选择、P6 quality acceptance或Final Acceptance。
- New-attempt planning使用`video-planner/3`：typed neutral intent、exact Shot/Scene/Character/asset/Review evidence与continuity只由`VideoPlanner`派生为plan内嵌`ProviderNeutralVideoRequirement`，Planning freshness boundary再签发cycle-neutral verified projection。Asset-free pre-generation `generated_video` Shot现在可用唯一empty `VIDEO` target role strict reopen；该pending role不产生Registry asset，existing candidate/committer仍是把exact generated MP4绑定并激活到该role的唯一owner。Explicit `GenerationOperation.TEXT_TO_VIDEO`只允许无media reference且continuity为none/semantic的typed request，Character/Scene semantic context不冒充asset evidence。Router独占Provider/profile/capability selection并生成prompt-free `ProviderBoundVideoRequest`；Fake、Local/cloud H3、Hailuo与Seedance compiler只将exact selected capability和sealed requirement确定性投影为native prompt/payload。无法完整表达时返回typed unsupported，不重创作Shot、不降级requirement、不选择替代Provider。New request/resolved/activation lineage分别为`/5`、`/6`、`/4`；historical planner `/2`与video request/resolved/activation hashes保持兼容，但planner `/2`不得进入new attempt boundary。
- Stock20 `ComfyUIVideoProvider` 的 camera-complete requirement `/4` 现同时接受 sealed `I2VA` first-only 与 `FL2VA` first/last conditioning，均保持 compiler version `2`、唯一 `_h3_prompt.py` native grammar、exact quality-profile identity 与 existing binding validation。I2VA 不生成或注入 last-frame binding；缺失/不合法 conditioning、profile mismatch、legacy profile 与 stale intent 仍 typed fail closed。此为既有 Stock20 capability 的编译支持补齐，不改变 Router selection、workflow/profile bytes、sampler/steps、Manifest 或 activation owner；offline tests 不证明本场景媒体质量。
- Local T8 Quality v1与hybrid Turbo v1的T2VA compiler current truth为version `3`：完整historical requirement `/1`和camera-complete `/4`均经唯一`_h3_prompt.py`投影为`h3-three-field-v1`字符串，workflow不再接收`generation_mode=...` neutral key-value或嵌套JSON字段。`/1`只表达current-Shot action、performance、motion、camera、lighting与audio；global Story/Scene bookkeeping、future beats、identity IDs和abstract objectives不得泄漏为Provider text。无对白intent只发射authored ambience/physical foley；任何待发射字段中的`speech`、`dialogue`、`narration`、`voice(s)`或H3 dialogue tag均返回exact field-path typed unsupported。dialogue language由可选canonical BCP-47 core字段向后兼容读取，extension/private-use不进入该contract，但H3 `/1`执行前必须sealed；当前仅支持`zh-* -> Chinese`与`en-* -> English`的确定性native tag映射，缺失或其他language typed unsupported，禁止从字符集猜测。`/4` T2VA必须不携带conditioning evidence；I2VA/FL2VA仍必须exact conditioning。旧compiler version `1`/`2`、不完整intent、非T2VA historical requirement或无法安全表达的legacy camera movement均typed unsupported且不产生Provider effect。历史request与媒体evidence保持原字节，不由本变更重写或升级。
- `ShotReadinessGate`现已作为pure、deterministic、provider-neutral的pre-submit structural owner落地：Planning-private verifier签发或拒绝current v3 request/plan/embedded requirement exact projection，Gate固定输出binding、eligibility与required-asset三项typed diagnostics并独占`READY/BLOCKED`。`require_current_video_plan()` compatibility façade只委托Gate；READY只交付`VerifiedGenerationRequirementProjection`，BLOCKED保持`PLANNING_PREFLIGHT_BLOCKED`与zero handoff/downstream calls。Historical planner `/2`仍可reopen但new attempt typed STOP；`plan_hint`没有恢复。该offline acceptance不证明Provider submit、media generation/activation、subjective quality、P6 Review或Final Acceptance。
- 该requirement slice的acceptance全部为pure/fake/offline/no-network：未读取credential，未启动ComfyUI或Provider，未提交、poll、fetch或生成媒体，也未调用creative runtime Skill。Candidate 1仅保留为未来strict-loopback Local H3 regression oracle；按`AGENTS.md`其local ComfyUI execution无需user authorization，但仍须独立exact request、Provider与media gates。本slice没有复制其英文prompt，也不拥有Shot Quality Gate、Review或Final Acceptance。
- Shot Continuity technical core以向后兼容的`ContinuityConstraintSet`、`TerminalFrameEvidence`与`ContinuityReferenceBinding`扩展request/resolved identity；Manifest 2.8由同一个`ProductionStateCommitter`保存terminal extraction/evidence pointer并原子激活generated MP4与derived terminal PNG。Evidence绑定exact source Shot/candidate/generation/Registry/provenance，replay不重复fetch/extract/activation，tamper/unreadable在reader/recovery阶段fail closed。P5只为显式terminal-image input建立typed asset-to-asset generation edge，不按Shot顺序blanket stale；P3/P4 timing/audio/caption/mux与HyperFrames owner未改变。
- Structured continuity constraints当前进入request fingerprint、P5 invalidation与review lineage；Hailuo/Seedance offline mapping消费exact first-frame bytes和既有prompt字段，但Provider没有原生scene/camera/motion/lighting结构化参数映射。该边界必须在live proof和subjective review中单独验收，不能由offline payload tests外推。
- `MiniMax-Hailuo-2.3` I2V first-frame mapping和`doubao-seedance-2-0-mini-260615` continuity first-frame/audio-opt-out payload已完成offline executable acceptance；前者新增的representative live truth与后者的pre-submit blocker在下文分别记录。Local MiniMax H3 `fl2va`已有official-derived deterministic API workflow、binding与profile，seal upstream、ComfyUI commit、四组件、native nodes、loopback endpoint与output bounds；adapter在R+1前fail closed且无remote fallback。Provider-neutral compiled request保持未指定creative seed的`seed=None`；Local H3 adapter在resolve时把exact `request_input_hash`机械映射为稳定的non-negative 63-bit `effective_seed`，显式seed仍原样保留，且resolved identity记录最终值，从而满足ComfyUI `RandomNoise.noise_seed`的required `INT`输入而不把该default扩散到其他Provider。
- 2026-08-21 separately authorized T10在`runs/c2-alice-local-h3-t10-regression-20260821-001/`完成exactly one loopback Local H3 submit。Current Planner `/3` proposed，`ShotReadinessGate` returned `READY`，Router selected `minimax-h3-fl2va-local-v1`，compiler保持`seed=None`，resolved `/6`从request hash确定性派生`effective_seed=7574281774261031181`；first frame精确绑定activated Shot 1 terminal SHA-256 `52596511bbea4314e39f0065559f6c01665303c4139a6c28326a8ee90b57e19b`。唯一submit/poll/fetch为`1/1/1`，remote call为零，candidate activation/reopen成功，exact replay全部Provider effect delta为零，explicit recovery保持Manifest revision 34不变。输出`alice-c2-local-h3-t10-regression-1.mp4` SHA-256 `d599093f2eb52acfde3884fe5a392fb593c04564407c011e26ddab87e8eef067`，H.264 High `1344x672@24fps`、124 frames、5.167s并含32 kHz stereo AAC；project-local `video-analysis`确认single scene、167/167 sampled frames unique与无structural issue。用户查看该exact artifact后给出“效果很好”的明确human `GO`。该结果不是Candidate 2/3，只证明本次exact technical chain并接受本次artifact；不得外推为通用identity/motion/blur质量结论、P6 Review或Final Acceptance。
- 2026-08-19 bounded Local H3 proof在`runs/h3-shot-continuity-live-20260819-v3/`完成两次loopback submit、零remote call。Shot A terminal PNG SHA-256 `cb5bbc17ad361b4b8445657608e245c179446efb68a5976b4df09eb0ecbaf42c`与Shot B `first_frame` binding一致；两段608x352、24fps、124-frame H.264/AAC MP4均完成fetch、measured validation、activation、reopen，replay新增调用为零。`video-analysis`确认单scene/非重复帧，同时指出分辨率低于quality review建议；因此仅为technical live-local accepted，blinded subjective quality仍pending。
- C2 editorial hard-cut technical contract现已区分于上述C1 direct terminal-first-frame path。P7新增strict `ContinuityTerminalImageReferenceBinding`与additive Qwen hard-cut profile，以`character + continuity_terminal`生成独立keyframe；P8新增`HardCutKeyframeBinding`并在submit intent前验证active Registry中的source video、terminal、P7 keyframe request/provenance与target Shot；P5显式投影`terminal -> generated keyframe -> generated video` typed closure。新field缺失时历史P7/P8/C1 fingerprints与reopen语义不变。2026-08-20 Alice咖啡厅representative prerequisite已用一次local H3 Shot 1和一次local Qwen hard-cut keyframe生成并共同激活：terminal SHA-256 `52596511bbea4314e39f0065559f6c01665303c4139a6c28326a8ee90b57e19b`，独立keyframe SHA-256 `5072a65b0e13a7fae2b809d3dc0afb24dc6980c75b0ce9342559b60801c158fc`，P5变化只落在该keyframe asset与Shot 2 visual projection。该prerequisite通过人工anchor检查，但Local H3 Shot 2与完整C2 local proof仍未执行。
- 同一2026-08-20授权task的首个`MiniMax-Hailuo-2.3`请求只消费上述activated keyframe作为`first_frame_image`并完成唯一一次remote POST、9次poll、一次fetch与measured/visual validation。真实MP4 SHA-256 `58e3a09658134a18cc184f4fb536834c6139a6ff9d55985b76fb5b7af663194d`，H.264 `1326x768`、`24fps`、`141` frames、`5.875s`、无原生音轨。该live result证明Hailuo I2V的`768P`几何随accepted first-frame aspect自适应，而非固定`1366x768 / 16:9`；adapter的新resolve/preview只签发adaptive geometry，frame/fps/container/audio contract保持不变，尚未submit的历史fixed-geometry request会fail closed。该首个request的zero-network corrected-contract migration复制了external-effect receipt，因此不计canonical activation。
- 用户随后以“全部授权继续”批准新的bounded adaptive Hailuo request；该独立request恰好消费一次POST，task `432581080539416`经8次poll succeeded、一次fetch，并由原生lifecycle完成candidate activation、project reopen、explicit recovery与exact replay。MP4 SHA-256 `90b4fc1842b4a74332ebcae7e20c1e0cc1cdd7166037702e4dd1b37d2ed0bdca`，H.264 `1326x768@24fps`、141 frames、5.875s、无音轨；P5 changed closure仅为generated video、derived terminal与Shot 2 visual。Replay新增submit/fetch/activation为`0/0/0`。Project-local `video-analysis`确认单scene、6个抽帧、无音轨；7个定点帧hash全异，black/freeze检测为零，首帧与resize后shared keyframe SSIM为`0.941854`。人工review判定`pass with minor concerns`，concern仅为轻微facial variation。
- `doubao-seedance-2-0-mini-260615`以`nominal_seconds`表达Provider API的整数seconds请求，同时保留audio-opt-out request sealing与fresh numeric preview；Mini当前official capability不再宣称`exact_seconds`。2026-08-29一次授权的15s T2V返回361帧、15.042s，验证了24fps inclusive endpoint行为；原attempt仍因当时sealed exact contract及Shot identity Gate失败而保持未激活，不能由新contract追认。2026-08-20新增的`SeedanceAssetMaterializationReceipt`/`SeedanceAssetReferenceResolver`继续独占already-materialized Ark identity import：只接受Ark Console人工观察的`Active asset-...`并精确绑定local bytes。2026-08-29官方R2V调查确认普通`reference_video`也可使用Provider可访问HTTPS URL；current runtime因此新增provider-output vertical slice：Seedance fetch把task/model、opaque file identity、URL hash/origin与exact downloaded MP4 hash/bytes/MIME封入`RemoteMediaMaterializationReceipt`，完整signed URL只进入最长5分钟的in-memory `SeedanceRemoteReferenceLease`。Canonical `VideoGenerationService.refresh_remote_reference_lease_once()`只接受Manifest current exact-active remote source，重开exact submit/status/fetch evidence并签发process-local one-use refresh permit；当source request本身使用`nominal_seconds`时，该permit还把exact reopened nominal milliseconds封入lease。Seedance helper消费该permit后重新query同一locator、完整GET复核同一bytes，lease每次resolve还会重验source仍current-active，才可驱动Mini `VIDEO_EXTEND`/R2V。对Mini进入2.0的15s family limit，remote R2V binding必须精确使用这一canonical lease证明的nominal 15.000s；物理15.042s measurement继续由active artifact/probe保留，不能被binding覆盖或改写，且binding仍精确绑定同一SHA/bytes/materialization。普通Ark/local/external reference与audio仍严格按15.000s total limit fail closed。stale source、另行mint、lease过期、locator轮换、bytes变化、缺少或不匹配nominal proof均typed stop且不fallback。该slice仍未实现local H3/T8 object-storage uploader；Alice keyframe的Ark Asset lane仍无materialization/receipt。
- 2026-08-29/30授权的`runs/seedance-mini-r2v-epic-skyship-20260829-002/`关闭了上述Mini provider-output vertical slice的首次live chain。Shot 1 exact-active artifact `aff994a4ebefb9a5fd3d65d824be4f4f290cae0272ff94fe6d4765aa3338afd8`由source request的sealed nominal `15.000s`与artifact/probe的physical `15.042s`分层表示；refresh重新query相同locator并完整GET验证同一bytes后，Shot 2唯一一次POST以`VIDEO_EXTEND`直接发送该HTTPS `reference_video`，未走Ark Asset、I2V、fallback或retry。Shot 2 artifact `e33f18bc09c178703af5abfd534982d2c15ce5c48a2135dc7f4be3101eb68029`为H.264 High `1280x720@24fps`、361 frames、15.042s、无音轨；project-local `video-analysis`与Agent Per-Shot Gate对technical output、source identity、motion continuity、cinematic scale和forbidden elements全部PASS，Shot 1尾帧/Shot 2首帧SSIM为`0.903748`，两attempt均settled并activation。该单次live proof不外推其他Seedance模型、P6、Final Acceptance、publication或Provider-wide质量。
- 2026-08-28基于Qingyan exact live rejection evidence，Seedance inline synthetic path已退役`synthetic_photorealistic_person` remote egress：该classification仍可strict reopen历史receipt，但`SeedanceSyntheticImageAuthorizer`与`SeedanceSyntheticImageReferenceResolver.validate_submit()`都会在Ark POST和one-use permit消费前返回`PAID_PROVIDER_EGRESS_NOT_AUTHORIZED`。这只保证known-risk bytes不再到达Ark classifier；它不伪造Provider接受、不自动动漫化、不选择fallback Provider，也不影响clearly illustrated或ordinary non-character synthetic inputs。
- MiniMax cloud H3/Hailuo adapters均已offline accepted且保持显式、可删除、非默认。Hailuo已有多条durable provider-succeeded/fetched MP4 evidence，本次Alice C2 adaptive request进一步完成canonical activation/recovery/replay。MiniMax cloud H3经授权的live尝试到达真实API，但余额不足返回`HTTP 402 / code 1008`并记录known-no-effect，因此该cloud adapter没有live-success artifact，也没有blind retry。
- 2026-08-21 `comfy-local-h3-t8` local Provider family以additive capability contract进入Shot Router：既有`minimax-h3-t8-t2va-quality-v1`仍为无LoRA、20-step `res_multistep` lane；新增`minimax-h3-t8-t2va-turbo-v1` seal Larryvrh Turbo plugin commit `4274783a23afcfdbea3b4876cb79effd6c510785`、version `1.2.3`与v4 LoRA SHA-256 `5f3a626cd72c93a8b9318d6760c510bc5092d2ab13aaba1f932c5bab07a416d3`，固定6 steps、`simple`、`1344x768`、124 frames、24fps、native audio与CRF 17。Family按exact identity委托compile/resolve/preview/preflight/submit/status/fetch；`VideoGenerationService`在status/fetch时把committer重开的resolved request与durable submission共同传入，family先验证request/submission/observation seals，因此fresh family无需last-selected或process-local cache即可路由回同一child。Pure Registry regression证明Local H3、Seedance、Hailuo、MiniMax H3等distinct-name objects可exact coexist，duplicate name fail closed且lookup不调用Provider。两条lane仍各自实现具体local action并复用唯一committer lifecycle，Router denial不fallback。该slice仅有offline source/test acceptance，未执行Turbo媒体生成、主观质量验收或P6 Final Acceptance；concrete product caller仍必须显式完成Registry lookup与Service injection。
- 2026-08-22 T8-native Turbo V2完成四份distinct T2VA/I2VA/FL2VA/Ref2VA template/binding/profile及单一cohesive local adapter，固定non-pruned mode-compatible base、official-source/pinned-converter/exact-header-audited LoRA、唯一`MiniMaxH3DualClockSamplerT8`、4 steps、`dual_clock_euler/native_flow`、`1344x768`、124 frames、24fps、native audio、CRF 17与final `*-audio.mp4`；Ref2VA只按canonical order映射`ref_images.ref_image_N`、`ref_videos.ref_video_N`与`ref_audios.ref_audio_N` T8/ComfyUI autogrow keys，`ref_video_audios`保持未连接。Shared Gate 0A seal `DARK-MING/MiniMax-H3-Turbo-Lora@dff52016c06373336893f94e64b6dfea9a4d2db0` source SHA-256 `8d645b67e606874e9179b277cea721c1f1e75830532fcc2206e23353cb33edc5`、pinned T8 converter与installed converted SHA-256 `5b8ad6cb7ac206852006f4efa3ce2d679cd6ffb5d5b8a4edce8e981393289df5`，preflight验证exact metadata和518 BF16 tensor key/dtype/shape。Gate 0B seal `Comfy-Org/MiniMax-H3@0f7fb980293fcc4d55c1158cbda920806682ed5d` non-pruned Ref2VA base `34038894550` bytes、SHA-256 `9eef934046a0671bc8a5daf87100705e1478419c574cfde70c50fbe6885f76a9`。T2VA/I2VA/FL2VA/Ref2VA fresh profile hashes依次为`ff374a645eb15112b4c9d4341d1c61bcb80f32bdc55f7325cfe1c8153db5d6bf`、`f73fe4a9adef3275ba4b6c9b3bd0307bed39a40542f80747867f2cb46df3e667`、`5fbb4b367eec6854c23d98600d96e366053da07e7635a9e920a6d856172b7a4f`、`0bb8ebf9688369f89d959d8273a81f73dcc6e02e96c1a20a52110583ace7db9d`；四条loopback technical outputs SHA-256依次为`43c6133f240af118598a52f8348e1558d41eec4134d7b648ca3e09ff33c27000`、`090082556e24bb3309ba01c55eaafabff622277cf1a903b305cec5d0767844ef`、`fcc4125e00ffc9a97319bc7e4478dd933b12f0c7e127d6d4b769725fea01d6d5`、`3f068b69c122be33e7a0c2641dc2de6e1d9fbb2aa2ade0a70e8152868cf22145`，均为H.264 High `1344x768@24fps`、124 unique decoded frames、5.167s、AAC、`1/1/1` effect与exact replay safe；wall time依次`124.531/119.068/123.274/155.391s`，peak GPU依次`19932/26236/24828/26797 MiB`，peak ComfyUI RSS依次`57988484/57960876/58056384/58233732 KiB`。Ref2VA首次submit在conditioning以known terminal failure暴露裸`ref_image_1`错误；official autogrow key修复后新attempt通过，无unknown outcome或blind retry。四条profile现均`live-ready`，six-child family snapshot固定为`d3b8e5cc31570763aae6f7454ca794737634c345ec3ea6bbbcaadc36196381dd`，既有Quality v1、Turbo v1及two-child fingerprint不变。Same-request T2VA对比中V2比Turbo v1快`18.2%`、比Quality v1快`65.6%`，但独立review指出V2 motion/detail较弱且约`-52.5 LUFS` audio可能功能性不可闻；所以V2只作为additive speed-first/experimental lane，不是主观quality、default promotion、v1 retirement、P6 Final Acceptance、push或release证据。
- Bounded hybrid continuity evaluator v1现有local/no-network executable adapter：absolute held-FD `ffmpeg` sampler产生exact RGB frame hashes；既有`continuity-visual-profile/1`保留CPU/offline contract，新增`continuity-visual-profile/2`显式绑定YOLOX-S release ONNX、由author OSNet x1.0 MSMT17 checkpoint在fixed revision导出的ONNX、各自preprocess/output/decode/NMS contract、exact NumPy/`onnxruntime-gpu`与CUDA 12.8/cuDNN 9 NVIDIA library identities、CUDA provider options与CPU-fallback denial。GPU runtime隔离于repo外的`continuity-gpu-venv`，主环境仍保留既有CPU ORT；2026-08-22本机RTX 5090技术smoke已验证两份sealed model的全部profiled nodes运行于`CUDAExecutionProvider`，detector输出`[1,8400,85]`、ReID输出`[1,512]`并生成finite unit embedding；model bytes同样位于repo外的local immutable store，runtime不自动下载。新增`local_continuity_reviewer.py`只提供显式factory：调用方必须提供absolute model/ffmpeg paths、sealed sampler/evaluator identities与非空bound human fallback，factory不读取环境变量、不下载model、不注册default，也不修改candidate-validation seam。Deterministic single-subject track只在coverage/confidence/identity stability满足profile时测量signed motion direction、absent-to-present entrance、present-to-absent exit与same-track unexpected re-entry；低coverage、ambiguous track、unsupported grammar全部为`not_evaluated`。由于generated-character thresholds尚未校准，profile `/2`固定`human-confirmation-required`：automatic mismatch可以fail closed，automatic match必须降为`not_evaluated`并由exact bound human fallback确认后才可进入P6 PASS。identity、camera axis、framing仍没有可信semantic backend，同样必须human fallback；model/evaluator不含verdict，PASS/FAIL/NOT_EVALUATED继续由P6派生。该technical smoke未使用真实Shot媒体，也不构成媒体quality acceptance或Final Acceptance。
- 2026-08-22 continuity-bound generated Shot activation现在以Manifest 2.10 additive capture checkpoint扩展既有2.9 evaluator checkpoint：唯一`ProductionStateCommitter`先保存exact request/policy/artifact/constraints/evaluator/profile-bound `ContinuityEvaluationIntent`，运行一次evaluator，并在pure adjudication前把content-addressed `GeneratedShotContinuityEvidence`、`VideoProbeReceipt`与`VideoProvenanceReceipt`一并选入`EVIDENCED` state。PASS继续candidate/activation；automatic或human FAIL保持fetched/not-candidate但拥有对称durable evidence；retry/recovery只strict reopen、rehash和重新adjudicate，不重复sampler、ONNX evaluator、human fallback、probe或Provider effect。Intent-only未知结果仍保持`VALIDATE`并返回`PRODUCTION_STATE_OUTCOME_UNKNOWN`。Manifest 2.9历史evidence-only state保持可读，但不得携带2.10 probe/provenance pointer；2.10 evidenced state缺任一pointer即fail closed。
- Q0 `capture_post_qa_quality_experience()`现提供显式、no-network post-QA capture seam：caller只提交exact project/attempt selector、non-Production dataset root与strict experiment/analyzer/optional human context，不能提交runtime projection；Q0 strict reopen Manifest 2.10或additive 2.11 attempt、fetch bytes、probe/provenance、continuity intent/evidence、Registry inputs、content-addressed `analysis/**` analyzer document、独立`reviews/**` human document与实际存在的P6 pointers，再调用pure `adjudicate_generated_shot_continuity()`。Record schema 1.1使用独立`continuity_evidence` binding并强制与record artifact/Shot/constraints一致；P6 observations不承载evaluator evidence，active receipts经过current freshness验证，累积历史repair outcomes固定`stale`；历史schema 1.0保持兼容，未持久化runtime lineage显式tagged `not_applicable`。PASS/FAIL固定投影`continuity_score=1.0/0.0`，`NOT_EVALUATED`、intent-only、tamper、incomplete/unreopenable analyzer与human metadata mismatch均zero-write fail closed。Exact replay复用同一`QualityRecordPointer`，different advisory bytes产生typed conflict；Production不import Q0，当前没有product-level automatic caller。该slice不覆盖non-continuity或Provider failure/outcome-unknown automatic capture，不运行Provider、媒体、analyzer、RAG rebuild或Pilot index closure。
- Agent-side Experience Learning不改变上述Q0边界：它可引用exact Q0 pointer、session record与eligible run summary进行cross-experiment synthesis，但不能伪造missing Q0 fields、写dataset/Manifest/P6或把development claim升级为Product selection。当前实现没有daemon、generic automatic patch engine或automatic Production adoption；用户确认后仍由normal Agent workflow执行exact previewed target change。
- `SeedanceVideoProvider`是显式、可删除的Ark adapter；strict dated profile完整覆盖七个official current/legacy-callable Model IDs并拒绝retired/marketing aliases、漏mode或能力扩张。Exact API Model/Endpoint target、expected query Model ID和container进入durable evidence；只接受injected `ARK_API_KEY` supplier，不读取或fallback到MiniMax credential。Ark asset import是独立local/no-network contract，不自动调用Assets API、不新增AK/SK或第二writer。
- 2026-08-20 新增独立`minimax_h3_fl2va_quality_local` profile；既有`minimax_h3_fl2va` profile/workflow/binding bytes与profile content hash `456b59c7a907d4b07c7d951d63ec03cbd0fb5c64638dbc8dad870aca09e2b604`保持不变。本次A/B固定`1344x672`、H.264 `re-encode`、CRF 17，并保持int8 diffusion、20 steps、`res_multistep`与`simple`不变；API workflow使用本机`SaveVideo` schema要求的flat dynamic inputs。相同插值Alice reference、相同prompts/seeds的two-Shot受控A/B将source视频码率从`559/798 kbps`提高到`2994/2958 kbps`；统一缩放到1024x512后，16帧Laplacian mean从`109.154`提高到`112.647`（+3.20%），Tenengrad mean从`9170.554`提高到`9701.114`（+5.79%），decoded boundary SSIM从`0.928515`提高到`0.936091`。这证明输出分辨率/编码是贡献因子，但不足以单独解释全部模糊。
- 同日使用现有loopback-only P7 Qwen Image Edit 2511 lane生成一次原生`1344x672` Alice reference，result terminal status为`succeeded`，PNG SHA-256 `99253a9dc5a705487a35b016e18e5fb5b693d78f444a959054c1fbce0c73ed6d`。P5 candidate scope validation在result materialize后拒绝activation并记录`outcome_unknown`；随后执行explicit recovery，未blind retry，并以exact result/receipt/request/profile provenance将该PNG诚实import为H3 reference，不宣称它是activated P7 candidate。最终two-Shot stream-copy成片位于`runs/h3-shot-router-quality-p7-20260820-v1/evidence/alice-cafe-exact-terminal.mp4`，SHA-256 `05c00691a7a04d8e0864f018073ea746ccba3e0cba38aa0c9f651180d30bae9e`，`1344x672`、H.264 High、yuv420p、248 frames、video bitrate `2653 kbps`；统一缩放后Laplacian mean为`244.187`（相对旧片+123.71%），Tenengrad mean为`11032.689`（+20.31%），decoded boundary SSIM为`0.937613`，terminal SHA与下一Shot first-frame input SHA均为`2dd12cac1381fd2cbc35067fc1d6f76b4e190135f5edc535172d94270f9ff27b`。Project-local`video-analysis`报告single scene、336/336 unique sampled frames、无heuristic issue。该证据接受本次Alice局部quality修复，不升级为通用P7 M8或H3 blinded benchmark acceptance。
- C2 editorial hard-cut technical contract现已区分于上述C1 direct terminal-first-frame path。P7新增strict `ContinuityTerminalImageReferenceBinding`与additive Qwen hard-cut profile，以`character + continuity_terminal`生成独立keyframe；P8新增`HardCutKeyframeBinding`并在submit intent前验证active Registry中的source video、terminal、P7 keyframe request/provenance与target Shot；P5显式投影`terminal -> generated keyframe -> generated video` typed closure。2026-08-20 Alice representative C2进行了两轮local/live subjective review，但人物姿态、构图与接缝流畅度仍未通过；technical lineage不等于quality acceptance。
- Shot Router第一阶段pure runtime提供no-I/O `ShotVisualResolver`与`VideoGenerationResolver`，消费Shot的`exact_terminal`、`reference`、`semantic`、`none`四档typed continuity contract。Provider routing是正交维度；selected exact Provider/profile capability必须满足Shot contract，不得改写或静默降档。Additive sequence-aware requirement routing复用exact `ContinuityTransitionPolicy`，并以hash-bound `ContinuityProviderRouteBinding`绑定previous `ProviderBoundVideoRequest`、source/destination `GenerationExecutionStackIdentity`及其exact Provider route：同一execution stack锁定上一Shot的Provider/model/profile/capability/compiler，alternate preselection（包括cost-driven selection）不得覆盖；cross-stack目标route必须与policy绑定的destination stack逐字段一致。只有policy显式声明cross-stack且continuity obligation已释放空间连续性，或`FULL_CONTINUITY`目标能力以previous exact terminal作为首帧时，才允许跨stack。`hard_cut`本身不释放连续性，`hard_cut + FULL_CONTINUITY`仍受约束，`IDENTITY_STYLE_CARRYOVER`与`SUBSTANTIAL_RESET`才允许相应重新路由。Transition policy、previous request与route binding hash共同进入routing semantic hash；只有`none` continuity的first-Shot、reset或独立Shot可省略sequence evidence，public `resolve()`会拒绝continuity-bearing context，continuity routing必须使用transition-aware `resolve_requirement()`。Readiness compatibility handoff在给定transition policy时保留其exact round-trip对象，避免在production callback边界丢失sequence evidence。Local H3 capability现精确公开sealed profile的width/height bounds与dimension multiple，Router不会再选择随后被Provider拒绝的geometry。既有capability grammar继续以I2V `first_frame`、`first_frame + last_frame`、binding cardinality/multi-anchor和R2V reference声明conditioning；reference consumption不等于空间/位置或camera/scene continuity保证。每个Router input asset绑定exact selected Registry revision；canonical角色/场景reference还绑定对应Character/Scene ID与content hash，projected request bindings保持唯一。Exact terminal像素只可绑定下一Shot `first_frame`；reference把terminal与canonical角色/场景作为reference并允许重新构图；semantic只seal character/story/wardrobe/injury/prop/scene state且排除terminal pixels；none不消费continuity输入。exact capability denial不fallback，policy与semantic hashes分离。Local H3 C2 derived-keyframe主观质量仍未验收，而Hailuo adaptive C2已有上文记录的canonical technical live与非盲人工review。该slice不物化Shot revision、不修改P5/state writer/Manifest/Registry/CLI，也不调用Provider。
- Seedance submit只在committer-issued one-use Paid Provider permit紧邻消费后发出一次POST；不自动重试POST，ambiguous outcome进入existing durable reconciliation。Poll/fetch绑定同一task/model，result URL禁redirect并受origin/byte ceiling/MIME/ISO-BMFF brand约束；默认tests使用fake transport，零网络、零费用。
- Seedance payload始终显式发送`generate_audio`，同时省略`watermark=false`、`return_last_frame=false`、`service_tier=default`、`output_format=mp4`和`priority=0`。一次Mini diagnostic以更小payload到达provider `succeeded`并fetch H.264 MP4，但因未发送`generate_audio`而产生AAC音轨；它只证明cloud connectivity，尚不证明tracked payload live acceptance或activation。
- Base AI Comic E2E 通过同一个 Manifest 2.5 / `ProductionStateCommitter` control path 串联 two-Shot P7 PNG、shared Character/Scene references、P4 fake voice/captions、P3 deterministic timeline/render、P6 strategy-aware review/repair/final acceptance。Manifest 2.5 在 voice、render、review、repair、recovery 与 replay 后保留全部 P7 evidence，不需要 schema migration、第二 writer 或 production coordinator。
- Approved composition repair 的 exact P5 closure 只有 `composition:main`、`timeline:main`、`renderer-source:main` 与 `render:main`；image、Character/Scene reference、voice 和 caption identities 保持不变。Fresh runtime 会 reopen exact Project/Registry/Graph/Render/Review/Repair evidence；exact replay 的 image/voice/analyzer/renderer calls 和 Manifest writes 全为零。

## Planned but Not Implemented

- P7.1 M8 blind quality benchmark、remote image generation 和 automatic upstream-reference regeneration。
- Ecommerce product interaction仍缺concrete product-aware Image Provider materializer的独立empirical qualification、generated-video product fidelity/temporal spray-action QA、calibrated automatic product/identity evaluator与真实Shot 04最小实验；以上都需要新的明确授权和独立acceptance gate。
- Local H3 C2 derived-keyframe Shot 2/live与subjective acceptance、未来editorial实现、Local H3 `ref2va` identity profile、blinded three-lane continuity quality benchmark，以及Seedance exact Ark asset materialization后的continuity cloud live proof。Hailuo 2.3 representative lane已有canonical technical live与非盲人工review；Shot Router runtime已实现，不再属于planned work。
- Seedance tracked audio-opt-out payload live acceptance、account Endpoint discovery、真实pricing/billing settlement与任何新的paid submit。
- Codex operating contract 的更高层自动编排；P6 repository runtime只提供durable Python contracts，不新增通用Agent runtime。
- Take、Reference v2、Human Review 和 Semantic Evaluation。
- 通用跨 Provider cost ledger、默认/live ElevenLabs operation、Captions.ai、Vidu、Kling 或其它未验收付费/远程 integration。

## Remaining Runtime Defects and Debt

- Succeeded resume fast path 只检查 Shot clip/frame 与 direct chain dependency，不检查 normalized clips 或 `final.mp4`。删除或损坏 final output 后，resume 仍可能直接 no-op。
- `project_config_hash`、`workflow_template_hash` 和 `workflow_binding_hash` 已记录但 resume 不比较。Prompt、explicit init image、character reference、config 或 workflow 变化仍可能被成功 fast path 跳过。
- Attempt 的 `running` state 和 Comfy prompt/job ID 没有在 submit 前后 crash-safe 持久化。进程或主机中断可能丢失 in-flight provenance，并留下 blind resubmit 风险。
- Legacy final output 没有 content hash；Legacy run multi-file artifact transaction 没有 journal/fsync 级 power-loss recovery。
- `CharacterProfile.reference_images` 接受多张图片，但当前 pipeline 只消费第一张；`future_lora` 仍只是 config placeholder。
- Whisper MCP 的 `word_timestamps=True` 会传给模型，但返回 schema 丢弃 per-word records，cache key 也没有包含该 flag；它不能直接作为新 Caption domain 的 word-timing owner。
- Legacy heuristic review仍可能把低帧差或少 scene标为 `static_visuals`；P6 Production path已与其隔离，并以verified Shot strategy和timeline windows判定，Legacy提示不得作为Production acceptance evidence。
- Legacy FPS 行为已经被测试锁定，但一个 `defaults.fps` 字段仍同时表达 generation fallback 和 delivery FPS；新 domain 需要分开建模，不能回写改变 Legacy semantics。

## Retained Legacy Design

- Manifest-first provenance、hash、resume 和 atomic persistence 思想继续保留。
- Local Wan + ComfyUI 继续作为 optional `generated_video` asset capability；不删除、不降级现有 CLI。
- Workflow loader、renderer 和 Comfy client 的现有 owner boundary 继续保留；未来 adapter 只能组合它们，不能复制。
- `_ArtifactPromotion` 的 staging/rollback 设计继续用于 Legacy runtime；P2A 已由独立 `ProductionStateCommitter` owner 提供 v2 project-state commit/recovery boundary，未改变 Legacy artifact transaction。

## Superseded Product Assumptions

- `Shot = prompt to video model` 不再是 v0.2 顶层 domain。
- Provider abstraction、Take 和 Seedance 不再是新 v0.2 的第一条主线。
- “所有 Shot 都生成视频”不属于目标；基础 AI 漫剧必须在没有 Video Provider 时完成。
- 旧 spec 的 P0-P9 ordering 已被新 roadmap 取代；其 paid-provider safety contracts 仍可复用。

## Evidence Commands

```bash
git status --short --branch
git log --oneline -20
python -m pytest -q
python -m pytest \
  tests/test_production_audio.py \
  tests/test_production_captions.py \
  tests/test_production_elevenlabs.py \
  tests/test_production_models.py \
  tests/test_production_validation.py \
  tests/test_production_registry.py \
  tests/test_production_project.py \
  tests/test_production_composition.py \
  tests/test_production_hyperframes.py \
  tests/test_production_state_commit.py \
  tests/test_production_state_recovery.py \
  tests/test_production_voice_captions_e2e.py -q
python -m pytest \
  tests/test_production_models.py \
  tests/test_production_validation.py \
  tests/test_production_registry.py \
  tests/test_production_project.py \
  tests/test_config.py tests/test_cli.py -q
rg -n "_persist_terminal_failure|_shot_is_current|_all_resume_shots_current|_ArtifactPromotion|delivery_fps" src/ai_video/pipeline.py
rg -n "successful_shot_is_valid|chain_input_hash|character_ref_hashes" src/ai_video/manifest.py src/ai_video/pipeline.py
rg -n '"-an"|normalize_clip|stitch_clips' src/ai_video/ffmpeg_tools.py
rg -n "word_timestamps|segments" src/ai_video_mcp/tools/transcribe.py
rg -n "ProductionStateCommitter|VoiceAssetProvider|CaptionTrack|render_with_hyperframes|ResolvedTimeline|active_render_state" src/ai_video/production
rg -n "DependencyGraphSnapshot|resolve_dependency_state|select_rebuild_nodes|active_dependency_graph|dependency_states" src/ai_video/production
python -m pytest -p no:cacheprovider tests/test_mcp_review.py tests/test_mcp_optimize_plan.py tests/test_mcp_apply_optimization.py tests/test_production_models.py tests/test_production_review.py tests/test_production_repair.py tests/test_production_project.py tests/test_production_state_commit.py tests/test_production_state_recovery.py -q
python -m pytest -p no:cacheprovider tests/test_production_image.py tests/test_production_image_e2e.py tests/test_production_models.py tests/test_production_registry.py tests/test_production_validation.py tests/test_production_project.py tests/test_production_dependency.py tests/test_production_selective_rebuild.py tests/test_production_state_commit.py tests/test_production_state_recovery.py tests/test_production_review.py tests/test_production_repair.py -q
```

P4 merged-main full result: `1094 passed, 4 skipped`。P5 final results: canonical `1256 passed, 3 skipped`、Legacy `58 passed`、full `1386 passed, 4 skipped`。P6 focused final result为 `739 passed`；独立 review verdict为 `accept with concerns`，唯一非阻塞 concern是未来可补一条完全不 monkeypatch 的 activated-render snapshot reopen E2E。合入并行 local-main commits后，在隔离 worktree物化同一 lock-owned HyperFrames package仅供路径验证，full suite为 `1462 passed, 4 skipped`。P7 branch acceptance为canonical `1128 passed`、image E2E `7 passed`、full `1686 passed, 4 skipped`与Architecture Gate PASS。Base AI Comic E2E Task 8 focused acceptance为 `980 passed, 3 skipped`，full suite为 `1714 passed, 4 skipped`，Architecture Gate PASS；independent architecture review verdict为 `accept`、无 blocking issue。P7/Base E2E验收未调用network、ComfyUI、live HyperFrames executable、video Provider、remote image Provider、secret或quota。

P8 activation/recovery closure由`a089eac`提交。Fresh staged receipt `.agent/harness/runs/p8-video-activation-20260819-v6/receipt.json`与commit-range receipt `.agent/harness/runs/p8-video-activation-20260819-range-v1/receipt.json`均通过self-verification；完整P8 provider policy为`742 passed`，production contracts为`2036 passed, 3 skipped`，state为`933 passed`，dependency为`1045 passed`，Architecture Gate PASS。该验收只使用offline/fake transport与历史durable live evidence，没有新的Provider调用、secret或费用。

## Non-Authorization

本文分别记录 `origin/main` 与 local `main` runtime truth，不授权push/release、P7.1后续live smoke/M8 benchmark、H3/Seedance或其它Provider新live submit、new CLI、P9、cloud fallback或新runtime dependency。Seedance已有一次Mini diagnostic succeeded/fetched evidence，但tracked payload、billing settlement与activation仍未live accepted；H3没有live success。未来live仍需rotated key、exact account Model/Endpoint access、current pricing snapshot、finite one-call budget、egress authorization与明确的一次submit授权。既有Hailuo/Seedance evidence都不授权重复调用。
