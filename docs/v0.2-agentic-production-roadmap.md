# AI-VIDEO v0.2 Agentic Production Roadmap

Status: Master dependency map approved as product direction on 2026-08-09. This document is not implementation authorization.
Target Spec: `docs/superpowers/specs/2026-08-09-ai-video-agentic-production-harness-v0.2.md`

## Purpose

本 roadmap 只定义 phase dependency、gate、parallel boundary 和 slice output。每个 runtime phase 仍需单独的 implementation plan、明确授权、tests、rollback 和 acceptance。不得把本文件当作一次性执行计划。

Documentation Contract Gate只在Development Governance中验证本roadmap/spec metadata的owner路径与
static repository anchors；它不把roadmap升级为runtime truth，也不能判断任意叙述段落或architecture
论证是否仍正确。动态implementation/evidence状态继续由`docs/v0.2-runtime-baseline.md`拥有。

## Dependency Graph

```text
P0 Product Reframe and Contract Migration [docs complete]
 |
 +--> P1 Legacy Runtime Stabilization [implemented on local main]
 |      |
 |      +--> Integration Base Gate
 |             local main vs origin/main disposition must be explicit
 |
 +--> P2 Production Project Core [accepted on local main]
          |
         P2A State Commit Protocol [accepted on local main]
          |
          +----------+
          |          |
          v          v
     P3 Composition  P4 Voice
     + HyperFrames   + Captions
     [local main]    [local main]
          |          |
          +-----+----+
                v
       P5 Dependency Graph
       + Selective Rebuild
       [origin/main]
                |
                v
       P6 Review / Repair Runtime [local main]
                |                 \
                |                  +-- P7 Image Assets [local main]
                |                                   /
                v                                  v
       Base AI Comic E2E [local main]
                |\
                | +--> P8 Optional Generated Video
                |              |
                +--------------+--> P9 Production Hardening

P7 is not a P5 prerequisite and reuses the existing typed AssetRecord/P5 resolver contract.
```

P1、accepted P2/P2A/P3/P4/P5/P6/P7/P8 与 Base AI Comic E2E 当前都在 local `main`；P5 已进入 `origin/main`，后续 local commits 尚未 push/release。P7.1 offline/runtime-contract milestones 已实施；2026-08-18 fresh authorized M7 live-local rerun 已通过 Qwen 与 FLUX technical live acceptance（loopback only, one provider call per lane, replay adds zero），但 M8 blind benchmark 仍缺人工证据。开始后续 runtime slice 前必须记录 integration base decision，不能把 local ahead state 当作 GitHub truth，也不能无意丢失或重复已验收 commits。

## Phase Status and Gates

| Phase | Status | Depends On | Produces | Exit Gate |
| --- | --- | --- | --- | --- |
| P0 Product Reframe and Contract Migration | Completed in documentation scope | Current audit | New spec、roadmap、contract pointers、supersession and current baseline | All planning docs distinguish implemented vs proposed; no `src/**` change |
| P1 Legacy Runtime Stabilization | Implemented and present in `origin/main` via P5 ancestry；not released | Old P0 baseline | terminal failure persistence、direct chain invalidation、FPS contract、artifact rollback | Targeted tests + full no-network suite; branch/release state explicit |
| P2 Production Project Core | Accepted after independent review and present in `origin/main` via P5 ancestry；not released | P0; explicit Git base decision | v2 project schema、creative artifacts、read-only Asset Registry verification、Shot visual strategy、static validation | No writer/renderer/provider dependency; Legacy suite unchanged |
| P2A Production State Commit Protocol | Independently accepted and present in `origin/main` via P5 ancestry；not released | P2 | atomic v2 project/registry snapshot commit、Manifest pointer switch、explicit recovery、orphan/partial reporting | Crash-injection tests cover temporary snapshots and pointer switch; focused P2/P2A `286 passed`, Legacy regression `50 passed`, full no-network `378 passed, 1 skipped` |
| P3 Deterministic Composition and HyperFrames Adapter | 已实现、独立审查并完成 live verification；于 `3296b713` fast-forward 合入 local `main`并随P5 ancestry进入`origin/main`；尚未release | P2 + P2A | CompositionSpec、ResolvedTimeline、renderer selection/source/render receipts、Manifest 2.1 render-state lifecycle、local HyperFrames adapter | 固定 `0.7.103` + Chrome `152.0.7928.2`；committed two-image `10@24` CUT proof；focused `547 passed, 1 skipped`、Legacy `61 passed`、full `700 passed, 2 skipped` |
| P4 Voice and Captions | 已实现、独立审查并于 `f9eedaeb5f6432d8ba0bf937c78111dbcfa3ce80` fast-forward 合入 local `main`并随P5 ancestry进入`origin/main`；尚未release | P2 + P2A + P3 contract | 五种 audio kinds、immutable provider-neutral Voice contract、alignment、canonical CaptionTrack、ResolvedTimeline 2.1 audio/caption extensions、Manifest 2.2 / Registry 2.1 lifecycle、selected HyperFrames render | Deterministic fake/no-network acceptance；focused `1012 passed, 3 skipped`、merged-main full `1094 passed, 4 skipped`；无 live call、secret 或 quota 使用 |
| P5 Dependency Graph and Selective Rebuild | 已实现、独立审查并于 `0d663566c4db4542922e38d770608e3e02d53745` fast-forward 合入 local `main`并push到`origin/main`；尚未release | P2 + P3 + P4 contracts；P7不是前置 | immutable graph 2.0 typed nodes/edges/reasons/contributions、Manifest 2.3 desired/applied lifecycle、precise invalidation/frontier、P2A co-activation/recovery；composition/timeline/source/render只由同一次final render activation原子fresh | Required mutation matrix（含alignment policy与receipt-only对照）、migration/crash/recovery fixtures；canonical `1256 passed, 3 skipped`、Legacy `58 passed`、merged-main full `1386 passed, 4 skipped`；无network/secret/provider/renderer executable |
| P6 Codex Review and Repair Runtime | 已完成、独立 review `accept with concerns`并 fast-forward整合 local `main`；未 push/release | P3 + P5; P4 for audio/caption QA | Manifest 2.4、strategy-aware immutable Review/Repair/Outcome/Final Acceptance receipts、committer-owned one-use analysis/authorization、exact P5 repair invalidation | Focused `739 passed`、整合并行commits后full `1462 passed, 4 skipped`；无 blocking review issue；仅保留 future real activated-render reopen E2E concern |
| P7 Image Asset Generation | 已实现并合入 local `main`；未 push/release | P2 + P2A + existing P5 contract；不以P6为runtime前置 | provider-neutral local-only `ImageAssetProvider` contract、Manifest 2.5 image lifecycle、Character/Scene reuse、generated PNG provenance、target Shot/Registry/graph atomic activation | 原始 deterministic fake/no-network acceptance 保持；无 remote Provider、renderer或video generation |
| Base AI Comic E2E | 已完成 acceptance 并合入 local `main`；未 push/release | P3 + P4 + P5 + P6 + P7 contracts | no-Video-Provider two-Shot production proof：images、voice/captions、timeline/render、strategy-aware review、exact repair、Final Acceptance、restart reopen、zero-side-effect replay | 原 Task 8 focused `980 passed, 3 skipped`、full `1714 passed, 4 skipped`、Architecture Gate PASS；independent review `accept` |
| P7.1 Hybrid Local Image Production | Offline/runtime-contract implementation complete on local `main`; M7 technical live-accepted on 2026-08-18; M8 still pending | Accepted P7 + local ComfyUI inventory; M8 needs manual benchmark evidence; any further live smoke/benchmark needs fresh authorization | sealed loopback-only Qwen/FLUX profiles、pre-R+1 exact compatibility preflight、concrete local adapter、truthful human web-image import、Character/Scene/Shot/repair atomic activation | Static compatibility PASS；2026-08-18 fresh authorized rerun passed both Qwen and FLUX live-local (one provider call per lane, replay adds zero, loopback only, no remote/browser/secret)；earlier two sessions failed closed without blind retry；M8未运行 |
| P8 Optional Generated-Video Providers | Provider-neutral lifecycle、Paid Provider Gate、Fake、MiniMax cloud H3/Hailuo与Seedance full-model adapter已offline accepted；Alice C2 adaptive Hailuo request已完成canonical submit/poll/fetch/activation/recovery/replay；MiniMax cloud H3 live因余额不足`402 / code 1008`未成功；Seedance Mini有一次diagnostic succeeded/fetched，但tracked audio-opt-out payload、billing settlement与activation仍未live accepted；未push/release | Accepted Base AI Comic E2E + paid-provider safety gates | sealed request/capability/profile、durable submit/poll/fetch、measured provenance、Registry 2.2 / Project / P5 candidate activation与explicit recovery；七个official current/legacy-callable Seedance Model IDs；future Wan/ComfyUI local adapter compatibility | Activation closure `a089eac`；fresh staged/range P8 policy `742 passed`、production contracts `2036 passed, 3 skipped`、state `933 passed`、dependency `1045 passed`、Architecture Gate PASS；Seedance payload regression/Paid Provider/recovery `108 passed`；Base path在全部Video Providers禁用时仍可工作；fetch不自动activation |
| Provider-Neutral Generation Requirement | T1-T9 offline vertical slice已实现于`007e99c`并进入current cached `origin/main`；尚未release；后续独立授权的Local H3 initial technical regression与motion-continuity repair已完成，cloud/paid live未执行 | Accepted Video Planner、Shot Router与P8 lifecycle contracts | `video-planner/3` typed intent -> plan-internal hash-bound requirement -> verified projection -> Router-owned exact selection -> prompt-free provider-bound request -> deterministic adapter compile -> existing lifecycle；historical hashes保持兼容，old direct construction paths退役 | Pure/fake/offline mappings覆盖Local H3、cloud H3、Hailuo与Seedance video/audio roles，typed unsupported与zero side effects；implementation snapshot receipt `.agent/harness/runs/20260820T223106358086Z/receipt.json` passed；Local H3 evidence见`docs/record_for_agent/2026-08-21-local-h3-readiness-seed-live-regression.md`；不证明cloud connectivity、通用媒体质量、P6 Review或Final Acceptance |
| Shot Readiness Gate | Offline implementation已完成于local `main` change；未push/release；独立seed fix与Local H3 rerun不属于本slice | Current `video-planner/3` requirement projection；historical `/2` reopen fixtures | Planning-owned exact projection verifier -> Gate-owned ordered binding/eligibility/required-asset diagnostics -> compatibility façade；BLOCKED typed STOP且downstream side effects为零，READY只进入既有Router/Provider/lifecycle gates | Focused Gate/Planner/Router/P8 seam `241 passed`、Harness/Architecture tests `89 passed`、Architecture Gate PASS；仅证明structural readiness，不证明generation、activation、media quality、P6 Review/Pilot或Final Acceptance |
| Generated MP4 Composition Compatibility | 已实现并于2026-08-19完成本地technical acceptance；未push/release | P3 + P4 + P5 contracts；P8 registered/local-validated MP4 evidence | H.264 MP4 visual span、canonical frame/sample trim、muted HyperFrames media、既有P4 mix/caption mux、durable source/render activation | 三个Hailuo MP4合成`423@24fps`成片；`video-analysis`证明H.264 video + AAC audio；不扩大Provider activation/CLI/schema |
| Shot Continuity + Local H3 Quality + Shot Router | C1 direct terminal-first-frame与C2 hard-cut technical contracts已实现；Local H3 `fl2va` C1 technical live-local accepted且additive quality profile已冻结；Router pure runtime已能消费`exact_terminal`/`reference`/`semantic`/`none`四档Shot contract；Hailuo 2.3 adaptive C2 lane canonical technical accepted并以人工review `pass with minor concerns`；Local H3 C2 subjective acceptance与Seedance continuity live仍未完成；未push/release | P8 lifecycle + generated-MP4 compatibility + explicit C2 technical/live authorization + user-approved Router scope | C1 exact terminal binding；C2 `character + continuity_terminal` additive keyframe profile、hard-cut lineage与typed `terminal -> keyframe -> video` closure；Router selected exact Provider/profile capability必须满足continuity mode，denial不fallback；Hailuo I2V `768P` geometry按live truth为adaptive；Seedance只接受sealed Active Ark identity；不新增timeline/renderer/writer/CLI | Shared terminal `52596511…e19b`与keyframe `5072a65b…58fc`已激活；bounded Hailuo adaptive request取得`90b4fc18…bdca`并完成activation/recovery/zero-effect replay，首帧SSIM `0.941854`。H3 quality final为`05c00691…ae9e`，exact-terminal SHA为`2dd12cac…f27b`。H3 -> Hailuo no-new-call portability smoke证明exact first-frame binding、typed reference block与identity-free T2V；不等于新的Hailuo continuity视频或blinded three-lane acceptance |
| QualityExperienceRecord v1 Q0 Passive Capture | Minimum runtime slice已实现于current local change；未push/release；4–8 Shots Pilot尚未开始 | Accepted Quality Intelligence data foundation、QualityExperienceRecord v1 Spec/Plan与existing Production/P6/P8 strict readers | strict/frozen prospective与historical schemas、dataset-root-only immutable store、base/terminal Manifest-derived cohort/attempt roster、exact-closure Pilot index、hash/field exact lookup、pure sanitized advisory projection；ProductionStateCommitter仍是唯一Production writer | Q0 focused `156 passed`、projection + Agent Memory isolation `43 passed`、integrated no-side-effect suite `710 passed`、Harness route `63 passed`、Architecture Gate PASS；不调用Provider/renderer/analyzer/recovery/activation，不写Agent Memory index；不证明Pilot、RAG rebuild、automatic learning/selection/repair、P6 acceptance或Final Acceptance |
| Decision C-lite Composition Strategy Shadow | Experimental Development Governance slice；20–50 Shot Pilot尚未开始 | Current Video Planner、ShotReadinessGate、Shot Router与Quality Intelligence advisory evidence boundaries | Agent-facing declarative Composition Playbooks、typed non-canonical `CompositionStrategyProposal`与pure `MATCH / DIFFERENT / NOT_COMPARABLE` comparison；proposal/playbook不进入Production hashes、state或execution | 只有20–50个真实Shots在固定rubric下证明proposal改善结果、发现Planner重复盲点、Playbook重复使用/字段稳定，或disagreement与quality outcome相关，才可另行设计Stage 3；当前不实现Product Planning Validator、Planner schema integration、formal CompositionSkillLibrary、ExecutionTrace、Promotion Gate或Skill Evolution |
| P9 Episode and Production Hardening | Not planned for implementation yet | Accepted Base AI Comic E2E; accepted P8 only for video-enabled profile hardening | batching、cost rollups、retention、migration、performance/recovery hardening | Multiple episodes/revisions recover and reproduce accepted renders |

## Parallel Work Rules

以下 work 可在 contract 稳定后并行规划，但 concurrent writers 必须使用独立 branch/worktree：

- P3、P4 和 P7 都依赖 accepted P2 + P2A。P3-P7 与 Base AI Comic E2E 已在 local `main`；P7 不是P5前置。
- P3 负责 resolved visual/composition timebase 和 renderer boundary；accepted P4 只扩展 audio/caption data，generated-MP4 compatibility只扩展visual media kind与source trim，仍由同一个 `ResolvedTimeline` 独占 order/frame/sample/timing，不得另造 timeline owner。
- P7 只负责 image asset production/provenance；它已复用未修改的 P5 resolver，不拥有 renderer、video generation 或 automatic upstream-reference regeneration。
- P8 provider lifecycle与generated-MP4 composition compatibility是两个独立slice。后者只消费已注册/本地验证并满足exact metadata的H.264 MP4，已完成frame-accurate P3/P4 integration；它不使fetched candidate自动成为active Registry/Project/Graph asset，也不授权任何future cloud submit。Paid Provider Gate、Budget、Egress、secret redaction和crash-safe job persistence仍分别适用。

以下 work 不得并行落地：

- 两个 writer 同时修改 ProductionProject/Asset Registry schema owner。
- HyperFrames 和 Remotion 同时争夺 canonical renderer/default path。
- P5 invalidation resolver 与另一个 ad-hoc stale implementation 并存。
- P6 strategy-aware QA 与 current heuristic auto-edit 形成两条 production control path。

## Mandatory Gates

### Integration Base Gate

开始 P2A 时已执行并记录；任何后续 runtime slice 仍必须执行并记录：

```bash
git status --short --branch
git log --oneline --decorate -10
git rev-list --left-right --count origin/main...main
```

选择包含 accepted P1-P8 与 Base E2E 的 local `main` 作为后续slice base。不得从 `origin/main` 开始后无意遗漏 local-only commits，也不得把 local ahead state 当作 GitHub runtime truth。

### Renderer Gate

P3 Renderer Gate 已完成：project-local pin 为 `hyperframes@0.7.103`，验证浏览器为 Chrome Headless Shell `152.0.7928.2`；compatibility 与 committed-fixture proof 均在 fresh user/network/PID namespace、controlled env 和 full process/network trace 下通过。P3 未实现 Remotion adapter，也没有 alternate renderer fallback。

### V2 State Commit Gate

P2 只读取并验证已存在的 project、creative artifact、registry snapshot 和
Manifest pointer。P2A implements the accepted write/activation boundary and is present in `origin/main` via P5 ancestry. The implementation provides:

- temporary snapshot write + file fsync + promote-without-overwrite + parent-directory fsync + reopen verification；
- durable attempt lifecycle writes may record running/failed state; after snapshot verification, only the final active-pointer Manifest replace is the logical commit point；
- explicit recovery with crash-injection coverage for partial temporary files, complete orphan snapshots and pointer switch；
- no change to Legacy Manifest v1 owner。

### Paid Provider Gate

P4 已实现的 ElevenLabs thin adapter 与 P7 local injected image contract都不等于live-call authorization。任何 ElevenLabs/image real call（包括使用免费额度）或 P8 cloud video real call 前，必须另获明确授权并已有：

- explicit opt-in；
- budget upper bound and reservation；
- egress preview；
- secret redaction；
- crash-safe request/job receipt；
- fake transport tests；
- explicit live-test authorization。

### Base AI Comic Gate

该 gate 已通过并合入 local `main`，证明：

```text
Story -> reusable Character/Scene state -> image assets
-> voice -> CaptionTrack -> ResolvedTimeline
-> deterministic render -> strategy-aware QA
-> selective repair -> final.mp4
```

这个 proof 未配置 Video Provider，并在 fresh runtime 中 reopen exact durable evidence；exact replay 的 image、voice、analysis、renderer 与 Manifest write counters 全为零。Approved repair 只重建 composition/timeline/source/render closure，P7 image、Character/Scene、voice 与 caption identities 保持不变。它没有新增 schema、CLI、production coordinator、Provider、asset layout 或 runtime dependency。

## Slice Planning Rules

每个 phase implementation plan 必须：

1. 只覆盖一个可独立验收的 slice。
2. 指定 exact owner、old path to retire、unchanged contract 和 focused verification command。
3. 列出 schema migration 和 rollback；没有 migration 也要明确写 `none`。
4. 使用 fake/no-network tests 作为默认 CI。
5. 对 external tool version、license、API 和 pricing 在 plan 当天重新核对。
6. 不把 planned next phase identifier 引入 current runtime。
7. 完成后更新 baseline、README 和 contract matrix，但只写已验证 behavior。

## Recommended Next Slice

Base AI Comic E2E、P8 provider-neutral/Paid Provider lifecycle与H3/Hailuo/Seedance offline adapters均已完成对应local acceptance。Seedance Mini diagnostic已经证明cloud submit/poll/fetch connectivity，但下一步仍不是自动live：P7.1 M8、H3 live success、Seedance tracked-payload reverify/billing settlement与P9各自需要独立授权和证据；不能因offline/fake acceptance或既有Hailuo/Seedance success自动push/release、重复调用Provider、声称quality或activation acceptance。

P3 implementation 增加 `composition.py`、`hyperframes.py` 与既有 P2/P2A render-state extensions；P4增加audio/caption contracts；P5增加pure dependency resolver；P6增加pure review decisions和content-addressed receipts；P7增加pure `image.py` contract并通过既有 `ProductionStateCommitter` façade与private implementation modules持久化Manifest 2.5 lifecycle。Base E2E 只补齐 2.5 compatibility 与 combined durable proof。P7/Base E2E没有修改`dependency.py`，也没有增加第二writer/resolver、timeline、renderer、concrete/live Provider、video generation、CLI或通用Agent runtime。
