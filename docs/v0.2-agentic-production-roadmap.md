# AI-VIDEO v0.2 Agentic Production Roadmap

Status: Master dependency map approved as product direction on 2026-08-09. This document is not implementation authorization.
Target Spec: `docs/superpowers/specs/2026-08-09-ai-video-agentic-production-harness-v0.2.md`

## Purpose

本 roadmap 只定义 phase dependency、gate、parallel boundary 和 slice output。每个 runtime phase 仍需单独的 implementation plan、明确授权、tests、rollback 和 acceptance。不得把本文件当作一次性执行计划。

## Dependency Graph

```text
P0 Product Reframe and Contract Migration [docs complete]
 |
 +--> P1 Legacy Runtime Stabilization [implemented on local main]
 |      |
 |      +--> Integration Base Gate
 |             local main vs origin/main disposition must be explicit
 |
 +--> P2 Production Project Core [accepted on local main]
          |
         P2A State Commit Protocol [accepted on local main]
          |
          +----------+----------+
          |          |          |
          v          v          v
     P3 Composition  P4 Voice   P7 Image Assets
     + HyperFrames   + Captions
          |          |          |
          +-----+----+----------+
                v
       P5 Dependency Graph
       + Selective Rebuild
                |
                v
       P6 Review / Repair Harness
                |
                v
       Base AI Comic E2E
                |\
                | +--> P8 Optional Generated Video
                |              |
                +--------------+--> P9 Production Hardening
```

P1、accepted P2 与 accepted P2A 当前都在 local `main`，而 `origin/main` 尚未包含它们。开始后续 runtime slice 前必须记录 integration base decision，不能把 local ahead state 当作 GitHub truth，也不能无意丢失或重复已验收的 commits。

## Phase Status and Gates

| Phase | Status | Depends On | Produces | Exit Gate |
| --- | --- | --- | --- | --- |
| P0 Product Reframe and Contract Migration | Completed in documentation scope | Current audit | New spec、roadmap、contract pointers、supersession and current baseline | All planning docs distinguish implemented vs proposed; no `src/**` change |
| P1 Legacy Runtime Stabilization | Implemented on local `main`; remote integration pending | Old P0 baseline | terminal failure persistence、direct chain invalidation、FPS contract、artifact rollback | Targeted tests + full no-network suite; branch/release state explicit |
| P2 Production Project Core | Accepted after independent review on local `main`; origin integration pending | P0; explicit Git base decision | v2 project schema、creative artifacts、read-only Asset Registry verification、Shot visual strategy、static validation | No writer/renderer/provider dependency; Legacy suite unchanged |
| P2A Production State Commit Protocol | Independently accepted on local `main`; not pushed or released | P2 | atomic v2 project/registry snapshot commit、Manifest pointer switch、explicit recovery、orphan/partial reporting | Crash-injection tests cover temporary snapshots and pointer switch; focused P2/P2A `286 passed`, Legacy regression `50 passed`, full no-network `378 passed, 1 skipped` |
| P3 Deterministic Composition and HyperFrames Adapter | Planning artifact exists; P2A prerequisite is satisfied, but runtime remains blocked by Renderer Gate, dependency-install authorization, API reconciliation and new explicit user authorization | P2 + P2A | CompositionSpec、ResolvedTimeline、renderer selection receipt、HyperFrames adapter contract/render proof | One canonical renderer per run; image-only silent composition deterministic |
| P4 Voice and Captions | Not planned for implementation yet | P2 + P2A; may run parallel with P3 | Audio assets/tracks、Voice Provider contract、alignment、CaptionTrack、cost/egress preview | Voice/caption artifacts local and hashed; no implicit paid call |
| P5 Dependency Graph and Selective Rebuild | Not planned for implementation yet | P2 + P3 + P4 contracts | dependency edges、fingerprints、desired/applied state、invalidation resolver | Required mutation matrix rebuilds only affected graph |
| P6 Codex Review and Repair Harness | Not planned for implementation yet | P3 + P5; P4 for audio/caption QA | strategy-aware Review Receipt、Repair Receipt、final acceptance | review -> repair -> rerender proven on bounded defect |
| P7 Image Asset Generation | Not planned for implementation yet | P2 + P2A; may run parallel with P3/P4 | image tool/provider adapter、Character/Scene reuse、image provenance | One character/scene reuse path with no video generation |
| P8 Optional Generated-Video Providers | Not planned for implementation yet | Accepted Base AI Comic E2E (P2-P7) + paid-provider safety gates | Wan v2 adapter first, then separately approved cloud adapter | Base path remains functional with all video Providers disabled |
| P9 Episode and Production Hardening | Not planned for implementation yet | Accepted Base AI Comic E2E; accepted P8 only for video-enabled profile hardening | batching、cost rollups、retention、migration、performance/recovery hardening | Multiple episodes/revisions recover and reproduce accepted renders |

## Parallel Work Rules

以下 work 可在 contract 稳定后并行规划，但 concurrent writers 必须使用独立 branch/worktree：

- P3、P4 和 P7 都依赖 accepted P2 + P2A. P2A independent acceptance satisfies that prerequisite; each remaining slice still needs its own authorization and remaining gates before execution.
- P3 负责 resolved visual/composition timebase 和 renderer boundary；P4 负责 audio/caption data，不得另造 timeline owner。
- P7 只负责 image asset production/provenance，不得修改 renderer 或 dependency resolver。
- P8 的 read-only design exploration 或 plan drafting 可以在 P2/P5 contracts 稳定后开始，但任何 P8 runtime implementation 必须等待 Base AI Comic E2E acceptance；任何 cloud submit 还需 Budget、Egress 和 crash-safe job persistence gates。

以下 work 不得并行落地：

- 两个 writer 同时修改 ProductionProject/Asset Registry schema owner。
- HyperFrames 和 Remotion 同时争夺 canonical renderer/default path。
- P5 invalidation resolver 与另一个 ad-hoc stale implementation 并存。
- P6 strategy-aware QA 与 current heuristic auto-edit 形成两条 production control path。

## Mandatory Gates

### Integration Base Gate

开始 P2A 时已执行并记录；任何后续 runtime slice 仍必须执行并记录：

```bash
git status --short --branch
git log --oneline --decorate -10
git rev-list --left-right --count origin/main...main
```

选择包含 accepted P1/P2/P2A 的 local `main` 作为 base，或先明确完成 remote integration。不得从 `origin/main` 开始后无意遗漏已验收 slices，也不得把 local ahead state 当作 GitHub runtime truth。

### Renderer Gate

P3 必须先验证 HyperFrames 的当前 plugin/CLI/version/license/runtime requirements，再决定 installation mode。该 gate 需要单独授权安装 Node dependency/tool。P3 不得顺带实现 Remotion adapter。

### V2 State Commit Gate

P2 只读取并验证已存在的 project、creative artifact、registry snapshot 和
Manifest pointer。P2A now implements the accepted write/activation boundary on local `main`; remote integration remains separate from the P3 execution gates. The implementation provides:

- temporary snapshot write + file fsync + promote-without-overwrite + parent-directory fsync + reopen verification；
- durable attempt lifecycle writes may record running/failed state; after snapshot verification, only the final active-pointer Manifest replace is the logical commit point；
- explicit recovery with crash-injection coverage for partial temporary files, complete orphan snapshots and pointer switch；
- no change to Legacy Manifest v1 owner。

### Paid Provider Gate

P4 的 ElevenLabs real call 或 P8 的 cloud video real call 前，必须已有：

- explicit opt-in；
- budget upper bound and reservation；
- egress preview；
- secret redaction；
- crash-safe request/job receipt；
- fake transport tests；
- explicit live-test authorization。

### Base AI Comic Gate

P8 之前必须证明：

```text
Story -> reusable Character/Scene state -> image assets
-> voice -> CaptionTrack -> ResolvedTimeline
-> deterministic render -> strategy-aware QA
-> selective repair -> final.mp4
```

这个 proof 不得配置 Video Provider。

## Slice Planning Rules

每个 phase implementation plan 必须：

1. 只覆盖一个可独立验收的 slice。
2. 指定 exact owner、old path to retire、unchanged contract 和 focused verification command。
3. 列出 schema migration 和 rollback；没有 migration 也要明确写 `none`。
4. 使用 fake/no-network tests 作为默认 CI。
5. 对 external tool version、license、API 和 pricing 在 plan 当天重新核对。
6. 不把 planned next phase identifier 引入 current runtime。
7. 完成后更新 baseline、README 和 contract matrix，但只写已验证 behavior。

## Recommended Next Slice

P0 文档迁移已完成，P1、independently accepted P2 和 independently accepted P2A 已在 local `main`，但尚未 push/release。P3 implementation is not yet authorized: its remaining stop gates are Renderer Gate verification, separate Node/tool installation authorization, API reconciliation, and a new explicit user authorization. Do not infer that HyperFrames is installed or that P3 is ready to execute.

P2A implementation file map: `src/ai_video/production/models.py`, `src/ai_video/production/project.py`, `src/ai_video/production/state_commit.py`, `src/ai_video/production/__init__.py`, `tests/production_project_factory.py`, `tests/test_production_models.py`, `tests/test_production_project.py`, `tests/test_production_validation.py`, `tests/test_production_state_commit.py`, `tests/test_production_state_recovery.py`, and `tests/helpers/p2a_crash_worker.py`.
